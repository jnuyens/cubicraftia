[plugin]

name="Build Stamp"
description="Auto-stamps res://version.txt with the git build id (sha + date) on every export and play-in-editor, so the in-game build label is always current."
author="Cubicraftia contributors"
version="1.0"
script="build_stamp.gd"
