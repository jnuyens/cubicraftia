---
phase: 09-nl-localisation-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locale/nl.po
  - assets/profanity/wordlist_nl.txt
  - .planning/phases/09-nl-localisation-review/09-01-REVIEW.md
autonomous: true
requirements:
  - LOCALE-01
  - LOCALE-02

must_haves:
  truths:
    - "Every msgid present in en.po also has a non-empty NL msgstr in nl.po (no untranslated strings)"
    - "The NL profanity wordlist contains no typoed forms and no obvious false-positive innocent Dutch words"
    - "All NL msgstr entries use builder->bouwer, keep brick/stud as brand terms (no steen/noppen), never mention Lego or Minecraft in player-visible text"
    - "A side-by-side EN/NL review document exists at 09-01-REVIEW.md for the human sign-off in Plan 09-02"
    - "The nl.po file header comment correctly states the brand-term mapping (stud stays stud, brick stays brick)"
  artifacts:
    - path: "locale/nl.po"
      provides: "Corrected Dutch translation — 0 empty msgstr for real strings, all EN msgids covered, terminology compliant"
    - path: "assets/profanity/wordlist_nl.txt"
      provides: "Corrected NL profanity wordlist — typos fixed, false positives removed, coverage improved"
    - path: ".planning/phases/09-nl-localisation-review/09-01-REVIEW.md"
      provides: "Side-by-side EN/NL review artifact for human sign-off"
  key_links:
    - from: "locale/nl.po"
      to: "src/autoload/translations.gd"
      via: "_load_po() at startup — only valid msgstr entries (non-empty) are registered"
      pattern: "translation.add_message\\(current_msgid, msgstr\\)"
    - from: "assets/profanity/wordlist_nl.txt"
      to: "src/networking/profanity_filter.gd"
      via: "_load_list() reads file at runtime, compiles _regex_nl"
      pattern: "_load_list.*wordlist_nl"
---

<objective>
AI performs the thorough first-pass review of all NL strings and the profanity wordlist (per D-01), proposes and applies corrections, and produces a side-by-side review document for the human native-speaker sign-off that follows in Plan 09-02.

Purpose: Deliver a corrected nl.po and wordlist_nl.txt — correct register, correct terminology, no gaps, no banned brand names in player-visible copy — ready for the native speaker to review and approve.

Output:
  - locale/nl.po — all EN msgids covered, terminology compliant per D-02/D-03, empty strings filled
  - assets/profanity/wordlist_nl.txt — typos fixed, false positives annotated/removed, Flemish coverage improved
  - .planning/phases/09-nl-localisation-review/09-01-REVIEW.md — side-by-side document for Plan 09-02 sign-off
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-nl-localisation-review/09-CONTEXT.md

<interfaces>
<!-- Key facts extracted from source files for this executor. No further exploration needed. -->

From locale/nl.po (current state):
  - 480 msgid entries (including header). 478 real strings.
  - 2 intentionally empty msgstr entries that the translation loader SKIPS (translations.gd line 102: "Empty msgstr is intentional for transitional keys — skip"):
      ui.signin.age_checkbox  (EN: "I am 13 or older.")   -- NEEDS NL translation
      ui.username.inline_valid (EN: empty string "")       -- EN is also empty; keep NL empty (intentional)
  - Header comment (line 6) says: "nop = stud" — INCORRECT per D-03. Fix to: "stud = 'stud' (brand term, keep untranslated), brick = 'brick' (brand term), bouwer = builder"

From locale/en.po vs locale/nl.po gap analysis (run before starting):
  72 msgids in en.po have NO corresponding entry in nl.po at all:
    bricks.* missing (44): axe, hammer, fishing_rod, compass, map, gold_ingot, coal, leather, string,
      wheat, sugar_cane, obsidian, furnace, bucket, sugar, bread, campfire, coal_block, bow,
      flint_and_steel, chestplate_iron, chestplate_leather, helmet_diamond, helmet_gold, nether_portal,
      raw_meat, sashimi, crafting_table_advanced, painting_small_1, painting_small_2, flower_pot,
      pot_red_flower, pot_blue_flower, pot_yellow_flower, pot_white_flower, pot_pink_flower,
      well, barrel, bookshelf, crate, chair_wood, anvil, table_wood
    recipes.* missing (17): pickaxe_stone, pickaxe_iron, pickaxe_diamond, pickaxe_gold,
      shovel_stone, shovel_iron, shovel_diamond, shovel_gold, sword_diamond, stone_block,
      glass_block, furnace, bucket, bread, sugar, campfire, coal_block, bow, flint_and_steel,
      chestplate_iron, chestplate_leather, helmet_diamond, helmet_gold, nether_portal
    ui.* missing (5): ui.builder.placed, ui.bed.picked_up, ui.crop.harvest_prompt,
      ui.title.customize, ui.mystery.orb
    Note: bricks.raw_meat and bricks.sashimi have no NL entries either.

From assets/profanity/wordlist_nl.txt (issues found):
  Typos confirmed:
    - Line 23: "homseksueel" → should be "homosexueel"
    - Line 63: "smerla" → likely "smeerlap" (common Flemish insult); "smerla" is not a real word
    - Line 65: "stomaak" → not a real Dutch word; likely meant "stomme maak" (no) or should be removed/replaced
  Probable false positives for gaming context (innocent in most NL sentences):
    - "gore" (means "grimy/dirty" but extremely common — "gore hekel" = strong dislike, not profanity)
    - "ras" (means "race/breed" — innocent in animal/gaming context; only slur as racial noun; risky FP)
    - "vent" (means "guy/bloke" — completely innocent)
    - "vieze" (means "dirty/gross" — innocent adjective; "vieze peer" etc.)
    - "vuil" (means "dirty/filthy" — too broad; FP in "vuile lucht" weather dialogue)
    - "waardeloos" (means "worthless" — innocent in gaming frustration, not profanity)
    - "prutsers" (means "bunglers" — mild, used in normal speech)
    - "puffer" (means "puffer jacket" or "one who puffs" — no profanity meaning in NL)
    - "plasser" (means "one who pees" — very mild, child-safe word for urination; used in kids' books)
    - "idioot" (common everyday word; used in frustration but not profane in NL/Flemish)
  Missing coverage (common Flemish/Dutch profanity not in list):
    - "godverdomme" / "godver" (very common Flemish oath)
    - "verdomme" (common oath)
    - "klere" / "klerezooi" (common Flemish profanity)
    - "tering" (common Dutch profanity similar to "kanker")
    - "tyfus" / "tyfuslijer" (Dutch disease-based profanity)
    - "eikel" (very common Dutch insult)
    - "lul" is present but "lullig" (mild) is not — no action needed
    - "seksist" / "seksistisch" not needed (descriptive, not profane)
    - "kukel" / "zakkerig" — optional additions

From src/networking/profanity_filter.gd:
  - Filter uses \b word-boundary regex; multi-word entries like "jullie hoeren" work because the regex wraps the whole alternation
  - Words with spaces in them ARE supported by the current _compile_regex implementation (the \b anchors surround the whole phrase including spaces)
  - 500-word limit (T-05-P2): NL wordlist currently has ~72 real entries — well within limit

From src/autoload/translations.gd:
  - translations.gd _load_po() skips blank msgstr ("") — so ui.username.inline_valid should remain empty (EN is also empty)
  - translations.gd valid prefixes: ui.*, bricks.*, device.*, toast.*, chests.*, items.*
  - The "recipes.*" namespace is NOT in _VALID_PREFIXES — recipe translation keys exist in both po files but will trigger a push_error in debug builds. This is a pre-existing issue; do not fix it in this phase (out of scope).

Brand-term mapping per D-03:
  - builder → bouwer (translate)
  - brick → brick (keep as brand term, do NOT write "steen")
  - stud → stud (keep as brand term, do NOT write "nop")
  - Cubicraftia → Cubicraftia (unchanged)
  - Lego / Minecraft → NEVER in player-visible msgstr (only in legal disclaimer which is allowlisted)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AI first-pass review — correct nl.po (all strings, terminology, gaps)</name>
  <files>locale/nl.po</files>
  <read_first>
    locale/nl.po (current file — read fully before editing)
    locale/en.po (EN source for accuracy reference)
    .planning/phases/09-nl-localisation-review/09-CONTEXT.md (decisions D-02, D-03 — register + terminology)
  </read_first>
  <action>
Read locale/nl.po in full. Then perform the following corrections and additions — edit msgstr values only, never touch msgid:

PART A — Fix the header comment (line 6):
  Change: "# Terminology: brick = "brick" (merknaam), nop = stud, bouwer = builder"
  To:     "# Terminology: brick = 'brick' (brand term, keep untranslated), stud = 'stud' (brand term, keep untranslated), builder -> bouwer"
  This corrects the "nop = stud" confusion per D-03: stud is a brand term kept as "stud", NOT translated to "nop".

PART B — Fill ui.signin.age_checkbox (currently empty, EN: "I am 13 or older."):
  Correct NL per D-02 informal register: "Ik ben 13 jaar of ouder."

PART C — Add 72 missing msgid/msgstr pairs (from the gap list in <interfaces>).
  Insert these after the existing entries, before the final blank line. Group them by namespace with a comment header.
  Use D-02 informal register (je/jij) throughout. Use D-03 terminology (brick stays brick, stud stays stud, bouwer for builder).

  Translation guidance for each group:

  Brick names (add after existing bricks.* block):
    bricks.raw_meat.name → "Rauw vlees"
    bricks.sashimi.name → "Sashimi"
    bricks.gold_ingot.name → "Goudstaaf"
    bricks.coal.name → "Steenkool"
    bricks.leather.name → "Leer"
    bricks.string.name → "Touw"
    bricks.wheat.name → "Tarwe"
    bricks.sugar_cane.name → "Suikerriet"
    bricks.obsidian.name → "Obsidiaan"
    bricks.furnace.name → "Oven"
    bricks.bucket.name → "Emmer"
    bricks.sugar.name → "Suiker"
    bricks.bread.name → "Brood"
    bricks.campfire.name → "Kampvuur"
    bricks.coal_block.name → "Steenkoolblok"
    bricks.bow.name → "Boog"
    bricks.flint_and_steel.name → "Vuursteen en staal"
    bricks.chestplate_iron.name → "IJzeren borstplaat"
    bricks.chestplate_leather.name → "Leren borstplaat"
    bricks.helmet_diamond.name → "Diamanten helm"
    bricks.helmet_gold.name → "Gouden helm"
    bricks.nether_portal.name → "Portaal"
    bricks.axe.name → "Bijl"
    bricks.hammer.name → "Hamer"
    bricks.fishing_rod.name → "Hengel"
    bricks.compass.name → "Kompas"
    bricks.map.name → "Kaart"
    bricks.crafting_table_advanced.name → "Geavanceerde werkbank"
    bricks.painting_small_1.name → "Schilderij (Berg)"
    bricks.painting_small_2.name → "Schilderij (Nacht)"
    bricks.flower_pot.name → "Bloempot"
    bricks.pot_red_flower.name → "Rode bloempot"
    bricks.pot_blue_flower.name → "Blauwe bloempot"
    bricks.pot_yellow_flower.name → "Gele bloempot"
    bricks.pot_white_flower.name → "Witte bloempot"
    bricks.pot_pink_flower.name → "Roze bloempot"
    bricks.well.name → "Waterput"
    bricks.barrel.name → "Ton"
    bricks.bookshelf.name → "Boekenkast"
    bricks.crate.name → "Krat"
    bricks.chair_wood.name → "Houten stoel"
    bricks.anvil.name → "Aambeeld"
    bricks.table_wood.name → "Houten tafel"

  Recipe names (add after existing recipes.* block):
    recipes.pickaxe_stone.name → "Stenen houweel"
    recipes.pickaxe_iron.name → "IJzeren houweel"
    recipes.pickaxe_diamond.name → "Diamanten houweel"
    recipes.pickaxe_gold.name → "Gouden houweel"
    recipes.shovel_stone.name → "Stenen schop"
    recipes.shovel_iron.name → "IJzeren schop"
    recipes.shovel_diamond.name → "Diamanten schop"
    recipes.shovel_gold.name → "Gouden schop"
    recipes.sword_diamond.name → "Diamanten zwaard"
    recipes.stone_block.name → "Steenblok"
    recipes.glass_block.name → "Glasblok"
    recipes.furnace.name → "Oven"
    recipes.bucket.name → "Emmer"
    recipes.bread.name → "Brood"
    recipes.sugar.name → "Suiker"
    recipes.campfire.name → "Kampvuur"
    recipes.coal_block.name → "Steenkoolblok"
    recipes.bow.name → "Boog"
    recipes.flint_and_steel.name → "Vuursteen en staal"
    recipes.chestplate_iron.name → "IJzeren borstplaat"
    recipes.chestplate_leather.name → "Leren borstplaat"
    recipes.helmet_diamond.name → "Diamanten helm"
    recipes.helmet_gold.name → "Gouden helm"
    recipes.nether_portal.name → "Portaal"

  UI strings:
    ui.builder.placed → "Geplaatst!"
    ui.bed.picked_up → "Bed opgeraapt"
    ui.crop.harvest_prompt → "Houd SHIFT ingedrukt om te oogsten"
    ui.title.customize → "Bouwer aanpassen"
    ui.mystery.orb → "Een groene bol schiet door de lucht… en laat alleen vragen achter."

PART D — Review ALL existing msgstr entries for:
  1. Terminology violations: any occurrence of "nop" or "noppen" for stud → change to "stud". Any "steen" used where "brick" is the brand name → change to "brick". Any "builder" left in English → change to "bouwer".
  2. Register violations: any use of "u" (formal) → change to "je" per D-02.
  3. Accuracy issues: compare each NL msgstr against the EN msgstr to catch obvious mistranslations.
  4. Known issues to fix:
     - "ui.hud.mode.sandbox" currently says "Creatief" but EN is "Sandbox" and the mode is called "Sandbox" in the game (not "Creative"). NL should be "Vrij bouwen" (free-build) or keep as "Sandbox" (brand-neutral, widely understood by NL gamers). Use "Sandbox" to match the game's own mode name and avoid confusion with "world_select.mode_creative" which also says "Creatief".
       Current: "Creatief" → Correct to: "Sandbox"
     - "ui.world_select.mode_creative" says "Creatief" — that IS the creative/sandbox mode label on the world select screen. This is actually fine since it refers to the world creation mode picker label; keep as "Creatief".
     - "ui.crafting.inline.heading" → "Ambachtstafel (2 bij 2)": "Ambachtstafel" is a reasonable translation for "Crafting" workbench but slightly verbose for inline 2x2. This is a style note for the human reviewer — do NOT change autonomously.
  5. Truncation risk: NL strings that are significantly longer than their EN counterpart (more than ~40% longer) — annotate these in the review document (Task 2); do not shorten autonomously unless obviously padded.

Write the corrected nl.po using the Edit tool for individual corrections. When adding the large block of 72 new entries, use a single Edit targeting the end of the file (after the last existing msgstr entry).

Constraint: never change msgid values. Only edit msgstr.
  </action>
  <verify>
    <automated>python3 -c "
import re
with open('locale/nl.po') as f:
    content = f.read()
pairs = re.findall(r'msgid \"(.*?)\"\nmsgstr \"(.*?)\"', content)
# ui.username.inline_valid is intentionally empty (EN is also empty)
empty = [(i,s) for i,s in pairs if i not in ('','ui.username.inline_valid') and s == '']
assert len(empty) == 0, f'Still has empty msgstr: {empty}'
# Check header fix
assert 'nop = stud' not in content, 'Header still says nop = stud'
# Check no 'noppen' or 'nop' as translation for stud in msgstr lines
msgstr_lines = [l for l in content.splitlines() if l.startswith('msgstr')]
for l in msgstr_lines:
    assert 'noppen' not in l.lower(), f'Found noppen in msgstr: {l}'
# Check bricks.axe.name present
assert 'msgid \"bricks.axe.name\"' in content, 'bricks.axe.name missing'
assert 'msgid \"ui.title.customize\"' in content, 'ui.title.customize missing'
print('PASS: nl.po corrections verified')
"
    </automated>
  </verify>
  <done>
    - locale/nl.po header comment corrected (stud terminology)
    - ui.signin.age_checkbox has a NL msgstr
    - All 72 previously missing msgids now present with NL translations
    - No remaining empty msgstr for real strings (only ui.username.inline_valid remains empty, matching EN)
    - "nop"/"noppen" does not appear in any msgstr
    - "Sandbox" mode correctly uses "Sandbox" not "Creatief" in ui.hud.mode.sandbox
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Correct NL profanity wordlist + produce side-by-side review document</name>
  <files>
    assets/profanity/wordlist_nl.txt
    .planning/phases/09-nl-localisation-review/09-01-REVIEW.md
  </files>
  <read_first>
    assets/profanity/wordlist_nl.txt (current file)
    assets/profanity/wordlist_en.txt (EN reference for parity check)
    src/networking/profanity_filter.gd (filter behaviour — 500-word limit, \b anchors)
    locale/nl.po (corrected in Task 1 — read to build the side-by-side document)
    locale/en.po (EN source for the side-by-side document)
  </read_first>
  <action>
PART A — Correct assets/profanity/wordlist_nl.txt:

Remove false positives (common innocent Dutch words that would block normal chat):
  - Remove: gore, ras, vent, vieze, vuil, waardeloos, prutsers, puffer, plasser, idioot
  Rationale: these are ordinary Dutch words used constantly in innocent gaming chat. A kid saying
  "wat een gore hekel heb ik aan springen" or "wat een waardeloze zet" must not be blocked.

Fix typos:
  - Line 23: change "homseksueel" → "homosexueel"
  - Line 63: change "smerla" → "smeerlap"
  - Line 65: remove "stomaak" (not a real word; no clear intended replacement identifiable)

Add missing common Flemish/Dutch profanity that the list lacks:
  Add these new entries (sorted into the alphabetical run):
    eikel
    godver
    godverdomme
    klere
    klerezooi
    tering
    tyfus

Verify word count stays under 500 after edits (T-05-P2 Tier-3 mobile constraint):
  Count non-comment, non-blank lines and confirm < 500.

After edits, sort the non-comment entries alphabetically within the existing structure
(keep the header comment block intact at top).

PART B — Produce .planning/phases/09-nl-localisation-review/09-01-REVIEW.md:

This document is the formal sign-off artifact for the human native speaker (Plan 09-02, D-04a).
Create it with the following structure:

```
# Phase 9 NL Localisation Review — Side-by-Side Sign-Off Document
**Generated:** 2026-06-21  **Status:** Awaiting human sign-off (Plan 09-02)

## Instructions for Reviewer
You are a native Flemish/Dutch speaker reviewing these translations.
For each section: read the EN string, check the NL translation, and note any changes.
Mark issues with [CHANGE: new text] or [OK] inline.
Register: informal (je/jij), kid-friendly, Flemish-first where a neutral exists.
Terminology: brick = "brick" (keep), stud = "stud" (keep), builder = "bouwer".

## Summary of AI Changes Applied
[list the key corrections made in Task 1 and Task 2 here]

## Section 1: UI — Title Screen & Navigation
| msgid | EN | NL (AI-corrected) | Notes |
[table rows for all ui.title.*, ui.deeplink.*, ui.signin.back_to_title]

## Section 2: Avatar Creator
[table rows for all ui.avatar.*]

## Section 3: World Select & New World
[table rows for all ui.world_select.*, ui.new_world.*]

## Section 4: FTUE
[table rows for all ui.ftue.*]

## Section 5: HUD & In-World
[table rows for ui.builder.*, ui.hotbar.*, ui.hud.*, ui.camera.*, ui.world.*, ui.weather.*, ui.tool.*]

## Section 6: Inventory, Crafting, Chest, Recipes
[table rows for ui.inventory.*, ui.crafting.*, ui.workbench.*, ui.chest.*, ui.recipes.*, ui.item.*, ui.bed.*, ui.strawberry.*, ui.death.*, ui.death_pile.*, ui.hp.*, ui.sleep.*]

## Section 7: Multiplayer — Sign-in, Friends, Session, Invite, Join
[table rows for ui.signin.*, ui.friends.*, ui.session.*, ui.invite.*, ui.join.*]

## Section 8: Multiplayer — Players Tab, Chat, Handover, Network
[table rows for ui.players.*, ui.chat.*, ui.handover.*, ui.netstatus.*]

## Section 9: Safety & Moderation
[table rows for ui.block.*, ui.report.*, ui.consent.*, ui.legal.*, ui.restricted.*, ui.username.*, ui.nameplate.*]

## Section 10: Settings
[table rows for ui.settings.*]

## Section 11: Brick Names
[table rows for bricks.* — ALL entries including the 44 newly added]

## Section 12: Recipe Names
[table rows for recipes.* — ALL entries including the 24 newly added]

## Section 13: Other (creatures, chests, items, food)
[table rows for creatures.*, chests.*, items.*]

## Section 14: Profanity Wordlist Review
### Words Removed (false positives)
[list the 10 removed words with brief rationale]

### Typos Fixed
[list the 3 fixes]

### Words Added (coverage gaps)
[list the 7 new Flemish profanity additions]

### Full Wordlist After Corrections
[verbatim content of corrected wordlist_nl.txt]

## Section 15: Truncation Risk Flags
[list any NL strings that are >40% longer than their EN counterpart]
```

For the table rows, use format: `| msgid | EN text | NL text | [CHANGE applied / AI note] |`
Include ALL msgid/msgstr pairs from both en.po and the corrected nl.po.
Flag any entries where the AI applied a substantive correction with "[AI changed: ...]".
Flag any entries where the AI is uncertain about the Flemish preference with "[REVIEWER: check Flemish preference]".
  </action>
  <verify>
    <automated>python3 -c "
# Verify wordlist corrections
with open('assets/profanity/wordlist_nl.txt') as f:
    lines = [l.strip() for l in f if l.strip() and not l.startswith('#')]
word_set = set(lines)
# Typos should be gone
assert 'homseksueel' not in word_set, 'homseksueel still present'
assert 'smerla' not in word_set, 'smerla still present'
assert 'stomaak' not in word_set, 'stomaak still present'
# Fixes applied
assert 'homosexueel' in word_set, 'homosexueel missing'
assert 'smeerlap' in word_set, 'smeerlap missing'
# False positives removed
for fp in ['vent', 'vuil', 'waardeloos', 'prutsers', 'puffer', 'plasser', 'idioot']:
    assert fp not in word_set, f'{fp} still in list (false positive)'
# Count under 500
assert len(lines) < 500, f'Word count {len(lines)} exceeds 500 (T-05-P2)'
# New entries added
assert 'eikel' in word_set, 'eikel missing'
assert 'godverdomme' in word_set, 'godverdomme missing'
print(f'PASS: wordlist has {len(lines)} words — all corrections verified')
"
    </automated>
    <automated>test -f .planning/phases/09-nl-localisation-review/09-01-REVIEW.md && echo "REVIEW.md exists" || echo "FAIL: REVIEW.md missing"</automated>
  </verify>
  <done>
    - wordlist_nl.txt: 3 typos fixed (homseksueel→homosexueel, smerla→smeerlap, stomaak removed), 10 false positives removed, 7 Flemish coverage additions, total remains under 500 words
    - 09-01-REVIEW.md exists with all 15 sections, contains every msgid from both PO files in tabular form for human sign-off
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wordlist_nl.txt → regex engine | File is bundled in .pck; attacker would need APK modification to inject regex-DoS patterns |
| nl.po → TranslationServer | .po loaded at runtime; translation strings appear verbatim in UI — XSS not applicable in Godot (no HTML renderer), but brand-name violations (Lego/Minecraft) in UI copy would be a PR/trademark risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | wordlist_nl.txt | accept | File bundled in signed .pck; modification requires APK tampering which is out of scope for this phase |
| T-09-02 | Information Disclosure | nl.po disclaimer msgstr | mitigate | Legal disclaimer msgstr intentionally includes "LEGO Groep" and "Mojang Studios" for trademark compliance — this is allowlisted (nl-po-allowlisted-glossary decision in STATE.md); verify no OTHER msgstr contains Lego/Minecraft |
| T-09-03 | Tampering | wordlist false-positive removal | accept | Removing false positives could allow some mild language; reviewed by native speaker in Plan 09-02; risk is UX friction if over-blocking, not security |
</threat_model>

<verification>
Run after completing both tasks:
```
python3 -c "
import re
# nl.po checks
with open('locale/nl.po') as f:
    nl = f.read()
with open('locale/en.po') as f:
    en = f.read()
en_ids = set(re.findall(r'^msgid \"(.+?)\"', en, re.MULTILINE))
nl_ids = set(re.findall(r'^msgid \"(.+?)\"', nl, re.MULTILINE))
missing = en_ids - nl_ids
assert not missing, f'Still missing from nl.po: {missing}'
# Check no 'nop' in msgstr
for line in nl.splitlines():
    if line.startswith('msgstr'):
        assert 'noppen' not in line.lower(), f'noppen in msgstr: {line}'
print('nl.po: PASS')

# wordlist checks
with open('assets/profanity/wordlist_nl.txt') as f:
    wl = [l.strip() for l in f if l.strip() and not l.startswith('#')]
assert len(wl) < 500, f'wordlist too long: {len(wl)}'
assert 'homseksueel' not in wl
assert 'homosexueel' in wl
print(f'wordlist: PASS ({len(wl)} words)')

# Review doc exists
import os
assert os.path.exists('.planning/phases/09-nl-localisation-review/09-01-REVIEW.md')
print('review doc: PASS')
"
```
</verification>

<success_criteria>
1. locale/nl.po has ZERO empty msgstr entries for real strings (only ui.username.inline_valid remains empty, mirroring EN)
2. Every msgid in locale/en.po also appears in locale/nl.po
3. No msgstr line contains "noppen" or translates "stud" to a Dutch word
4. No player-visible msgstr contains "Lego" or "Minecraft" outside the legal disclaimer (which is allowlisted)
5. assets/profanity/wordlist_nl.txt has < 500 words; the 3 typos are fixed; 10 false positives are removed; 7 Flemish words are added
6. .planning/phases/09-nl-localisation-review/09-01-REVIEW.md exists with all 15 sections and every msgid tabulated
</success_criteria>

<output>
Create `.planning/phases/09-nl-localisation-review/09-01-SUMMARY.md` when done
</output>
