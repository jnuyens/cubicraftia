---
phase: 09-nl-localisation-review
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - tests/unit/test_profanity_multilang.gd
  - .planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md
autonomous: true
requirements:
  - LOCALE-03

must_haves:
  truths:
    - "The profanity filter regression tests pass with the corrected NL wordlist"
    - "A screenshot record exists documenting NL text rendering on all required in-game surfaces"
    - "No NL string appears untranslated (showing raw key like 'ui.builder.place') on any surface"
    - "No NL string is visually truncated or overflows its UI container on any surface"
  artifacts:
    - path: "tests/unit/test_profanity_multilang.gd"
      provides: "NL-coverage regression tests for the corrected wordlist"
    - path: ".planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md"
      provides: "Screenshot manifest documenting in-game NL rendering pass results"
  key_links:
    - from: "tests/unit/test_profanity_multilang.gd"
      to: "assets/profanity/wordlist_nl.txt"
      via: "ProfanityFilter.load_word_lists() loads wordlist at test time"
      pattern: "load_word_lists\\(\\)"
    - from: "src/autoload/translations.gd"
      to: "locale/nl.po"
      via: "_load_po() — locale is set to 'nl' by forcing OS locale or settings.cfg"
      pattern: "TranslationServer.set_locale"
---

<objective>
Verify the corrected NL locale in-game: profanity filter regression tests pass with the new wordlist, and a screenshot pass across all text-bearing surfaces confirms no untranslated, truncated, or garbled strings.

Purpose: Satisfy LOCALE-03 — in-game correctness of the NL locale confirmed, not just file-level correctness.

Output:
  - tests/unit/test_profanity_multilang.gd — extended with NL-specific wordlist coverage tests
  - .planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md — screenshot manifest
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-nl-localisation-review/09-CONTEXT.md
@.planning/phases/09-nl-localisation-review/09-01-REVIEW.md

<interfaces>
<!-- Key facts for this executor. No further exploration needed. -->

From src/networking/profanity_filter.gd:
  - ProfanityFilter.load_word_lists() loads both wordlist_en.txt and wordlist_nl.txt from res://
  - ProfanityFilter.filter(text) → replaces matches with "[filtered]"
  - ProfanityFilter.filter_reject(text) → bool
  - Static vars _regex_en/_regex_nl/_regex_custom must be reset to null before each test (see existing test pattern)

From tests/unit/test_profanity_multilang.gd (existing tests):
  - Already tests "kut" and "lul" for NL filter
  - Uses before_each() to reset static vars
  - Extends GutTest
  - test_filter_nl_word_after_wordlist_load() and test_filter_reject_returns_true_for_nl_word() are the NL tests

From assets/profanity/wordlist_nl.txt (post-09-02 corrections):
  - Typos fixed: homosexueel (was homseksueel), smeerlap (was smerla), stomaak removed
  - False positives removed: vent, vuil, waardeloos, etc.
  - New words added: eikel, godverdomme, godver, klere, klerezooi, tering, tyfus

From src/autoload/translations.gd:
  - Locale set to "nl" via user://settings.cfg [settings] locale = "nl" OR OS locale "nl"
  - To force NL in headless test: write a temp settings.cfg with locale=nl before running Godot
  - Translations autoload calls TranslationServer.set_locale() at _ready()

For the screenshot pass:
  - Build the game in export mode OR run in editor with NL locale forced
  - Force NL locale: write user://settings.cfg (in OS user data dir) with:
      [settings]
      locale = "nl"
  - On macOS, user data dir: ~/Library/Application Support/Godot/app_userdata/Cubicraftia/
  - Screenshot surfaces (all required per D-04b):
      S1: Title screen (ui.title.* strings)
      S2: Avatar creator (ui.avatar.* strings)
      S3: World select screen (ui.world_select.* strings)
      S4: New world modal (ui.new_world.* strings)
      S5: FTUE overlay (ui.ftue.* strings)
      S6: HUD in-game (ui.hotbar.*, ui.hud.mode.*, ui.hp.bar_sr, ui.builder.*)
      S7: Chat overlay (ui.chat.*)
      S8: Inventory panel (ui.inventory.*, ui.crafting.*)
      S9: Settings menu (ui.settings.*)
      S10: Sign-in / account modal (ui.signin.*)
      S11: Friends panel (ui.friends.*)
      S12: Parental consent panel (ui.consent.*)
  - Run game with --headless for automated checks is NOT sufficient for visual truncation;
    the screenshot pass requires the editor or an exported build with a display.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend profanity filter tests with corrected NL wordlist coverage</name>
  <files>tests/unit/test_profanity_multilang.gd</files>
  <read_first>
    tests/unit/test_profanity_multilang.gd (current file — read in full)
    assets/profanity/wordlist_nl.txt (post-09-02 corrections — to know which words to test)
  </read_first>
  <action>
Read the existing tests. The file currently tests "kut" and "lul" as NL profanity samples.
Add the following new test functions AFTER the existing tests (do not modify existing functions):

Test 1 — Verify a previously-typoed word is now correctly blocked (regression):
  func test_corrected_homosexueel_is_blocked() -> void:
    ProfanityFilter.load_word_lists()
    var rejected := ProfanityFilter.filter_reject("homosexueel")
    assert_true(rejected, "homosexueel (corrected from homseksueel) must be blocked by NL filter")

Test 2 — Verify a corrected word (smeerlap) is blocked:
  func test_corrected_smeerlap_is_blocked() -> void:
    ProfanityFilter.load_word_lists()
    var rejected := ProfanityFilter.filter_reject("smeerlap")
    assert_true(rejected, "smeerlap (corrected from smerla) must be blocked by NL filter")

Test 3 — Verify a removed false positive passes through:
  func test_removed_false_positive_vent_passes() -> void:
    ProfanityFilter.load_word_lists()
    var rejected := ProfanityFilter.filter_reject("vent")
    assert_false(rejected, "vent (removed false positive) must NOT be blocked")

Test 4 — Verify a newly-added Flemish word is blocked:
  func test_new_godverdomme_is_blocked() -> void:
    ProfanityFilter.load_word_lists()
    var rejected := ProfanityFilter.filter_reject("godverdomme")
    assert_true(rejected, "godverdomme (new addition) must be blocked by NL filter")

Test 5 — Verify eikel is blocked:
  func test_new_eikel_is_blocked() -> void:
    ProfanityFilter.load_word_lists()
    var rejected := ProfanityFilter.filter_reject("eikel")
    assert_true(rejected, "eikel (new addition) must be blocked by NL filter")

Test 6 — Verify filter() replaces a new word in context:
  func test_filter_replaces_tering_in_sentence() -> void:
    ProfanityFilter.load_word_lists()
    var result := ProfanityFilter.filter("Wat een tering zet.")
    assert_eq(result, "Wat een [filtered] zet.", "filter() must replace NL disease-insult with [filtered]")

Test 7 — Verify stomaak (removed typo) does NOT appear in block list:
  func test_removed_stomaak_typo_not_blocking_innocent_word() -> void:
    ProfanityFilter.load_word_lists()
    # "stomaak" was a typo and is now removed; this test confirms removal.
    # There is no Dutch word "stomaak" so this should not match anything.
    var rejected := ProfanityFilter.filter_reject("stomaak")
    assert_false(rejected, "stomaak (removed invalid entry) must NOT be blocked")

Each new test must call before_each() state (static vars are reset by before_each() automatically).
Each test that calls load_word_lists() must be preceded by the before_each() reset; since before_each()
is called automatically by GUT before each test function, this is already handled.

After adding the 7 tests, run the full GUT suite headlessly to confirm 0 failures:
  godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -ginclude_subdirs -gexit 2>&1 | tail -20

If godot binary is not on PATH, try:
  /Applications/Godot_v4.6-stable_macos.universal/Godot.app/Contents/MacOS/Godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -ginclude_subdirs -gexit 2>&1 | tail -20
  </action>
  <verify>
    <automated>grep -c "func test_corrected_homosexueel_is_blocked\|func test_new_godverdomme_is_blocked\|func test_removed_false_positive_vent_passes" tests/unit/test_profanity_multilang.gd</automated>
    <automated>cd /Users/jnuyens/src/Cubicraftia && (godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gtest=test_profanity_multilang -gexit 2>&1 || /Applications/Godot_v4.6-stable_macos.universal/Godot.app/Contents/MacOS/Godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gtest=test_profanity_multilang -gexit 2>&1) | tail -5</automated>
  </verify>
  <done>
    - 7 new test functions added to test_profanity_multilang.gd
    - All tests pass when run headlessly (0 failures in test_profanity_multilang.gd)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: NL in-game screenshot pass and 09-03-SCREENSHOTS.md manifest</name>
  <files>.planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md</files>
  <read_first>
    .planning/phases/09-nl-localisation-review/09-CONTEXT.md (D-04b: surfaces to screenshot)
    locale/nl.po (post-09-02 — to know what to look for on each surface)
    src/autoload/translations.gd (how to force NL locale)
  </read_first>
  <action>
STEP 1 — Force NL locale for the game run:
  Write user://settings.cfg (on macOS: ~/Library/Application\ Support/Godot/app_userdata/Cubicraftia/settings.cfg)
  with content:
    [settings]
    locale = "nl"
  Use a shell command to create/overwrite this file:
    mkdir -p ~/Library/Application\ Support/Godot/app_userdata/Cubicraftia/
    printf '[settings]\nlocale = "nl"\n' > ~/Library/Application\ Support/Godot/app_userdata/Cubicraftia/settings.cfg

STEP 2 — Launch the game in the Godot editor and take screenshots:
  The user (jnuyens) will play on this machine (no git pull / no export needed — just run from editor).
  Open Godot 4.6 editor, press F5 to run. Navigate to each of the 12 surfaces listed below
  and take a screenshot (Cmd+Shift+5 on macOS, save to .planning/phases/09-nl-localisation-review/screenshots/).

  For each surface, record what was observed:

  SURFACES TO SCREENSHOT (D-04b):
    S1: Title screen — look for: "Bouwen. Overleven. Spelen met vrienden.", "Inloggen", "Account aanmaken", "Verdergaan", "Offline doorgaan", "Instellingen"
    S2: Avatar creator — look for: "Kies je bouwer", preset names (De Verkenner, De Bouwer, ...), section labels (Huid, Hoofd, Lichaam...), "Klaar", "Terug"
    S3: World select screen — look for: "Jouw werelden", "Vrienden", "Nieuwe wereld", mode labels ("Creatief"/"Overleving"), context menu (Hernoemen, Dupliceren, Exporteren, Verwijderen)
    S4: New world modal — look for: "Nieuwe wereld", "Wereldnaam", "Mijn wereld", "Seed (optioneel)", "Willekeurig", "Modus", "Overleving", "Creatief", "Aanmaken"
    S5: FTUE overlay (play in survival on a fresh world) — look for FTUE step text in Dutch
    S6: HUD in-game — look for: hotbar slot names, HP bar, mode badge (should say "Overleving" or "Sandbox"), camera mode hint
    S7: Chat overlay — look for: "Bericht…", "Versturen", "Chat", rate limit text
    S8: Inventory panel (press I) — look for: "Inventaris", tab labels, cell accessibility labels, crafting heading "Ambachtstafel (2 bij 2)"
    S9: Settings menu — look for: "Taal", "Nederlands" / "English", legal section "Juridisch", account section "Account"
    S10: Sign-in modal — look for: "Inloggen bij Cubicraftia", "E-mailadres", "Wachtwoord", age checkbox "Ik ben 13 jaar of ouder."
    S11: Friends panel — look for: "Vrienden", "Zoek op gebruikersnaam…", status strings
    S12: Parental consent panel — look for: "Vraag een ouder om te bevestigen", "E-mailadres van ouder of voogd"

  For surfaces that are hard to reach in a quick run (S10-S12: sign-in/friends/consent):
    These can be verified by checking the translations.gd autoload test or by temporarily logging out.

STEP 3 — For each surface, note:
  a) Any string still showing as a raw key (e.g. "ui.builder.place" instead of "Plaatsen")
  b) Any string visually truncated (text cut off at container edge)
  c) Any string that overflows its UI container (text extends beyond button/label bounds)
  d) Any string that looks garbled (encoding issues — unlikely but check)

STEP 4 — Produce 09-03-SCREENSHOTS.md:

Create .planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md with:

```markdown
# Phase 9 — NL In-Game Screenshot Pass

**Date:** 2026-06-21
**Build:** Editor run (F5), locale forced to "nl" via settings.cfg
**Status:** [PASS / FAIL / PARTIAL — update after run]

## Surface Results

| # | Surface | Status | Issues Found |
|---|---------|--------|--------------|
| S1 | Title screen | [PASS/FAIL] | [none / describe] |
| S2 | Avatar creator | [PASS/FAIL] | [none / describe] |
| S3 | World select | [PASS/FAIL] | [none / describe] |
| S4 | New world modal | [PASS/FAIL] | [none / describe] |
| S5 | FTUE overlay | [PASS/FAIL] | [none / describe] |
| S6 | HUD in-game | [PASS/FAIL] | [none / describe] |
| S7 | Chat overlay | [PASS/FAIL] | [none / describe] |
| S8 | Inventory panel | [PASS/FAIL] | [none / describe] |
| S9 | Settings menu | [PASS/FAIL] | [none / describe] |
| S10 | Sign-in modal | [PASS/FAIL] | [none / describe] |
| S11 | Friends panel | [PASS/FAIL] | [none / describe] |
| S12 | Parental consent | [PASS/FAIL] | [none / describe] |

## Truncation Issues Found
[List any truncation/overflow; include surface, msgid, and description]

## Untranslated Strings Found
[List any raw keys showing in-game; include surface, key, expected NL]

## Follow-up Actions
[List any msgstr corrections needed based on visual inspection]
```

If the game cannot be run in this executor context (no display), create 09-03-SCREENSHOTS.md
with status "DEFERRED — requires editor run with display" and list the exact steps for the
user to run manually (surfaces, what to look for). This is the same "deferred hardware verification"
pattern used in Phase 1, 2, 3, 4, 6 (see STATE.md § Deferred Items).

The LOCALE-03 requirement is satisfied either by:
  (a) A completed screenshot pass with PASS on all surfaces, OR
  (b) A deferred manifest with exact instructions, consistent with the project's established
      hardware-gated deferred verification pattern. Mark LOCALE-03 as "deferred" in that case,
      following the same precedent as UAT-01/02/03 in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md && echo "PASS: screenshots manifest exists" || echo "FAIL: 09-03-SCREENSHOTS.md missing"</automated>
    <automated>grep -c "Surface Results\|S1.*Title\|S12.*Parental" .planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md</automated>
    <automated>python3 -c "
import re
with open('.planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md') as f:
    lines = f.read().splitlines()
# Each surface row must carry a real status cell (| PASS |), NOT the [PASS/FAIL] placeholder
rows = [l for l in lines if re.match(r'^\|\s*S\d+\b', l) and re.search(r'\|\s*(PASS|FAIL|DEFERRED)\s*\|', l)]
assert len(rows) == 12, f'Expected 12 surfaces with a recorded PASS/FAIL/DEFERRED status, found {len(rows)} (blank [PASS/FAIL] placeholders do not count)'
print('PASS: all 12 surfaces have a recorded status')
"</automated>
  </verify>
  <done>
    - 09-03-SCREENSHOTS.md exists with 12 surface entries
    - Status for each surface recorded as PASS, FAIL, or DEFERRED with exact follow-up steps
    - Any truncation or untranslated string issues documented with msgid references
    - Follow-up corrections (if any) applied to locale/nl.po before plan is marked done
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.cfg write → locale selection | Writing locale=nl to user://settings.cfg forces NL display; this is the standard locale-forcing mechanism used in translations.gd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | Screenshot manifest inaccuracy | accept | This is a human-verified artifact; false claims are operator-level risk not a code security issue |
| T-09-06 | Information Disclosure | NL profanity test reveals blocked word list | accept | Tests are in the source repo under GPL; the word list itself is already CC-BY-4.0 public |
</threat_model>

<verification>
After both tasks complete:

1. GUT profanity tests pass:
   ```
   godot --headless --path /Users/jnuyens/src/Cubicraftia -s addons/gut/gut_cmdln.gd \
     -gdir=res://tests/unit -gtest=test_profanity_multilang -gexit 2>&1 | grep -E "PASS|FAIL|passed|failed"
   ```

2. Screenshot manifest exists with all 12 surfaces:
   ```
   grep -c "^| S" /Users/jnuyens/src/Cubicraftia/.planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md
   ```
   Expected output: 12

3. No FAIL status surfaces (if screenshot pass completed):
   ```
   grep -v "PASS\|DEFERRED" /Users/jnuyens/src/Cubicraftia/.planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md | grep "^| S" || echo "All surfaces PASS or DEFERRED"
   ```
</verification>

<success_criteria>
1. tests/unit/test_profanity_multilang.gd has 7 new test functions covering wordlist corrections; all pass (0 failures)
2. .planning/phases/09-nl-localisation-review/09-03-SCREENSHOTS.md exists with all 12 surfaces documented
3. No untranslated raw keys (ui.* etc.) visible on any NL surface (or corrections applied to nl.po)
4. No truncated strings visible on any surface (or documented as acceptable / filed for fix)
5. LOCALE-03 is satisfied: either all 12 surfaces PASS, or the manifest is DEFERRED following the established hardware-gated deferral pattern with exact run instructions
</success_criteria>

<output>
Create `.planning/phases/09-nl-localisation-review/09-03-SUMMARY.md` when done
</output>
