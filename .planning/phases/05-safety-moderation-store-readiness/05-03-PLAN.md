---
phase: 05-safety-moderation-store-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/networking/profanity_filter.gd
  - assets/profanity/wordlist_en.txt
  - assets/profanity/wordlist_nl.txt
  - assets/profanity/README.md
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "profanity_filter.gd loads EN and NL word lists from assets/profanity/ at startup"
    - "filter_reject(text) -> bool returns true for known bad words, false for clean text"
    - "Two compiled regexes (_regex_en, _regex_nl) — not a single merged regex"
    - "Assets/profanity/README.md contains required CC-BY-4.0 attribution for LDNOOBW"
    - "Word lists contain 10+ test words (curated subset; real store lists shipped post-review)"
  artifacts:
    - path: "src/networking/profanity_filter.gd"
      provides: "Extended ProfanityFilter with filter_reject() and dual-regex load_word_lists()"
      exports: ["ProfanityFilter"]
    - path: "assets/profanity/wordlist_en.txt"
      provides: "Curated EN word list (~300-500 words, CC-BY-4.0 LDNOOBW derived)"
      min_lines: 10
    - path: "assets/profanity/wordlist_nl.txt"
      provides: "Curated NL word list (~300-500 words, CC-BY-4.0 LDNOOBW derived)"
      min_lines: 10
    - path: "assets/profanity/README.md"
      provides: "CC-BY-4.0 attribution for LDNOOBW"
      contains: "CC-BY-4.0"
  key_links:
    - from: "src/networking/profanity_filter.gd"
      to: "assets/profanity/wordlist_en.txt"
      via: "FileAccess.open('res://assets/profanity/wordlist_en.txt')"
      pattern: "wordlist_en"
    - from: "src/networking/profanity_filter.gd"
      to: "assets/profanity/wordlist_nl.txt"
      via: "FileAccess.open('res://assets/profanity/wordlist_nl.txt')"
      pattern: "wordlist_nl"
---

<objective>
Harden the Phase 4 profanity filter stub into a production-ready dual-language filter with separate compiled regexes for EN and NL, a new filter_reject() method, and curated word lists with proper attribution.

Purpose: Every user-authored string (chat, username, world name, avatar name) flows through this filter before submission. Plans 05-07 through 05-10 depend on filter_reject() existing.

Output: profanity_filter.gd with load_word_lists() + filter_reject(); two word list text files; CC-BY-4.0 attribution README.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-RESEARCH.md

<interfaces>
<!-- Existing Phase 4 profanity_filter.gd public API — extend, do NOT replace -->

From src/networking/profanity_filter.gd (Phase 4 implementation):
```gdscript
class_name ProfanityFilter
extends RefCounted

# Phase 4 stub word list — Phase 5 replaces with load_word_lists()
const _WORD_LIST: Array[String] = ["badword1", "badword2"]  # ~20 test words

static var _regex: RegEx = null

static func set_word_list(words: Array) -> void:
    _regex = null  # force recompile
    # implementation loads words into _regex

static func _ensure_regex() -> void:
    if _regex != null:
        return
    # compiles single regex from word list

static func filter(text: String) -> String:
    _ensure_regex()
    if _regex == null:
        return text
    return _regex.sub(text, "[filtered]", true)
```

Phase 5 adds:
- Dual static vars: _regex_en: RegEx, _regex_nl: RegEx (NEVER merge into one regex — Pitfall 2)
- load_word_lists() static func reading from res://assets/profanity/wordlist_en.txt and wordlist_nl.txt
- filter_reject(text: String) -> bool returns true if any word matches (per 05-CONTEXT.md Area 2)
- filter(text) updated to use both _regex_en and _regex_nl (apply _regex_en always, _regex_nl as second pass)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Word list files + attribution README</name>
  <files>assets/profanity/wordlist_en.txt, assets/profanity/wordlist_nl.txt, assets/profanity/README.md</files>
  <behavior>
    - wordlist_en.txt: minimum 50 curated English words from LDNOOBW EN list. One word per line, lowercase. Must include real test words that verify the filter works (e.g., common offensive terms visible in public lists). Trimmed of false positives that appear as common gaming terms (e.g., 'kill', 'die', 'gun' are NOT in the filter — these are normal gameplay words). Include the 10-15 strongest EN terms that cover the most common harassment patterns. Max 500 words (regex engine limit per Pitfall 2).
    - wordlist_nl.txt: minimum 30 curated Dutch words from LDNOOBW NL list. Same format. Include common NL offensive terms. Max 500 words.
    - README.md: MUST include the CC-BY-4.0 attribution text: "Word lists derived from LDNOOBW/List-of-Dirty-Naughty-Obscene-and-Otherwise-Bad-Words, licensed under Creative Commons Attribution 4.0 International (CC-BY-4.0). Copyright Shutterstock, Inc. https://github.com/LDNOOBW/List-of-Dirty-Naughty-Obscene-and-Otherwise-Bad-Words" and a note that the lists are curated (false positives for gaming context removed). Also include a note for contributors on how to add words and submit a PR.
  </behavior>
  <action>Create the 3 files. For wordlist_en.txt and wordlist_nl.txt: these are real curated word lists — do not use placeholder content. Include actual offensive/profane terms from the public LDNOOBW repository (the list is openly published at github.com/LDNOOBW/). Include a comment line at the top of each file: `# LDNOOBW-derived, CC-BY-4.0, curated for gaming context — see README.md`. Lines beginning with # are comment lines; the filter's load_word_lists() must skip them. The attribution README must satisfy the CC-BY-4.0 attribution requirement precisely — include: (1) the word "Copyright", (2) the copyright holder (Shutterstock, Inc.), (3) the license name (CC-BY-4.0), and (4) the source URL.</action>
  <verify>
    <automated>wc -l /Users/jnuyens/src/LegoMinecraft/assets/profanity/wordlist_en.txt && wc -l /Users/jnuyens/src/LegoMinecraft/assets/profanity/wordlist_nl.txt && grep "CC-BY-4.0" /Users/jnuyens/src/LegoMinecraft/assets/profanity/README.md && grep "Shutterstock" /Users/jnuyens/src/LegoMinecraft/assets/profanity/README.md</automated>
  </verify>
  <done>wordlist_en.txt has 50+ non-comment lines; wordlist_nl.txt has 30+ non-comment lines; README.md contains CC-BY-4.0 attribution with copyright holder and source URL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend profanity_filter.gd with dual-regex and filter_reject()</name>
  <files>src/networking/profanity_filter.gd</files>
  <behavior>
    - Replace single _regex static var with _regex_en: RegEx = null and _regex_nl: RegEx = null
    - Add load_word_lists() static func: reads wordlist_en.txt and wordlist_nl.txt using FileAccess.open("res://assets/profanity/wordlist_{lang}.txt"), skips lines starting with '#', strips whitespace, lowercases, deduplicates. Compiles separate _regex_en (from EN words) and _regex_nl (from NL words). Regex pattern: "(?i)\\b(" + "|".join(words) + ")\\b" — word boundary anchors, case insensitive.
    - Update _ensure_regex() to call load_word_lists() if both _regex_en and _regex_nl are null. This preserves the lazy-init pattern from Phase 4.
    - Update filter(text) to apply _regex_en first (always), then _regex_nl second pass. Return result after both passes.
    - Add filter_reject(text: String) -> bool: calls _ensure_regex(); returns (_regex_en != null and _regex_en.search(text) != null) or (_regex_nl != null and _regex_nl.search(text) != null). Returns false for empty string without regex check.
    - Keep set_word_list(words) for backward compatibility — existing Phase 4 callers still work. Update it to set _regex_en (as the single combined list) and set _regex_nl = null.
    - Maintain class_name ProfanityFilter / extends RefCounted. Keep SPDX header.
    - Performance: ensure _ensure_regex() is idempotent (only compiles if null). The regex warmup happens once at startup via the FriendsClient._ready() or a dedicated autoload._ready() call to ProfanityFilter._ensure_regex().
  </behavior>
  <action>Modify src/networking/profanity_filter.gd. Read the existing file first to understand the exact Phase 4 implementation, then extend it. Do not break set_word_list() backward compat. The key new method is filter_reject() — used in 5 locations: username sign-up, world name creation, avatar name save, and the two test stubs. The dual-regex split is critical for Tier-3 performance (Pitfall 2): do NOT merge EN and NL into a single 600-word alternation. Words loaded from files: skip blank lines and comment lines (lines starting with '#'). On file-not-found: push_warning and continue (graceful degradation — filter runs with whatever lists loaded successfully).</action>
  <verify>
    <automated>godot --headless -s tests/gut_main.gd -- -gtest=tests/unit/test_profanity_multilang.gd 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | tail -5</automated>
  </verify>
  <done>profanity_filter.gd has _regex_en + _regex_nl static vars; load_word_lists() reads from assets/profanity/; filter_reject() returns bool; filter() applies both regexes; GUT test file runs (pending stubs, no errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → ProfanityFilter | Text input from user (username, world name, chat) enters filter; must never echo rejected content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-P2 | Denial of Service | 500-word regex on Tier-3 Android (Pitfall 2) | mitigate | Two separate compiled regexes (_regex_en, _regex_nl) — each under 500 words |
| T-05-P-bypass | Information Disclosure | Rejection error echoes attempted profanity | mitigate | filter_reject() returns bool only; callers show "Please choose another name." not the rejected input |
| T-05-SC | Tampering | Word list file modification | accept | Plain text in git repo; changes reviewed via PR; no executable surface |
</threat_model>

<verification>
```
grep "filter_reject" src/networking/profanity_filter.gd
grep "_regex_en" src/networking/profanity_filter.gd
grep "_regex_nl" src/networking/profanity_filter.gd
wc -l assets/profanity/wordlist_en.txt
grep "CC-BY-4.0" assets/profanity/README.md
```
All must exit 0.
</verification>

<success_criteria>
- profanity_filter.gd has filter_reject(text: String) -> bool method
- Two separate compiled regexes: _regex_en and _regex_nl
- load_word_lists() reads from res://assets/profanity/wordlist_en.txt and nl.txt
- wordlist_en.txt has 50+ curated EN words (comment lines prefixed with #)
- wordlist_nl.txt has 30+ curated NL words
- README.md satisfies CC-BY-4.0 attribution requirement
- filter() still works (backward compatible with Phase 4 chat relay)
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-03-SUMMARY.md` when done
</output>
