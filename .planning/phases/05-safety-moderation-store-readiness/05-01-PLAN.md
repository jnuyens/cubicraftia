---
phase: 05-safety-moderation-store-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest_phase5.gd
  - tests/unit/test_block_unblock.gd
  - tests/unit/test_report_submission.gd
  - tests/unit/test_dob_parser.gd
  - tests/unit/test_parental_consent_token.gd
  - tests/unit/test_profanity_multilang.gd
  - tests/unit/test_username_validator.gd
  - tests/unit/test_eula_hash_recompute.gd
  - tests/integration/test_blocks_check_on_join.gd
  - tests/integration/test_parental_consent_e2e.gd
  - locale/en.po
  - assets/themes/cubicraftia.tres
  - assets/textures/icons/icon_warning.png
  - assets/textures/icons/icon_email.png
  - assets/textures/icons/icon_check.png
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "All 9 Phase 5 test files exist with pending() stubs — GUT suite runs green (0 failures)"
    - "53 new i18n keys are present in locale/en.po"
    - "3 new StyleBoxes present in cubicraftia.tres (legal_summary, warning_banner, context_menu_panel)"
    - "3 new icon assets exist at the correct paths"
  artifacts:
    - path: "tests/conftest_phase5.gd"
      provides: "Shared GUT fixtures: mock blocks/reports/parental_consents/username_change_log tables"
      exports: ["Phase5Fixtures"]
    - path: "tests/unit/test_block_unblock.gd"
      provides: "Block/unblock RLS logic stubs"
    - path: "tests/unit/test_report_submission.gd"
      provides: "Report audit trail and surface enum stubs"
    - path: "tests/unit/test_dob_parser.gd"
      provides: "Date-of-birth parser and under-13 detection stubs"
    - path: "tests/unit/test_parental_consent_token.gd"
      provides: "128-bit token, TTL, single-use semantics stubs"
    - path: "tests/unit/test_profanity_multilang.gd"
      provides: "filter() and filter_reject() with EN+NL word loading stubs"
    - path: "tests/unit/test_username_validator.gd"
      provides: "Reserved prefix, format regex, 30-day cooldown stubs"
    - path: "tests/unit/test_eula_hash_recompute.gd"
      provides: "SHA-256 hash and mismatch detection stubs"
    - path: "tests/integration/test_blocks_check_on_join.gd"
      provides: "Integration stub for blocks-check on session join"
    - path: "tests/integration/test_parental_consent_e2e.gd"
      provides: "Integration stub for full parental consent lifecycle"
  key_links:
    - from: "tests/unit/test_profanity_multilang.gd"
      to: "src/networking/profanity_filter.gd"
      via: "preload()"
      pattern: "preload.*profanity_filter"
    - from: "tests/unit/test_username_validator.gd"
      to: "src/autoload/username_policy.gd"
      via: "preload()"
      pattern: "preload.*username_policy"
---

<objective>
Wave 0 scaffold: all Phase 5 GUT test stubs, 53 new i18n keys in locale/en.po, 3 new StyleBox entries in cubicraftia.tres, and 3 new icon assets.

Purpose: No later plan can verify its work without these stubs existing. All other plans reference `tests/conftest_phase5.gd` and individual test files created here. The icon assets and StyleBoxes are referenced by UI plans in Wave 3.

Output: 10 test files (all passing with pending() stubs), 53 new msgid entries in en.po, 3 StyleBoxes in cubicraftia.tres, 3 PNG icon stubs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-RESEARCH.md
@.planning/phases/05-safety-moderation-store-readiness/05-UI-SPEC.md
@.planning/phases/04-multiplayer-seamless-host-failover/04-PATTERNS.md

<interfaces>
<!-- Existing test patterns to replicate verbatim -->

From tests/conftest_phase4.gd (structure to copy):
```gdscript
class_name Phase4Fixtures
extends RefCounted

static func open_temp_world(mode: String = "survival") -> String: ...
static func make_rtt_table(peer_ids: Array, rtts: Array) -> Dictionary: ...
static func cleanup_temp_world(world_id: String) -> void: ...
```

From tests/unit/test_invite_token.gd (GUT test structure):
```gdscript
extends GutTest
const Phase4Fixtures = preload("res://tests/conftest_phase4.gd")

func before_each() -> void: ...
func after_each() -> void: ...
func test_token_is_26_chars() -> void:
    pending("Phase 4 — activate in 04-11")
```

From src/networking/profanity_filter.gd (existing class):
```gdscript
class_name ProfanityFilter
extends RefCounted
static func filter(text: String) -> String: ...
static func set_word_list(words: Array) -> void: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: conftest_phase5.gd + 7 unit test stubs</name>
  <files>tests/conftest_phase5.gd, tests/unit/test_block_unblock.gd, tests/unit/test_report_submission.gd, tests/unit/test_dob_parser.gd, tests/unit/test_parental_consent_token.gd, tests/unit/test_profanity_multilang.gd, tests/unit/test_username_validator.gd, tests/unit/test_eula_hash_recompute.gd</files>
  <behavior>
    - conftest_phase5.gd: class_name Phase5Fixtures extends RefCounted; static helpers: open_temp_world() (delegates to Phase4Fixtures pattern), make_mock_block_row(blocker, blocked), make_mock_report_row(reporter, reported, surface, category), make_mock_consent_row(child_uid, parent_email), cleanup_temp_world(). Use SPDX header GPL-3.0-or-later.
    - test_block_unblock.gd: 4 stubs — test_block_stores_direction(), test_blocker_cannot_see_blocked_uid_rows(), test_unblock_removes_row(), test_blocked_uid_sees_nothing(). All use pending("Phase 5 — activate in 05-12").
    - test_report_submission.gd: 3 stubs — test_reporter_can_insert(), test_reported_uid_cannot_see_own_reports(), test_evidence_captures_chat_context(). All pending.
    - test_dob_parser.gd: 4 stubs — test_age_12_is_under_13(), test_age_13_is_not_under_13(), test_age_exactly_13_today(), test_invalid_dob_date(). All pending.
    - test_parental_consent_token.gd: 4 stubs — test_token_is_128_bit_base32(), test_token_single_use_cleared_after_confirm(), test_token_expired_after_7_days(), test_revoke_token_distinct_from_consent_token(). All pending.
    - test_profanity_multilang.gd: 4 stubs — test_filter_replaces_en_word_with_filtered(), test_filter_reject_returns_true_for_en_word(), test_filter_reject_returns_false_for_clean_text(), test_filter_nl_word_after_wordlist_load(). All pending.
    - test_username_validator.gd: 5 stubs — test_reserved_prefix_admin_rejected(), test_format_regex_accepts_valid(), test_format_regex_rejects_special_chars(), test_format_rejects_too_short(), test_cooldown_blocks_second_change_within_30_days(). All pending.
    - test_eula_hash_recompute.gd: 3 stubs — test_sha256_produces_consistent_hash(), test_different_content_produces_different_hash(), test_stored_hash_mismatch_detected(). All pending.
  </behavior>
  <action>Create all 8 files. Each test file: SPDX header, extends GutTest, const Phase5Fixtures = preload("res://tests/conftest_phase5.gd"), test functions with pending() stubs. No before_each/after_each unless needed for a specific stub's setup. Use the Phase4Fixtures pattern as the structural template (copy file header, class structure, static method signatures). conftest_phase5.gd must export class_name Phase5Fixtures and include make_mock_block_row, make_mock_report_row, make_mock_consent_row helpers that return Dictionary objects matching the SQL schema column names from 05-RESEARCH.md Pattern 1.</action>
  <verify>
    <automated>godot --headless -s tests/gut_main.gd -- -gtest=tests/unit/test_block_unblock.gd -gtest=tests/unit/test_report_submission.gd -gtest=tests/unit/test_dob_parser.gd -gtest=tests/unit/test_parental_consent_token.gd -gtest=tests/unit/test_profanity_multilang.gd -gtest=tests/unit/test_username_validator.gd -gtest=tests/unit/test_eula_hash_recompute.gd 2>&1 | grep -E "(PASSED|FAILED|ERROR|pending)" | tail -20</automated>
  </verify>
  <done>All 7 unit test files parse without error; all test functions show as pending (not failed); conftest_phase5.gd exports Phase5Fixtures with the required mock helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test stubs + i18n keys + theme StyleBoxes + icon assets</name>
  <files>tests/integration/test_blocks_check_on_join.gd, tests/integration/test_parental_consent_e2e.gd, locale/en.po, assets/themes/cubicraftia.tres, assets/textures/icons/icon_warning.png, assets/textures/icons/icon_email.png, assets/textures/icons/icon_check.png</files>
  <action>
    1. Create tests/integration/test_blocks_check_on_join.gd: extends GutTest, 2 stubs — test_blocked_user_cannot_join_session() and test_unblocked_user_can_join_after_unblock(). Both pending("Phase 5 — activate in 05-12").

    2. Create tests/integration/test_parental_consent_e2e.gd: extends GutTest, 3 stubs — test_under_13_account_is_restricted_before_consent(), test_parent_consent_confirms_and_unlocks_account(), test_parent_revoke_blocks_account(). All pending("Phase 5 — activate in 05-12").

    3. Add 53 new msgid keys to locale/en.po. Append below the last Phase 4 entry. All msgstr values are empty strings (translators fill in Phase 6 NL pass). Keys from 05-UI-SPEC.md i18n contract: ui.block.title, ui.block.body, ui.block.confirm_action, ui.block.cancel, ui.block.toast_blocked, ui.report.title_player, ui.report.title_message, ui.report.subtitle_category, ui.report.category_harassment, ui.report.category_spam, ui.report.category_cheating, ui.report.category_csam, ui.report.category_other, ui.report.reason_placeholder, ui.report.char_counter, ui.report.context_label, ui.report.next, ui.report.back, ui.report.submit, ui.report.cancel, ui.report.toast_submitted, ui.signin.dob_label, ui.signin.dob_day_placeholder, ui.signin.dob_month_placeholder, ui.signin.dob_year_placeholder, ui.signin.dob_month_jan through ui.signin.dob_month_dec (12 keys), ui.signin.error_dob_incomplete, ui.signin.error_dob_invalid, ui.consent.title, ui.consent.body, ui.consent.parent_email_placeholder, ui.consent.send_link, ui.consent.sending, ui.consent.why_label, ui.consent.why_explanation, ui.consent.skip, ui.consent.success_heading, ui.consent.success_body, ui.consent.done, ui.consent.resend, ui.consent.resent_toast, ui.consent.error_invalid_email, ui.consent.error_network, ui.restricted.banner_label, ui.restricted.resend_link, ui.restricted.join_blocked_title, ui.restricted.join_blocked_body, ui.restricted.join_blocked_resend, ui.restricted.join_blocked_cancel, ui.username.error_format, ui.username.error_taken, ui.username.error_profanity, ui.username.error_reserved, ui.username.error_cooldown, ui.username.cooldown_label, ui.username.inline_valid, ui.legal.terms_link, ui.legal.privacy_link, ui.legal.viewer_close, ui.legal.footer_version, ui.legal.agree_button, ui.legal.english_only_notice, ui.legal.reack_title, ui.legal.reack_body, ui.legal.reack_review, ui.legal.reack_signout, ui.friends.report, ui.chat.report_context_menu_item, ui.nameplate.report_action, ui.nameplate.block_action, ui.settings.legal_section_title, ui.settings.terms_button, ui.settings.privacy_button, ui.settings.account_section_title, ui.settings.account_username_label, ui.settings.account_change_username, ui.settings.account_change_save, ui.settings.account_change_cancel, ui.settings.account_delete_button, ui.settings.account_delete_title, ui.settings.account_delete_body, ui.settings.account_delete_confirm, ui.settings.account_delete_cancel, ui.settings.account_delete_pending, ui.settings.account_delete_cancel_action. Use standard PO format with msgid/msgstr pairs. Msgstr for English: copy copywriting contract values from 05-UI-SPEC.md for keys that have an English value there; for others use empty string.

    4. Add 3 new StyleBox entries to assets/themes/cubicraftia.tres using the exact StyleBoxFlat property values from 05-UI-SPEC.md "Component Extensions" section: StyleBox_legal_summary (transparent bg, 2px accent-yellow left border, 8px padding), StyleBox_warning_banner (warning amber fill, no border, no rounding, zero margin), StyleBox_context_menu_panel (navy 0.96 alpha, 8px rounding, 8px content margin). Do NOT modify any existing StyleBox entries from Phase 1-4.

    5. Create 3 placeholder 1×1 transparent PNG files at assets/textures/icons/icon_warning.png, icon_email.png, icon_check.png. These are placeholder stubs; Wave 3 UI plans will replace them with real icons. Use GDScript via a one-time tool script or create minimal valid PNGs (the smallest valid PNG is 67 bytes — a 1×1 transparent pixel). The files MUST be valid PNGs so Godot's importer accepts them without errors.
  </action>
  <verify>
    <automated>godot --headless -s tests/gut_main.gd -- -gtest=tests/integration/test_blocks_check_on_join.gd -gtest=tests/integration/test_parental_consent_e2e.gd 2>&1 | grep -E "(PASSED|FAILED|ERROR|pending)" | tail -10 && grep -c "ui\.block\." /Users/jnuyens/src/LegoMinecraft/locale/en.po && grep -c "StyleBox_legal_summary" /Users/jnuyens/src/LegoMinecraft/assets/themes/cubicraftia.tres</automated>
  </verify>
  <done>2 integration test files parse and show pending stubs; en.po has 53+ new msgid entries in ui.block/ui.report/ui.signin/ui.consent/ui.restricted/ui.username/ui.legal/ui.settings/ui.friends/ui.chat/ui.nameplate namespaces; cubicraftia.tres has StyleBox_legal_summary, StyleBox_warning_banner, StyleBox_context_menu_panel entries; 3 icon PNG stubs exist and are valid files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → production code | Tests import production classes via preload(); stubs must not import non-existent files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-W0-SC | Tampering | locale/en.po modification | accept | en.po is a plain text file, no executable surface; changes are code-reviewed via git diff |
</threat_model>

<verification>
Full GUT suite must run without new failures after this plan:
```
godot --headless -s tests/gut_main.gd 2>&1 | tail -5
```
Pre-existing failures (7 from Phase 1-4) must not increase.
</verification>

<success_criteria>
- 10 test files exist (7 unit + 2 integration + 1 conftest) with pending() stubs
- `godot --headless -s tests/gut_main.gd` exits green (failure count unchanged from pre-Phase-5 baseline of 7)
- `grep -c "^msgid" locale/en.po` increases by 53 from the Phase 4 baseline
- `grep "StyleBox_legal_summary" assets/themes/cubicraftia.tres` exits 0
- 3 PNG files exist and are non-empty at assets/textures/icons/icon_{warning,email,check}.png
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-01-SUMMARY.md` when done
</output>
