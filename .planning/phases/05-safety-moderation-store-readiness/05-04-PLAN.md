---
phase: 05-safety-moderation-store-readiness
plan: 04
type: execute
wave: 2
depends_on: [05-02]
files_modified:
  - signaling-server/internal/hub/hub.go
  - signaling-server/internal/hub/consent.go
  - signaling-server/cmd/signaling/main.go
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "session.join handler queries Supabase blocks table (both directions) and rejects if found"
    - "session.join handler checks parental_consents.consented_at for is_under_13 accounts and rejects unconsented joins"
    - "POST /consent/request generates 128-bit base32 tokens, inserts parental_consents row, sends SMTP email"
    - "GET /consent/confirm?token= validates token, sets consented_at, clears token (single-use)"
    - "GET /consent/revoke?token= sets revoked_at"
    - "GET /admin/reports returns paginated JSON, requires ADMIN_SECRET header"
  artifacts:
    - path: "signaling-server/internal/hub/consent.go"
      provides: "ConsentHandler with HandleRequest, HandleConfirm, HandleRevoke"
      contains: "ConsentHandler"
    - path: "signaling-server/internal/hub/hub.go"
      provides: "isBlockedBetween() + isConsentRequired() + HandleAdminReports()"
      contains: "isBlockedBetween"
    - path: "signaling-server/cmd/signaling/main.go"
      provides: "Route registration for /consent/* and /admin/reports"
      contains: "/consent/request"
  key_links:
    - from: "signaling-server/internal/hub/hub.go"
      to: "Supabase PostgREST /rest/v1/blocks"
      via: "isBlockedBetween() HTTP GET with service role key"
      pattern: "isBlockedBetween"
    - from: "signaling-server/internal/hub/consent.go"
      to: "Supabase PostgREST /rest/v1/parental_consents"
      via: "INSERT via service role"
      pattern: "parental_consents"
---

<objective>
Extend the Go signaling server with blocks-check on session join, consent-check for under-13 accounts, parental consent lifecycle endpoints, and the operator moderation reports endpoint.

Purpose: The signaling server is the hard enforcement gate for block and consent logic. Client-side checks are UX only — a modified client must be rejected at the server. All checks use the service-role key to bypass RLS, as established in Plan 04-02.

Output: consent.go (new file), hub.go extended with isBlockedBetween() + isConsentRequired() + HandleAdminReports(), main.go with new route registration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-RESEARCH.md
@.planning/phases/04-multiplayer-seamless-host-failover/04-PATTERNS.md

<interfaces>
<!-- Existing Go hub pattern to extend -->

From signaling-server/internal/hub/hub.go (Phase 4 relay check — injection point):
```go
// handleRelay is the injection point for block-check and consent-check.
// Currently guards peer-count cap for "offer" message type.
// Phase 5 adds: isBlockedBetween(hostUID, joinerUID) check before relay.
func (h *Hub) handleRelay(client *Client, msg Message) {
    if msg.Type == "offer" {
        // peer-count guard already here
        // Phase 5 inserts block check here
    }
    // relay to target
}
```

From signaling-server/internal/hub/auth.go (JWT verification — already implemented):
```go
// verifyJWT returns the user UID from the Bearer token.
// Phase 5 code can call this to get the joining peer's UID.
func verifyJWT(tokenStr string, secret []byte) (string, error) { ... }
```

From signaling-server/internal/config/config.go:
```go
type Config struct {
    SupabaseURL        string
    SupabaseServiceKey string  // bypasses RLS — for server-side checks
    AdminSecret        string  // Phase 5 adds this field
    SMTPHost           string  // Phase 5 adds
    SMTPPort           int     // Phase 5 adds
    SMTPUser           string  // Phase 5 adds
    SMTPPassword       string  // Phase 5 adds
    SMTPFrom           string  // Phase 5 adds
    ConsentBaseURL     string  // Phase 5 adds — for consent link URLs
}
```

Go code style: no third-party modules; use standard library only (net/http, net/smtp, encoding/json, crypto/rand, encoding/base32). Error handling returns HTTP status + JSON error body. All Supabase calls use h.cfg.SupabaseServiceKey in apikey and Authorization headers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hub.go blocks-check + consent-check + admin reports</name>
  <files>signaling-server/internal/hub/hub.go</files>
  <behavior>
    - Add isBlockedBetween(uidA, uidB string) (bool, error): HTTP GET to Supabase PostgREST /rest/v1/blocks with OR filter for both directions (uidA blocks uidB OR uidB blocks uidA), limit=1, select=blocker_uid. Use service-role key. Return true if any row found.
    - Add isConsentRequired(uid string) (bool, error): Check GoTrue user metadata for is_under_13=true AND check parental_consents table for consented_at IS NULL. Implementation: GET /rest/v1/parental_consents?child_uid=eq.{uid}&select=consented_at — if row exists and consented_at is null, return true (consent required). Also check if row exists at all (if no row, the account hasn't even requested consent).
    - Add rate-limit check for reports: insertReportRateLimitCheck(reporterUID string) (bool, error): GET /rest/v1/reports?reporter_uid=eq.{uid}&created_at=gte.{24h_ago}&select=id&limit=6 — return false (rate limited) if count >= 5.
    - Extend handleRelay(): for "offer" type messages, after the peer-count check, call isBlockedBetween(hostUID, joinerUID). If blocked: send error message to joiner {"type":"error","payload":{"code":"blocked"}} and return. Then call isConsentRequired(joinerUID). If consent required: send error {"type":"error","payload":{"code":"consent_required"}} and return.
    - Add HandleAdminReports(w, r): GET only; check Authorization header for "Bearer {ADMIN_SECRET}"; query /rest/v1/reports with service role key, accept ?page= and ?limit= query params (default limit=50); return JSON array.
    - Add RequireAdminSecret middleware func: wraps a handler, checks Authorization header matches cfg.AdminSecret.
  </behavior>
  <action>Modify signaling-server/internal/hub/hub.go. Read the existing file structure first. The isBlockedBetween function follows the exact pattern from 05-RESEARCH.md Pattern 2 — use the PostgREST OR filter syntax: `?or=(and(blocker_uid.eq.A,blocked_uid.eq.B),and(blocker_uid.eq.B,blocked_uid.eq.A))&select=blocker_uid&limit=1`. The isConsentRequired function uses the Supabase service key to bypass the child-only SELECT RLS policy. For rate limiting: use time.Now().Add(-24*time.Hour).Format(time.RFC3339) for the 24h_ago timestamp in the gte filter. The admin reports endpoint returns a JSON array; add X-Total-Count header from a count query if feasible. Error responses use JSON: {"error": "code", "message": "description"}.</action>
  <verify>
    <automated>cd /Users/jnuyens/src/LegoMinecraft/signaling-server && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>hub.go compiles without errors; isBlockedBetween, isConsentRequired, HandleAdminReports functions present; handleRelay injects block and consent checks before forwarding offer messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: consent.go + main.go route registration</name>
  <files>signaling-server/internal/hub/consent.go, signaling-server/cmd/signaling/main.go</files>
  <behavior>
    - consent.go: Create ConsentHandler struct with Hub *Hub field. Implement HandleRequest(w, r): POST, requires JWT auth (call verifyJWT from auth.go to get child_uid); decode body {parent_email: string}; generate 16 random bytes via crypto/rand, base32-encode (no padding) for consent_token; generate separate 16 random bytes for revoke_token; INSERT INTO parental_consents via service role; send SMTP email using net/smtp to parent_email with consent link https://{cfg.ConsentBaseURL}/consent/confirm?token={consent_token} and revoke link https://{cfg.ConsentBaseURL}/consent/revoke?token={revoke_token}. Email must be HTML+text multipart MIME. Include COPPA 2025 confirmation statement: "By clicking Confirm, I confirm that I am the parent or legal guardian of [child_username] on Cubicraftia." Return 200 OK on success, 409 if row already exists.
    - HandleConfirm(w, r): GET ?token=; SELECT from parental_consents WHERE consent_token=token AND consented_at IS NULL; verify requested_at + 7 days > now; UPDATE SET consented_at=NOW(), consent_token=NULL (single-use clear); PATCH GoTrue user metadata via Supabase admin API /auth/v1/admin/users/{child_uid} to set user_metadata.consented_at; return HTML response "Parental consent confirmed."
    - HandleRevoke(w, r): GET ?token=; SELECT WHERE revoke_token=token AND revoked_at IS NULL; UPDATE SET revoked_at=NOW(); PATCH GoTrue user metadata to set user_metadata.consented_at=null; return HTML "Parental consent revoked."
    - main.go: Add ConsentHandler routes and admin reports route to the existing mux, following the pattern from 05-RESEARCH.md "Go Signaling Routes" code example. Add new Config fields (SMTPHost, SMTPPort, SMTPUser, SMTPPassword, SMTPFrom, AdminSecret, ConsentBaseURL) to the config loading logic (from env vars: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_FROM, ADMIN_SECRET, CONSENT_BASE_URL).
  </behavior>
  <action>Create consent.go and modify main.go. For SMTP: use net/smtp.SendMail(addr, auth, from, []string{to}, message). The message is a raw MIME multipart message built with a strings.Builder. The GoTrue admin PATCH uses PATCH method to /auth/v1/admin/users/{uid} with header "apikey: {service_key}" and body {"user_metadata": {"consented_at": "...ISO8601..."}}. Use h.cfg.SupabaseServiceKey as the service key. Token generation follows the FriendsClient invite token pattern from Phase 4: crypto/rand.Read(b[:]) then base32.StdEncoding.WithPadding(base32.NoPadding).EncodeToString(b). Do NOT use GoTrue email template system for the consent email (Pitfall 3 in RESEARCH.md) — use net/smtp directly.</action>
  <verify>
    <automated>cd /Users/jnuyens/src/LegoMinecraft/signaling-server && go build ./... 2>&1 | head -20 && grep "consent/request" /Users/jnuyens/src/LegoMinecraft/signaling-server/cmd/signaling/main.go</automated>
  </verify>
  <done>Go project compiles; consent.go has HandleRequest/HandleConfirm/HandleRevoke; main.go registers /consent/request, /consent/confirm, /consent/revoke, /admin/reports routes; Config has SMTP and admin fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Joiner → signaling server offer relay | Untrusted join attempt; server is the hard gate |
| Parent → consent/confirm endpoint | Unauthenticated browser click; token is the credential |
| Operator → /admin/reports | ADMIN_SECRET is the credential; not a user JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-E3 | Elevation of Privilege | Under-13 joins via modified client | mitigate | isConsentRequired() called server-side on every offer relay; client-side check is UX only |
| T-05-E2 | Elevation of Privilege | Blocked user joins via direct offer | mitigate | isBlockedBetween() called server-side on every offer relay using service-role key |
| T-05-S1 | Spoofing | Consent token brute-force | mitigate | 128-bit random token (crypto/rand); single-use (cleared on confirm); 7-day TTL |
| T-05-D1 | Denial of Service | Mass-report flood | mitigate | insertReportRateLimitCheck() enforces 5 reports/24h per reporter before insert |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages — only Go stdlib net/smtp, crypto/rand, encoding/base32 used |
</threat_model>

<verification>
```
cd signaling-server && go build ./...
grep "isBlockedBetween" signaling-server/internal/hub/hub.go
grep "isConsentRequired" signaling-server/internal/hub/hub.go
grep "ConsentHandler" signaling-server/internal/hub/consent.go
grep "/consent/request" signaling-server/cmd/signaling/main.go
grep "/admin/reports" signaling-server/cmd/signaling/main.go
```
All must exit 0.
</verification>

<success_criteria>
- signaling-server compiles with `go build ./...` (no errors)
- isBlockedBetween() and isConsentRequired() integrated into handleRelay for "offer" type
- consent.go has HandleRequest, HandleConfirm, HandleRevoke
- SMTP email sent via net/smtp (not GoTrue templates)
- /admin/reports requires ADMIN_SECRET Authorization header
- Rate limit check (5/24h) runs before any report INSERT
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-04-SUMMARY.md` when done
</output>
