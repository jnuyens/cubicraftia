---
phase: 05-safety-moderation-store-readiness
plan: 10
type: execute
wave: 4
depends_on: [05-05, 05-07, 05-08, 05-09]
files_modified:
  - src/ui/sign_in_panel.gd
  - src/autoload/network_manager.gd
  - src/autoload/world_save.gd
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "Username validation (format + reserved + profanity) runs on sign-up with inline feedback"
    - "WorldSave.create_world() rejects world names matching the profanity filter"
    - "NetworkManager checks blocks locally before emitting peer_joined and suppresses under-13 outbound chat"
    - "Username error states (Surface G) render with icon_error + destructive red label"
    - "Profanity filter warms up on FriendsClient._ready() — not on first keystroke"
  artifacts:
    - path: "src/ui/sign_in_panel.gd"
      provides: "Username validation on create-account tab (Surface G)"
    - path: "src/autoload/network_manager.gd"
      provides: "Block-aware peer_joined and consent-aware chat suppression"
    - path: "src/autoload/world_save.gd"
      provides: "World name profanity filter on create_world()"
  key_links:
    - from: "src/ui/sign_in_panel.gd"
      to: "src/autoload/username_policy.gd"
      via: "UsernamePol.validate(username)"
      pattern: "UsernamePol.validate"
    - from: "src/autoload/world_save.gd"
      to: "src/networking/profanity_filter.gd"
      via: "ProfanityFilter.filter_reject(name)"
      pattern: "ProfanityFilter.filter_reject"
    - from: "src/autoload/network_manager.gd"
      to: "src/autoload/friends_client.gd"
      via: "FriendsClient.get_blocks() used at peer_joined to check if peer is blocked"
      pattern: "get_blocks"
---

<objective>
Wire username validation (Surface G), world-name profanity rejection, and NetworkManager safety checks (block-aware peer display, under-13 chat suppression) as the final wiring layer before test activation.

Purpose: These connections complete the safety system's enforcement layer on the client side. All pieces (username_policy.gd, profanity filter, FriendsClient block/consent state) were built in earlier plans; this plan wires them into the call sites.

Output: sign_in_panel.gd username validation wired; world_save.gd create_world() filtering; network_manager.gd block and consent guards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-UI-SPEC.md
@.planning/phases/04-multiplayer-seamless-host-failover/04-PATTERNS.md

<interfaces>
<!-- Existing method signatures to extend -->

From src/autoload/world_save.gd (existing create_world signature):
```gdscript
func create_world(world_name: String, seed: int = 0, mode: String = "sandbox") -> bool:
    # Phase 5: add profanity filter check before creating the SQLite file
    # if ProfanityFilter.filter_reject(world_name): return false (show error in caller)
```

From src/autoload/network_manager.gd (existing peer_joined emission):
```gdscript
# In _on_peer_connected() or equivalent:
# Phase 5: before emitting peer_joined, check if peer is in blocks list
# if _is_blocked_locally(peer_uid): hide nameplate, suppress from peer list

# In the outbound chat packet path:
# Phase 5: if FriendsClient.is_under_13 and not FriendsClient.is_consented: drop packet
```

From src/ui/sign_in_panel.gd (username field):
```gdscript
# Phase 4: _username_field LineEdit exists on Create Account tab
# Phase 5: add 500ms debounce Timer for inline validation
# On timeout: result = UsernamePol.validate(_username_field.text)
# if result.valid: show icon_check.png green → right of field
# else: show icon_error.png + _error_label.text = tr(result.error_key)
# Server uniqueness check ONLY on submit (not inline — prevents username enumeration)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Username validation in sign_in_panel + world name filter in world_save</name>
  <files>src/ui/sign_in_panel.gd, src/autoload/world_save.gd</files>
  <behavior>
    sign_in_panel.gd (Create Account tab username field):
    - Add _username_validation_timer: Timer (wait_time=0.5, one_shot=true) in _ready().
    - Connect _username_field.text_changed → _username_validation_timer.start() (restart on each keystroke).
    - Timer.timeout → _validate_username_inline().
    - _validate_username_inline(): result = UsernamePol.validate(_username_field.text). If result.valid: show green icon_check.png (TextureRect 16×16 right of field, tinted #3DB560); clear error label. Else: show red icon_error.png (16×16); _error_label.text = tr(result.error_key). If result.error_key is "" (empty): hide both icons.
    - On submit: also run UsernamePol.validate() synchronously. If invalid: show error, return early. Do NOT make a server uniqueness check inline — only on submit (anti-pattern: prevents username enumeration per 05-UI-SPEC.md checker recommendation 4).
    - Also call ProfanityFilter._ensure_regex() during FriendsClient._ready() to warm up the regex at startup — add this call to FriendsClient._ready() (single call, no performance impact; prevents first-keystroke lag on Tier-3).

    world_save.gd:
    - In create_world(world_name, seed, mode): add guard at top of function — before any SQLite operations. If ProfanityFilter.filter_reject(world_name): push_warning("WorldSave.create_world: world name rejected by profanity filter."); return false. Caller shows "Please choose another name." error.
    - Similarly: if world_name is empty or len < 1: return false (already validated by caller, but defensive check).
    - World name validation is silent (no UI in world_save.gd itself — UI layer in the world creation dialog handles the error case using the false return).
  </behavior>
  <action>Modify sign_in_panel.gd and world_save.gd. Read both files first. For sign_in_panel: the inline validation icons require adding two TextureRect nodes to the scene (icon_check and icon_error) positioned to the right of the username field. These can be added as overlapping absolute-positioned children of the username field's parent container. For world_save: the create_world() guard is a 2-line addition — but it requires ProfanityFilter to be loaded. Add `const ProfanityFilter = preload("res://src/networking/profanity_filter.gd")` at the top if not already present, or access via class_name if registered. For FriendsClient warmup: add `ProfanityFilter._ensure_regex()` to FriendsClient._ready() after existing setup code.</action>
  <verify>
    <automated>godot --headless --check-only src/ui/sign_in_panel.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5 && grep "filter_reject" /Users/jnuyens/src/LegoMinecraft/src/autoload/world_save.gd</automated>
  </verify>
  <done>sign_in_panel.gd has 500ms debounce username validation using UsernamePol.validate(); inline icons shown; server-check only on submit; world_save.gd create_world() calls ProfanityFilter.filter_reject() and returns false on profanity match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetworkManager safety hooks (block-aware peer display + chat suppression)</name>
  <files>src/autoload/network_manager.gd</files>
  <behavior>
    Block-aware peer display:
    - In the peer_joined handling path (after receiving a new peer's UID): call _is_blocked_locally(peer_uid) which checks FriendsClient._blocks_cache (the local cache from get_blocks()). If blocked: do NOT emit peer_joined; instead mark peer as hidden in the session (they are still connected at the WebRTC level — this is a display suppression only; the server-side hard gate already prevented them from joining if blocked). If not blocked: emit peer_joined normally.
    - Add _blocks_cache: Array = [] private var updated when FriendsClient.blocks_loaded signal fires (connect in _ready()).
    - _is_blocked_locally(uid: String) -> bool: return _blocks_cache.any(func(b): return b.get("blocked_uid", "") == uid).

    Under-13 chat suppression:
    - In the outbound chat packet dispatch path (where the chat message RPC is sent): add guard before sending. Check if FriendsClient has an is_under_13 property AND if consent is not yet recorded. If is_under_13 and not consented: drop the packet locally (do not send); show an in-game tip via Toasts autoload: "Chat requires parent approval." (use existing Toasts pattern from Phase 1).
    - Add is_under_13: bool = false and is_consented: bool = false properties to FriendsClient (or use a getter that reads from stored GoTrue metadata). Connect FriendsClient.consent_status_received to update is_consented in NetworkManager.

    Session-join gate for unconsented under-13 (client side):
    - Before NetworkManager initiates a join (not as host), check FriendsClient.is_under_13 and not FriendsClient.is_consented. If true: emit a join_blocked signal (or call the parental gate panel directly) and abort the join. The server-side check (Plan 05-04) is the hard gate; this client-side check is for immediate UX feedback.

    Auto-mute of reported users:
    - Add _session_muted: Array = [] private var. After submit_report() emits report_submitted signal: NetworkManager connects and adds the reported uid to _session_muted.
    - In the incoming chat message dispatch (before routing to ChatOverlay): if sender_uid in _session_muted: drop the message (display filter, not a real block).
  </behavior>
  <action>Modify src/autoload/network_manager.gd. Read the existing file first to understand the Phase 4 state machine and peer management. The blocks_cache integration requires FriendsClient to call get_blocks() at sign-in; connect FriendsClient.blocks_loaded signal in NetworkManager._ready(). The chat suppression guard goes in the RPC method that broadcasts chat messages — likely a method named `_send_chat` or similar. The auto-mute list is cleared on session end (in the IDLE state transition). All new guards use is_instance_valid() checks before accessing FriendsClient per the Phase 4 null-safe singleton pattern.</action>
  <verify>
    <automated>godot --headless --check-only src/autoload/network_manager.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5 && grep "_is_blocked_locally" /Users/jnuyens/src/LegoMinecraft/src/autoload/network_manager.gd</automated>
  </verify>
  <done>network_manager.gd parses without errors; has _blocks_cache updated from FriendsClient.blocks_loaded; _is_blocked_locally() used before peer_joined emission; chat packet suppressed for unconsented under-13 accounts; _session_muted list suppresses reported users' incoming messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client chat → peer | NetworkManager is client-side; server-side Go relay is the hard gate |
| World creation → WorldSave | Username and world name pass through ProfanityFilter before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-bypass | Elevation of Privilege | Client skips block/consent check by patching network_manager | mitigate | Server-side checks in Go signaling (Plan 05-04) are the hard gates; client checks are UX-only |
| T-05-P-wn | Information Disclosure | Username inline validation reveals taken usernames | mitigate | Inline validation is format-only (UsernamePol); server uniqueness check only on submit — no enumeration possible |
</threat_model>

<verification>
```
godot --headless --check-only src/autoload/network_manager.gd
grep "_is_blocked_locally" src/autoload/network_manager.gd
grep "filter_reject" src/autoload/world_save.gd
grep "UsernamePol.validate" src/ui/sign_in_panel.gd
grep "_session_muted" src/autoload/network_manager.gd
```
All must exit 0.
</verification>

<success_criteria>
- sign_in_panel.gd: 500ms debounce username validation; inline icons (check/error); server uniqueness only on submit
- world_save.gd: ProfanityFilter.filter_reject() called in create_world(); returns false on match
- network_manager.gd: _is_blocked_locally() gate before peer_joined emission; chat packet suppressed for unconsented under-13; _session_muted list filters incoming messages
- ProfanityFilter warmed up in FriendsClient._ready()
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-10-SUMMARY.md` when done
</output>
