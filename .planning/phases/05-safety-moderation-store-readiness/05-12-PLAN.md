---
phase: 05-safety-moderation-store-readiness
plan: 12
type: execute
wave: 5
depends_on: [05-03, 05-04, 05-05, 05-07, 05-08, 05-09, 05-10]
files_modified:
  - tests/unit/test_block_unblock.gd
  - tests/unit/test_report_submission.gd
  - tests/unit/test_dob_parser.gd
  - tests/unit/test_parental_consent_token.gd
  - tests/unit/test_profanity_multilang.gd
  - tests/unit/test_username_validator.gd
  - tests/unit/test_eula_hash_recompute.gd
  - tests/integration/test_blocks_check_on_join.gd
  - tests/integration/test_parental_consent_e2e.gd
  - .planning/DOCS.md
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "All 7 Phase 5 unit test files have real assertions (no pending() stubs)"
    - "test_profanity_multilang.gd: filter() replaces EN word, filter_reject() returns true for EN+NL words"
    - "test_username_validator.gd: admin rejected, format regex enforced, cooldown blocks second change"
    - "test_eula_hash_recompute.gd: SHA-256 is consistent; mismatch detected"
    - "test_parental_consent_token.gd: 128-bit base32 token verified"
    - "DOCS.md §8 updated to reflect Phase 5 architecture"
    - "All 40+ Phase 5 tests pass; pre-existing Phase 1-4 test count unchanged"
  artifacts:
    - path: "tests/unit/test_profanity_multilang.gd"
      provides: "Real profanity filter assertions"
    - path: "tests/unit/test_username_validator.gd"
      provides: "Real username policy assertions"
    - path: "tests/unit/test_eula_hash_recompute.gd"
      provides: "Real SHA-256 hash assertions"
    - path: ".planning/DOCS.md"
      provides: "§8 Safety, accounts, and reporting — updated to ground truth"
      contains: "Phase 5"
  key_links:
    - from: "tests/unit/test_profanity_multilang.gd"
      to: "src/networking/profanity_filter.gd"
      via: "ProfanityFilter.filter() + filter_reject()"
      pattern: "ProfanityFilter"
    - from: "tests/unit/test_username_validator.gd"
      to: "src/autoload/username_policy.gd"
      via: "UsernamePol.validate() + is_reserved()"
      pattern: "UsernamePol"
---

<objective>
Activate all Phase 5 test stubs into real assertions, add integration tests for blocks-check and parental consent lifecycle, and update DOCS.md §8 to match the implemented architecture.

Purpose: DDD discipline — DOCS.md §8 is not "a true description of the implementation" until this sync plan runs. Test activation follows the Phase 4 Plan 11 pattern exactly.

Output: 7 unit tests fully passing; 2 integration tests passing; DOCS.md §8 updated to ground truth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-RESEARCH.md
@.planning/phases/04-multiplayer-seamless-host-failover/04-11-SUMMARY.md

<interfaces>
<!-- Test activation pattern from Phase 4 Plan 11 -->

From tests/unit/test_chat_rate_limit.gd (Phase 4 real assertion structure):
```gdscript
func test_profanity_filter_replaces_word() -> void:
    ProfanityFilter.set_word_list(["badword"])
    var result := ProfanityFilter.filter("This is a badword message")
    assert_eq(result, "This is a [filtered] message",
        "filter() must replace matched word with [filtered]")
```

From tests/unit/test_invite_token.gd (Phase 4 token assertion structure):
```gdscript
func test_token_is_26_chars() -> void:
    var token := FriendsClient._generate_invite_token()
    assert_eq(token.length(), 26,
        "Invite token must be exactly 26 base32 characters (16 bytes)")
```

From Phase 4 Plan 11 decision: use .new() + .free() (not add_child_autoqfree) for autoload instantiation in tests to avoid ObjectDB leaks.

GUT pending → real assertion swap: replace `pending("Phase 5 — activate in 05-12")` with actual test logic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate 7 unit test stubs</name>
  <files>tests/unit/test_block_unblock.gd, tests/unit/test_report_submission.gd, tests/unit/test_dob_parser.gd, tests/unit/test_parental_consent_token.gd, tests/unit/test_profanity_multilang.gd, tests/unit/test_username_validator.gd, tests/unit/test_eula_hash_recompute.gd</files>
  <behavior>
    test_profanity_multilang.gd (4 real tests):
    - test_filter_replaces_en_word: ProfanityFilter.load_word_lists(); ProfanityFilter.filter("This is a [test_en_word]") == "This is a [filtered]". Use a known word from wordlist_en.txt. Assert.
    - test_filter_reject_returns_true_for_en_word: ProfanityFilter.load_word_lists(); ProfanityFilter.filter_reject("[test_en_word]") == true.
    - test_filter_reject_returns_false_for_clean_text: ProfanityFilter.filter_reject("hello world builder") == false.
    - test_filter_nl_word: ProfanityFilter.load_word_lists(); ProfanityFilter.filter_reject("[test_nl_word]") == true. Use a known word from wordlist_nl.txt.

    test_username_validator.gd (5 real tests):
    - test_reserved_prefix_admin_rejected: UsernamePol.is_reserved("admin") == true.
    - test_reserved_prefix_admin_prefix_rejected: UsernamePol.is_reserved("admin123") == true.
    - test_format_regex_accepts_valid: UsernamePol.is_valid_format("Builder_42") == true.
    - test_format_regex_rejects_special_chars: UsernamePol.is_valid_format("hello!") == false.
    - test_validate_returns_error_key_for_reserved: result = UsernamePol.validate("cubicraftia"); result.valid == false; result.error_key == "ui.username.error_reserved".

    test_eula_hash_recompute.gd (3 real tests):
    - test_sha256_consistent: hash the literal string "test content" twice via HashingContext; assert both results equal.
    - test_different_content_different_hash: hash "content A" and "content B"; assert not equal.
    - test_mismatch_detected: write a temp file with content "version 1", compute hash, write another temp file with "version 2", compute hash; assert hashes are not equal.

    test_parental_consent_token.gd (4 real tests):
    - test_token_is_128_bit: token = use Crypto.generate_random_bytes(16); token.size() == 16.
    - test_base32_token_length: encode 16 bytes as base32 NoPadding; length == 26 chars.
    - test_two_tokens_are_distinct: generate two tokens via separate Crypto calls; assert not equal.
    - test_revoke_token_distinct_from_consent_token: generate two separate tokens; assert they are different (probabilistically certain with 128-bit entropy).

    test_block_unblock.gd (4 real tests using mock data):
    - test_block_row_has_correct_direction: row = Phase5Fixtures.make_mock_block_row("uid_a", "uid_b"); row.blocker_uid == "uid_a"; row.blocked_uid == "uid_b".
    - test_blocker_sees_own_blocks: Simulate: blocks = [make_mock_block_row("me", "them")]; filter(lambda b: b.blocker_uid == "me") has 1 result.
    - test_blocked_uid_sees_nothing: blocks = [make_mock_block_row("me", "them")]; filter(lambda b: b.blocker_uid == "them") has 0 results (blocked can't see they're blocked).
    - test_unblock_removes_row: list = [row1, row2]; remove row where blocker_uid=="me" and blocked_uid=="them"; assert list reduced by 1.

    test_report_submission.gd (3 real tests using mock data):
    - test_reporter_uid_matches_sender: row = Phase5Fixtures.make_mock_report_row("reporter", "reported", "player", "spam"); row.reporter_uid == "reporter".
    - test_reported_uid_cannot_see_own_reports: rows = [row]; rows filtered by (r.reporter_uid == "reported") has 0 results (reported user sees nothing).
    - test_evidence_chat_context: row = Phase5Fixtures.make_mock_report_row("r","t","chat_message","spam"); row.surface == "chat_message".

    test_dob_parser.gd (4 real tests):
    - test_age_12_is_under_13: compute today - 12*365*86400; assert is_under_13 == true. Use Time.get_unix_time_from_system() for "today".
    - test_age_13_is_not_under_13: today - 13*365*86400 - 86400*30 (13 years + 1 month ago); assert is_under_13 == false.
    - test_age_exactly_13_today_is_not_under_13: compute date exactly 13 years ago; assert is_under_13 == false.
    - test_invalid_date_rejected: day=31, month=2, year=2010 is invalid; validate using Time.get_unix_time_from_datetime_dict; assert result == -1 (invalid).
  </behavior>
  <action>For each test file: replace all pending() stubs with real assertions. Do NOT add before_each/after_each unless the test requires cleanup of side effects. Use the assert_eq/assert_true/assert_false pattern from Phase 4 tests with descriptive error messages referencing DOCS §8 or CONTEXT area numbers. For profanity tests: call ProfanityFilter.load_word_lists() in before_each() so word lists are loaded. For the DOB "exactly 13" test: use Time to get the date 13 years minus 1 day ago (to avoid off-by-one on leap years). All tests must be deterministic (no random seeds that could vary).</action>
  <verify>
    <automated>godot --headless -s tests/gut_main.gd -- -gtest=tests/unit/test_profanity_multilang.gd -gtest=tests/unit/test_username_validator.gd -gtest=tests/unit/test_eula_hash_recompute.gd -gtest=tests/unit/test_parental_consent_token.gd -gtest=tests/unit/test_block_unblock.gd -gtest=tests/unit/test_report_submission.gd -gtest=tests/unit/test_dob_parser.gd 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 unit test files have real assertions (no pending() calls); all pass when run with GUT; no ObjectDB leaks; error messages reference DOCS §8.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests + DOCS.md §8 sync</name>
  <files>tests/integration/test_blocks_check_on_join.gd, tests/integration/test_parental_consent_e2e.gd, .planning/DOCS.md</files>
  <behavior>
    test_blocks_check_on_join.gd (2 real tests):
    - test_blocked_user_cannot_join_session: Simulate NetworkManager receiving a peer_joined event for a UID that is in _blocks_cache. Assert that peer_joined signal is NOT emitted. Mock: create NetworkManager.new(), set _blocks_cache = [{blocked_uid: "blocked_uid_x"}], call _on_peer_joined_internal("blocked_uid_x"), watch_signals(nm), assert_signal_not_emitted(nm, "peer_joined").
    - test_unblocked_user_can_join: Same setup but blocks_cache is empty. Assert peer_joined IS emitted.

    test_parental_consent_e2e.gd (3 real tests):
    - test_under_13_account_is_restricted: Create a Phase5Fixtures mock consent row with consented_at=null; simulate FriendsClient.consent_status_received(false, true); assert NetworkManager._should_suppress_chat() returns true for an is_under_13 account.
    - test_consent_confirmed_unlocks_chat: consent_status_received(true, false); assert _should_suppress_chat() returns false.
    - test_revoked_consent_re_restricts: consent_status_received(false, false) after being consented; assert _should_suppress_chat() returns true again.

    DOCS.md §8 sync:
    - Read existing DOCS.md §8 section.
    - Update or add a "§8 Safety, Accounts, and Reporting (Phase 5)" section documenting:
      - Block system: global mutual block, Supabase blocks table, Go signaling hard gate on join, client fast-hide
      - Report flow: 3 surfaces (player/build/chat_message), 5 categories, Supabase reports table immutable, /admin/reports operator endpoint, 5/24h rate limit
      - Profanity filter: dual-regex EN+NL, LDNOOBW CC-BY-4.0 sourced, filter() + filter_reject() API, all user-authored strings
      - Parental consent: DOB at sign-up, is_under_13 boolean stored (not raw DOB), email-plus COPPA 2025, 7-day token TTL, Go signaling enforcement on join, client restrictions for unconsented under-13
      - EULA/Privacy: bundled Markdown, SHA-256 hash versioning, re-acknowledge gate on hash change
      - Username gating: 3-20 chars, reserved prefixes, 30-day cooldown, Postgres triggers
      - Store readiness: 7 docs/store-readiness/ files, iOS CI on macos-15 (D-04 closure)
    - Format: match existing DOCS.md heading style (## and ### levels). No tables (DOCS.md uses prose). Add a `**Phase 5 carryover debt:**` note for any remaining manual UAT items (COPPA legal review, NCMEC CSAM setup, Apple Developer Program purchase).
  </behavior>
  <action>Activate integration tests and update DOCS.md. For the integration tests: the NetworkManager.new() + .free() pattern from Phase 4 Plan 11 is critical — do NOT use add_child_autoqfree (per Plan 11 decision: causes ObjectDB leaks). The _should_suppress_chat() method must be added to NetworkManager as a testable method (not inline in the RPC dispatch) — this makes it unit-testable. For DOCS.md: read the current §8 section first. If it already has partial content from Phase 4 setup, update it in place. If missing, add it. The DOCS.md sync is the DDD gate: §8 is not "done" until this section is a true description of the shipped implementation.</action>
  <verify>
    <automated>godot --headless -s tests/gut_main.gd 2>&1 | tail -5</automated>
  </verify>
  <done>All Phase 5 tests pass (unit + integration); full GUT suite failure count unchanged from pre-Phase-5 baseline + 0 new failures; DOCS.md §8 has Phase 5 section covering all 7 DOC-08 deliverables; carryover debt noted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → production autoloads | NetworkManager.new() without tree context — ObjectDB leak risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages — pure GDScript and DOCS.md update |
</threat_model>

<verification>
```
godot --headless -s tests/gut_main.gd 2>&1 | grep -E "^(PASSED|FAILED|Totals)" | tail -5
grep -c "pending" tests/unit/test_profanity_multilang.gd
grep "Phase 5" .planning/DOCS.md
```
First: total failures must equal pre-Phase-5 baseline (7). Second: grep must return 0 (no pending() stubs). Third: DOCS §8 Phase 5 section present.
</verification>

<success_criteria>
- 0 pending() stubs in any Phase 5 test file
- All Phase 5 unit and integration tests pass
- Full GUT suite: failure count = pre-Phase-5 baseline
- DOCS.md §8 updated with implemented architecture for all 7 DOC-08 deliverables
- Phase 5 carryover debt documented (COPPA legal review, NCMEC, Apple Developer Program)
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-12-SUMMARY.md` when done
</output>
