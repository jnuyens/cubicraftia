---
phase: 05-safety-moderation-store-readiness
plan: 07
type: execute
wave: 3
depends_on: [05-01, 05-05]
files_modified:
  - src/ui/sign_in_panel.gd
  - src/ui/sign_in_panel.tscn
  - src/ui/parental_gate_panel.gd
  - src/ui/parental_gate_panel.tscn
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "Sign-in panel's Create Account tab has DOB picker row (Day/Month/Year OptionButtons)"
    - "Under-13 detection runs client-side at submit; raw DOB never sent to server"
    - "On under-13 sign-up success, Parental Gate Panel is shown automatically"
    - "Parental Gate Panel: email field + Send link + Skip for now + loading state + success state"
    - "Restricted-account banner (amber, full-width, layer 5) shown when is_under_13 and not consented"
  artifacts:
    - path: "src/ui/sign_in_panel.gd"
      provides: "Extended sign-in panel with DOB picker (Surface C/K)"
      exports: ["SignInPanel"]
    - path: "src/ui/parental_gate_panel.gd"
      provides: "Parental consent UI (Surface D + restricted banner Surface F)"
      exports: ["ParentalGatePanel"]
  key_links:
    - from: "src/ui/sign_in_panel.gd"
      to: "src/autoload/friends_client.gd"
      via: "FriendsClient.sign_up(email, password, dob_day, dob_month, dob_year)"
      pattern: "FriendsClient.sign_up"
    - from: "src/ui/parental_gate_panel.gd"
      to: "src/autoload/friends_client.gd"
      via: "FriendsClient.request_parental_consent(parent_email)"
      pattern: "FriendsClient.request_parental_consent"
---

<objective>
Implement the DOB picker (Surface C) in sign_in_panel.gd and the standalone Parental Gate Panel (Surface D + restricted-account banner Surface F).

Purpose: COPPA compliance requires age-gate at sign-up and parental consent for under-13. The restricted-account banner must show persistently for unconsented accounts.

Output: sign_in_panel.gd extended with DOB row; parental_gate_panel.gd/.tscn new scene; restricted-account banner integrated into the HUD CanvasLayer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-UI-SPEC.md
@.planning/phases/04-multiplayer-seamless-host-failover/04-PATTERNS.md

<interfaces>
<!-- Pattern: sign_in_panel.gd already exists — extend, do not rewrite -->

From src/ui/sign_in_panel.gd (Phase 4 existing nodes):
```gdscript
extends Control

@onready var _email_field: LineEdit = $...
@onready var _password_field: LineEdit = $...
@onready var _age_checkbox: CheckBox = $...       # Phase 5: REMOVE this, replace with DOB row
@onready var _error_label: Label = $...           # Phase 5: reuse for DOB errors

# Phase 5 new nodes to add in tscn and reference:
@onready var _dob_day: OptionButton = $...
@onready var _dob_month: OptionButton = $...
@onready var _dob_year: OptionButton = $...
```

From Phase 4 patterns (HP bar _ready pattern for signal subscription):
```gdscript
func _ready() -> void:
    FriendsClient.signed_in.connect(_on_signed_in)
    FriendsClient.sign_in_failed.connect(_on_sign_in_failed)
    # Phase 5: also connect consent_status_received
```

CanvasLayer layer structure (from 05-UI-SPEC.md Panel Mutual-Exclusion):
- Layer 5: HUD layer (restricted banner goes here, top of HUD)
- Layer 20: Modals (parental gate panel)

DOB validation (from 05-UI-SPEC.md Surface C):
- All 3 OptionButtons must be non-placeholder (day != 0, month != 0, year != 0)
- Invalid date check (e.g. Feb 31): use Time.get_unix_time_from_datetime_dict({year,month,day}) — returns -1 for invalid
- Under-13: birth_date + 13 years > today
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sign_in_panel.gd DOB picker (Surface C/K)</name>
  <files>src/ui/sign_in_panel.gd, src/ui/sign_in_panel.tscn</files>
  <behavior>
    - Remove _age_checkbox reference from GDScript and scene. The msgid ui.signin.age_checkbox key remains in en.po (per 05-UI-SPEC.md: "must NOT be removed").
    - Add 3 OptionButton nodes to sign_in_panel.tscn in an HBoxContainer between _password_field and _error_label. HBoxContainer: 8px separation (sm token). OptionButtons: _dob_day (72px, values 1-31), _dob_month (120px, 12 month name keys using tr()), _dob_year (96px, values current_year-100 to current_year descending, populated in _build_dob_year_dropdown()). All start with placeholder text ("Day"/"Month"/"Year" = 0 value option at top).
    - Combined DOB row: min height 44px (mobile touch target per DOCS §7.6).
    - _build_dob_ui() method: called from _ready(). Populates Day (1-31), Month (tr(ui.signin.dob_month_jan) through dec), Year (current_year-100 to current_year, add_item in loop).
    - Validation in _on_submit_create_account(): check all 3 selections non-zero. If incomplete: _error_label.text = tr("ui.signin.error_dob_incomplete"); return. Validate date validity using Time.get_unix_time_from_datetime_dict. If invalid: _error_label.text = tr("ui.signin.error_dob_invalid"); return.
    - Under-13 detection: compute birth_unix = Time.get_unix_time_from_datetime_dict({year, month, day}). Get today_unix = Time.get_unix_time_from_system(). is_under_13 = (today_unix - birth_unix) < (13 * 365.25 * 86400). Pass is_under_13 result to FriendsClient.sign_up() via the extended DOB params. NEVER pass raw DOB values to FriendsClient.
    - After successful sign_up with is_under_13=true: transition to parental gate panel (show_parental_gate_panel()). Do not show the normal "signed in" state.
  </behavior>
  <action>Modify src/ui/sign_in_panel.gd and sign_in_panel.tscn. Read both files first. Remove the _age_checkbox node from the scene tree and its references in GDScript. Add the HBoxContainer with 3 OptionButtons. Use the @onready pattern from Phase 4 patterns for node references. The Year dropdown has ~100 items — populate in _build_dob_ui() called once from _ready() to avoid per-frame overhead (per 05-UI-SPEC.md Performance Constraints). The under-13 check computes a float comparison — use the 13 * 365.25 * 86400 seconds approximation which is accurate enough for this use case (COPPA "good faith" standard, per 05-RESEARCH.md Pitfall 6).</action>
  <verify>
    <automated>godot --headless --check-only src/ui/sign_in_panel.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5</automated>
  </verify>
  <done>sign_in_panel.gd parses without errors; has _dob_day, _dob_month, _dob_year OptionButton references; _build_dob_ui() populates all 3 dropdowns; submit handler validates DOB completeness and validity; under-13 detection passes is_under_13 flag to FriendsClient.sign_up(); raw DOB never sent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parental_gate_panel.gd + restricted-account banner (Surface D + F)</name>
  <files>src/ui/parental_gate_panel.gd, src/ui/parental_gate_panel.tscn</files>
  <behavior>
    - New Control node (not PanelContainer — it renders its own panel chrome). CanvasLayer layer 20. Full-screen overlay with navy 60% alpha ColorRect backdrop (mouse_filter = MOUSE_FILTER_STOP).
    - Inner PanelContainer: 480px wide centred, 24px content margin. StyleBox: dominant navy 0.92 alpha.
    - States:
      1. DEFAULT: heading "Ask a parent to confirm" (tr(ui.consent.title)), body paragraph, parent email LineEdit, "Send link" button (primary), "Why am I seeing this?" LinkButton (toggles collapsible VBox), "Skip for now" button (secondary).
      2. LOADING: "Send link" button text = tr(ui.consent.sending), button.disabled = true.
      3. SUCCESS: icon_email.png (48×48), heading "Link sent!" (tr(ui.consent.success_heading)), body tr(ui.consent.success_body) with {parent_email} substituted. "Done" button (secondary, 160px). "Resend" LinkButton below.
      4. ERROR: destructive red label below email field.
    - Restricted-account banner (Surface F): separate HBoxContainer, 40px height, full-width, anchored to top of main scene CanvasLayer 5. Background: StyleBox_warning_banner (amber, from 05-01). Content: icon_warning.png (16×16, navy-tinted) + Label(tr(ui.restricted.banner_label)) + LinkButton(tr(ui.restricted.resend_link)). Visibility: hidden by default; shown when FriendsClient emits consent_status_received(false, *). LinkButton press: opens this panel at the resend state (email field pre-filled).
    - Signal subscriptions in _ready(): FriendsClient.consent_status_received.connect(_on_consent_status).
    - On consent_status_received(is_consented=true): hide this panel AND hide the restricted-account banner.
    - Session-join gate: connect to SessionRegistry or FriendsClient session-join signal; if is_under_13 and not consented, intercept with a blocked modal (400px, navy overlay, per 05-UI-SPEC.md Surface F "Session join gate" spec).
  </behavior>
  <action>Create src/ui/parental_gate_panel.gd and parental_gate_panel.tscn. For the restricted-account banner: it can be a child node of the main scene's CanvasLayer 5, or a separate scene autoloaded. Simpler: add it as a child of the main_scene's HUD CanvasLayer (same approach as hp_bar.gd). Use the get_node_or_null tree traversal pattern from Phase 4 patterns. For email validation: basic check — must contain "@" and "." (per 05-UI-SPEC.md Surface D). For the "Why am I seeing this?" collapsible: use a VBoxContainer with visible=false toggled on button press. The explanation text uses i18n key ui.consent.why_explanation. Use SPDX header on the .gd file.</action>
  <verify>
    <automated>godot --headless --check-only src/ui/parental_gate_panel.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5 && test -f /Users/jnuyens/src/LegoMinecraft/src/ui/parental_gate_panel.tscn</automated>
  </verify>
  <done>parental_gate_panel.gd and .tscn exist; parses without errors; has 4 states (DEFAULT/LOADING/SUCCESS/ERROR); connects to FriendsClient.consent_status_received; restricted-account banner logic present; email validation client-side; all text via tr() keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Under-13 account → sign_up() | Client computes is_under_13; server re-validates via GoTrue metadata + consent check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-S2 | Spoofing | DOB lying — under-13 declares 13+ | accept | COPPA "good faith" self-declaration standard; no technical mitigation; documented in PRIVACY.md |
| T-05-E3 | Elevation of Privilege | Under-13 bypasses client DOB check | mitigate | Go signaling server re-checks consent status on every offer relay (Plan 05-04); client check is UX only |
</threat_model>

<verification>
```
godot --headless --check-only src/ui/sign_in_panel.gd
godot --headless --check-only src/ui/parental_gate_panel.gd
grep "_dob_day" src/ui/sign_in_panel.gd
grep "request_parental_consent" src/ui/parental_gate_panel.gd
grep "consent_status_received" src/ui/parental_gate_panel.gd
```
All must exit 0.
</verification>

<success_criteria>
- sign_in_panel.gd: has DOB picker (_dob_day/_dob_month/_dob_year); validates DOB; computes is_under_13; never sends raw DOB to server
- parental_gate_panel.gd: has 4 UI states; calls FriendsClient.request_parental_consent(); restricted-account banner toggles on consent_status_received signal
- Both files use tr() for all strings; no hardcoded English
- Both .tscn files exist
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-07-SUMMARY.md` when done
</output>
