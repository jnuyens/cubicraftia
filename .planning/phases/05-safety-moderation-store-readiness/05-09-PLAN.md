---
phase: 05-safety-moderation-store-readiness
plan: 09
type: execute
wave: 3
depends_on: [05-01, 05-05, 05-06]
files_modified:
  - src/ui/legal_viewer.gd
  - src/ui/legal_viewer.tscn
  - src/ui/eula_acknowledge_modal.gd
  - src/ui/eula_acknowledge_modal.tscn
  - src/ui/settings_menu.gd
  - src/ui/settings_menu.tscn
autonomous: true
requirements: [DOC-08]
gap_closure: false

must_haves:
  truths:
    - "LegalViewer opens EULA.md or PRIVACY.md via FileAccess and renders Markdown → BBCode in RichTextLabel"
    - "LegalViewer shows SHA-256 version hash (first 16 hex chars) in footer"
    - "I agree button visible only on first view or re-acknowledge flow; writes hash to settings.cfg"
    - "EULA re-acknowledge modal shown on app launch when bundled hash differs from stored hash"
    - "Settings menu has Legal section (Terms/Privacy buttons) and Account section (username + delete)"
  artifacts:
    - path: "src/ui/legal_viewer.gd"
      provides: "Markdown-to-BBCode renderer for EULA/Privacy (Surface E)"
      exports: ["LegalViewer"]
    - path: "src/ui/eula_acknowledge_modal.gd"
      provides: "Hash-mismatch re-acknowledge gate modal"
      exports: ["EulaAcknowledgeModal"]
    - path: "src/ui/settings_menu.gd"
      provides: "Settings extended with Legal + Account sections (Surface L mod)"
  key_links:
    - from: "src/ui/legal_viewer.gd"
      to: "docs/EULA.md"
      via: "FileAccess.open('res://docs/EULA.md')"
      pattern: "docs/EULA.md"
    - from: "src/ui/eula_acknowledge_modal.gd"
      to: "src/autoload/friends_client.gd"
      via: "FriendsClient.check_eula_acknowledgement() and store_eula_hash()"
      pattern: "check_eula_acknowledgement"
    - from: "src/ui/settings_menu.gd"
      to: "src/ui/legal_viewer.gd"
      via: "LegalViewer.open('eula') / open('privacy')"
      pattern: "LegalViewer"
---

<objective>
Implement the EULA+Privacy in-app viewer (Surface E), the EULA re-acknowledge modal, and extend the Settings menu with Legal and Account sections (Surface L).

Purpose: Store submission requires a visible EULA/Privacy link. The hash-versioned re-acknowledge gate ensures users accept updated terms. Account deletion and username management live in Settings.

Output: legal_viewer.gd/.tscn, eula_acknowledge_modal.gd/.tscn, settings_menu.gd extended.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-safety-moderation-store-readiness/05-CONTEXT.md
@.planning/phases/05-safety-moderation-store-readiness/05-UI-SPEC.md
@.planning/phases/04-multiplayer-seamless-host-failover/04-PATTERNS.md

<interfaces>
<!-- Existing settings_menu.gd structure — extend at the bottom -->

From src/ui/settings_menu.gd (Phase 4 structure):
```gdscript
extends PanelContainer

# Existing sections: Graphics, Brick Packs, About
# Phase 5: add Legal section and Account section after About

func _ready() -> void:
    _setup_labels()
    _build_preset_chips()
    _update_iap_section()
    _setup_buttons()
    _load_saved_preset()
    # Phase 5: _setup_legal_section(), _setup_account_section()
```

From existing ConfigFile pattern (settings_menu.gd / friends_client.gd):
```gdscript
var cfg := ConfigFile.new()
cfg.load("user://settings.cfg")
var stored_hash: String = cfg.get_value("legal", "eula_hash", "")
```

From 05-RESEARCH.md Pattern 4 (EULA hash check — exact implementation):
```gdscript
func check_eula_acknowledgement() -> bool:
    var ctx := HashingContext.new()
    ctx.start(HashingContext.HASH_SHA256)
    var f := FileAccess.open("res://docs/EULA.md", FileAccess.READ)
    # ... chunked read, ctx.update(), ctx.finish()
    var current_hash: String = hash_bytes.hex_encode().substr(0, 16)
    # compare to stored_hash
```

Markdown subset supported by renderer (from 05-UI-SPEC.md):
- # H1 → font_size 20, bold
- ## H2 → bold  
- ### H3 → bold + italic
- **bold** text → [b]...[/b]
- *italic* text → [i]...[/i]
- - bullet list → "• " prefix
- Plain paragraphs → Label nodes
- Plain-language summary block → PanelContainer with StyleBox_legal_summary
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LegalViewer + EulaAcknowledgeModal (Surface E)</name>
  <files>src/ui/legal_viewer.gd, src/ui/legal_viewer.tscn, src/ui/eula_acknowledge_modal.gd, src/ui/eula_acknowledge_modal.tscn</files>
  <behavior>
    LegalViewer:
    - CanvasLayer layer 15. PanelContainer: 560px desktop, full-width-minus-32px mobile. Height: 80% viewport. Navy 0.95 alpha background with 16px rounded corners. open(doc_type: String) public method where doc_type is "eula" or "privacy".
    - Panel header (HBoxContainer, 48px): Title Label (heading 20px semibold, brick white, expand fill). "Close" Button (secondary, 80px). Close hides the CanvasLayer.
    - ScrollContainer fills remaining height. Inside: VBoxContainer built from Markdown parse of the document.
    - Markdown parser (static func md_to_nodes(content: String) -> Array[Control]): processes lines. H1 → Label (font_size 20, bold). H2 → Label (bold). H3 → Label (bold+italic). "- " or "* " prefix → Label with "• " prepended. **bold** → use RichTextLabel with bbcode_enabled=true for inline formatting in that line. Plain line → Label (16px regular, auto-wrap, expand). Empty line → 8px gap Control. Plain-language summary block: lines between "## Summary" and the next "## " heading are wrapped in a PanelContainer with StyleBox_legal_summary.
    - Footer bar (HBoxContainer, 40px, top border 1px brick-white 20% alpha): version hash Label (14px, brick-white 40%, left). "I agree" Button (primary, 160px, right) — visible only when show_agree=true (set by EulaAcknowledgeModal flow or first-ever view). On "I agree": call FriendsClient.store_eula_hash(current_hash); emit eula_agreed signal; hide agree button.
    - open() reads file, computes SHA-256 hash, builds node tree, shows. Document parse happens once per open — no re-parse on scroll.

    EulaAcknowledgeModal:
    - CanvasLayer layer 20. PanelContainer 400px, centred, navy overlay.
    - Content: Heading tr(ui.legal.reack_title). Body tr(ui.legal.reack_body). "Review Terms" Button (primary, full-width): opens LegalViewer(eula) with show_agree=true; connects to LegalViewer.eula_agreed signal → dismiss this modal. "Sign out" Button (secondary, full-width): calls FriendsClient.sign_out(); dismisses modal.
    - Cannot be dismissed without choosing one option (no tap-outside, no Escape).
    - show() public method: called from the app's main launch sequence after sign-in if FriendsClient.check_eula_acknowledgement() returns false.
  </behavior>
  <action>Create all 4 files. For the Markdown parser: keep it simple — handle exactly the 7 constructs from the spec. The trickiest part is inline **bold**: detect lines containing ** pairs; split the line at ** boundaries; build a RichTextLabel with bbcode_enabled=true for those lines. For all other lines: bare Label nodes are sufficient (RichTextLabel is heavier). The summary block detection: track when we're inside the "## Summary" section and apply StyleBox_legal_summary to that section's PanelContainer. For SHA-256: use HashingContext.HASH_SHA256 with chunked reads of 4096 bytes per 05-RESEARCH.md Pattern 4 exactly. For mobile sizing: use get_viewport().get_visible_rect().size to compute 80% height and full-width-minus-32px width.</action>
  <verify>
    <automated>godot --headless --check-only src/ui/legal_viewer.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5 && godot --headless --check-only src/ui/eula_acknowledge_modal.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5</automated>
  </verify>
  <done>legal_viewer.gd parses without errors; has open(doc_type) method; renders Markdown subset; shows SHA-256 hash in footer; I agree button writes hash via FriendsClient.store_eula_hash(); eula_acknowledge_modal.gd has show() method and cannot be dismissed without a choice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settings menu Legal + Account sections (Surface L)</name>
  <files>src/ui/settings_menu.gd, src/ui/settings_menu.tscn</files>
  <behavior>
    Settings menu extensions (after existing About section):

    Legal section:
    - HSeparator above. Label tr(ui.settings.legal_section_title) (14px semibold, brick-white 70% alpha). Two buttons: tr(ui.settings.terms_button) and tr(ui.settings.privacy_button), both secondary style, full-width. On press: LegalViewer.open("eula") or LegalViewer.open("privacy").

    Account section:
    - HSeparator above. Label tr(ui.settings.account_section_title) (14px semibold). Username row: Label tr(ui.settings.account_username_label) + current username value (FriendsClient.get_username() or stored from sign-in). "Change username" button (secondary, full-width): if cooldown active (days_until_change_allowed > 0), show as disabled with Label tr(ui.settings.account_change_username) + " (in {n} days)" suffix; otherwise enabled. On press: expand inline username change form (VBoxContainer, 0.22s tween — same animation as slide-in panels): LineEdit (placeholder "New username"), inline validation icon (icon_check.png green or icon_error.png red) with 500ms debounce using UsernamePol.validate(text). "Save" Button (primary, disabled until valid format). "Cancel" Button (secondary, 120px). On save: call FriendsClient.change_username(new_username) — this method needs adding to FriendsClient if not present (PATCH /rest/v1/profiles?id=eq.{uid} with body {username: new_username}); on success collapse form, update username label. On error: show error from UsernamePol or server (taken, reserved, cooldown).
    - "Delete account" Button (StyleBox_button_destructive, full-width). On press: confirmation modal (400px, centred, navy overlay): Heading tr(ui.settings.account_delete_title) (destructive red). Body tr(ui.settings.account_delete_body). "Delete account" button (destructive). "Cancel" button (secondary). On confirm: call FriendsClient.request_account_deletion() (add this method to FriendsClient — POST to GoTrue /auth/v1/user/... or Supabase admin delete; for v1: mark a deletion_requested_at column in profiles). Show pending state in Account section.
    - If deletion_pending: show tr(ui.settings.account_delete_pending) label (warning amber) + "Cancel deletion" button (secondary). Cancel deletion: call FriendsClient.cancel_account_deletion().
  </behavior>
  <action>Modify src/ui/settings_menu.gd and settings_menu.tscn. Read both existing files first. Add _setup_legal_section() and _setup_account_section() called from _ready(). The inline username form uses the tween kill-before-create pattern from Phase 4 patterns. The cooldown days display requires fetching the last change timestamp — call FriendsClient or UsernamePol.days_until_change_allowed(last_changed_unix). For change_username: if not already in FriendsClient, add it (PATCH to Supabase profiles table with the new username, handle 409 conflict = taken, handle 422 = reserved/format/cooldown trigger). For account deletion: request_account_deletion() can POST to a Go signaling /account/delete endpoint or use Supabase admin DELETE — for v1, implement as PATCH profiles SET deletion_requested_at = NOW() (a scheduled cleanup job handles the actual deletion after 7 days).</action>
  <verify>
    <automated>godot --headless --check-only src/ui/settings_menu.gd 2>&1 | grep -E "(ERROR|Parse error)" | head -5 && grep "legal_section" /Users/jnuyens/src/LegoMinecraft/src/ui/settings_menu.gd && grep "account_section" /Users/jnuyens/src/LegoMinecraft/src/ui/settings_menu.gd</automated>
  </verify>
  <done>settings_menu.gd parses without errors; has _setup_legal_section() and _setup_account_section(); Legal section opens LegalViewer; Account section has username display + change form + delete button; all text via tr().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App launch → EULA hash gate | Local settings.cfg compared to bundled hash; fail-open on missing file |
| User → username change | FriendsClient PATCH + server trigger enforces 30-day cooldown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-EULA-skip | Tampering | Player clears user://settings.cfg to bypass re-acknowledge | accept | Low risk — clearing settings only makes the EULA reappear (cannot skip it, just delays it); the modal blocks play until accepted |
| T-05-DEL | Repudiation | Account deletion during grace period | accept | 7-day grace with visible cancellation option; deletion is irreversible after grace — user-visible in settings |
</threat_model>

<verification>
```
godot --headless --check-only src/ui/legal_viewer.gd
godot --headless --check-only src/ui/eula_acknowledge_modal.gd
godot --headless --check-only src/ui/settings_menu.gd
grep "store_eula_hash" src/ui/legal_viewer.gd
grep "LegalViewer" src/ui/settings_menu.gd
grep "check_eula_acknowledgement" src/ui/eula_acknowledge_modal.gd
```
All must exit 0.
</verification>

<success_criteria>
- LegalViewer renders EULA.md and PRIVACY.md with supported Markdown constructs
- SHA-256 hash of document shown in footer; I agree writes hash to settings.cfg
- EulaAcknowledgeModal shown when hash mismatch; cannot be dismissed without choice
- Settings menu has Legal section with Terms/Privacy buttons opening LegalViewer
- Settings menu has Account section with username display, change form (inline, with cooldown), delete button
</success_criteria>

<output>
Create `.planning/phases/05-safety-moderation-store-readiness/05-09-SUMMARY.md` when done
</output>
