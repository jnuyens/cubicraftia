---
phase: 07-asset-integration
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - src/ui/hp_bar.gd
  - src/ui/ftue_overlay.gd
  - src/ui/world_select_screen.gd
autonomous: true
requirements:
  - ASSET-04
  - ASSET-05
must_haves:
  truths:
    - "HP bar heart icons display real art (health_heart_full/half/empty.png) in-game"
    - "FTUE overlay arrow displays the real ftue_arrow.png during all 4 steps"
    - "FTUE overlay shows a matching storyboard panel image at each of the 4 steps"
    - "World-select screen background resolves to a real texture (icons/title_bg.png)"
  artifacts:
    - path: "src/ui/hp_bar.gd"
      provides: "Fixed ICON_FULL/HALF/EMPTY constants to health_heart_* filenames"
      contains: "health_heart_full"
    - path: "src/ui/ftue_overlay.gd"
      provides: "Fixed arrow path + new storyboard TextureRect in _setup_nodes; panel load in _activate_step"
      contains: "assets/textures/icons/ftue_arrow.png"
    - path: "src/ui/world_select_screen.gd"
      provides: "Dead title_bg_bricks.png path replaced with icons/title_bg.png"
      contains: "assets/textures/icons/title_bg.png"
  key_links:
    - from: "src/ui/hp_bar.gd ICON_FULL"
      to: "assets/textures/icons/health_heart_full.png"
      via: "const string + load()"
      pattern: "health_heart_full"
    - from: "src/ui/ftue_overlay.gd line 211"
      to: "assets/textures/icons/ftue_arrow.png"
      via: "ResourceLoader.exists() guard + load()"
      pattern: "assets/textures/icons/ftue_arrow"
    - from: "src/ui/ftue_overlay.gd _activate_step"
      to: "assets/textures/ftue/panel_NN_*.png"
      via: "_STEP_PANELS const array + ResourceLoader.exists() + load()"
      pattern: "assets/textures/ftue/panel_"
    - from: "src/ui/world_select_screen.gd _build_ui"
      to: "assets/textures/icons/title_bg.png"
      via: "ResourceLoader.exists() guard"
      pattern: "assets/textures/icons/title_bg"
---

<objective>
Fix all code-side path/naming gaps (G-01, G-02, G-06, G-08) so existing real art assets are correctly referenced.

Purpose: The committed art already exists at the right file paths — the code is pointing at wrong names or wrong directories. This plan fixes the code without touching assets. After this plan, the audit test from 07-01 will pass for ASSET-04 and ASSET-05 items.

Output: Three modified GDScript files with corrected res:// path strings and the new FTUE storyboard TextureRect + per-step panel loading.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-asset-integration/07-RESEARCH.md
@.planning/phases/07-asset-integration/07-CONTEXT.md
@.planning/phases/07-asset-integration/07-01-SUMMARY.md

<interfaces>
<!-- From src/ui/hp_bar.gd — current wrong constants (lines 39-45) -->
const ICON_FULL:  String = "res://assets/textures/icons/heart_full.png"    # WRONG — file is health_heart_full.png
const ICON_HALF:  String = "res://assets/textures/icons/heart_half.png"    # WRONG — file is health_heart_half.png
const ICON_EMPTY: String = "res://assets/textures/icons/heart_empty.png"   # WRONG — file is health_heart_empty.png

<!-- From src/ui/ftue_overlay.gd — current wrong arrow path (line 211) -->
var arrow_tex: Texture2D = load("res://assets/icons/ftue_arrow.png") if ResourceLoader.exists("res://assets/icons/ftue_arrow.png") else null
<!-- Actual asset path: res://assets/textures/icons/ftue_arrow.png (confirmed 3.2 KB, real art) -->

<!-- From src/ui/ftue_overlay.gd — state vars declared at top (lines 99-105) -->
var _narration_container: PanelContainer = null
var _narration_label: Label = null
var _arrow_texture: TextureRect = null
<!-- _storyboard_panel does NOT exist yet — must be added -->

<!-- From src/ui/ftue_overlay.gd — _setup_nodes() adds children to _narration_container -->
<!-- narration_row (HBoxContainer) is a child of _narration_container -->
<!-- _activate_step(step) handles steps 1-4 with match statement -->

<!-- From src/ui/world_select_screen.gd — dead path (line 153) -->
var bg_tex_path := "res://assets/textures/title_bg_bricks.png"   # NEVER EXISTED
<!-- Fix: replace with "res://assets/textures/icons/title_bg.png" (confirmed 1.6 MB real art) -->

<!-- FTUE storyboard panel filenames (confirmed real art, 550-720 KB each) -->
"res://assets/textures/ftue/panel_01_builder_lands_on_island.png"  # step 1 — arrival
"res://assets/textures/ftue/panel_02_breaks_first_block.png"       # step 2 — mine tree
"res://assets/textures/ftue/panel_04_places_block.png"             # step 3 — place plank
"res://assets/textures/ftue/panel_05_crafts_a_tool.png"            # step 4 — completion/progress
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hp_bar.gd heart icon path constants (G-01)</name>
  <files>src/ui/hp_bar.gd</files>
  <read_first>
    - src/ui/hp_bar.gd lines 39-45 (ICON_FULL, ICON_HALF, ICON_EMPTY constants)
    - assets/textures/icons/ listing (health_heart_full.png, health_heart_half.png, health_heart_empty.png confirmed ~60 KB each — real art)
  </read_first>
  <action>
Edit src/ui/hp_bar.gd: update the three path constants at lines 39-45 to match the actual committed filenames.

Change ICON_FULL from "res://assets/textures/icons/heart_full.png" to "res://assets/textures/icons/health_heart_full.png"
Change ICON_HALF from "res://assets/textures/icons/heart_half.png" to "res://assets/textures/icons/health_heart_half.png"
Change ICON_EMPTY from "res://assets/textures/icons/heart_empty.png" to "res://assets/textures/icons/health_heart_empty.png"

Update the comment on line 10 (currently says "heart_full.png / heart_half.png / heart_empty.png") to reflect the actual filenames "health_heart_full.png / health_heart_half.png / health_heart_empty.png".

Do not change any other logic. The ResourceLoader.exists() guard that consumes these constants already handles missing files gracefully — this fix just makes the guard succeed.
  </action>
  <verify>
    <automated>grep -c "health_heart_full" /Users/jnuyens/src/Cubicraftia/src/ui/hp_bar.gd</automated>
  </verify>
  <done>hp_bar.gd ICON_FULL/HALF/EMPTY all reference health_heart_*.png. grep -c "health_heart_full" returns >= 1. No "heart_full.png" (old wrong name) remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ftue_overlay.gd arrow path + add storyboard panel (G-02 + G-06)</name>
  <files>src/ui/ftue_overlay.gd</files>
  <read_first>
    - src/ui/ftue_overlay.gd lines 80-106 (state variable declarations — need to add _storyboard_panel var)
    - src/ui/ftue_overlay.gd lines 173-215 (_setup_nodes — narration_container and narration_row structure; line 211 has wrong arrow path)
    - src/ui/ftue_overlay.gd lines 330-370 (_activate_step match statement — where to add panel texture loads)
    - .planning/phases/07-asset-integration/07-RESEARCH.md Code Examples section (FTUE storyboard panel TextureRect pattern)
  </read_first>
  <action>
Make three edits to src/ui/ftue_overlay.gd:

EDIT 1 — Add _storyboard_panel state var (after the existing var declarations near line 105):
Add: var _storyboard_panel: TextureRect = null
This must be placed alongside the other node-reference vars (_narration_container, _narration_label, etc.)

EDIT 2 — Fix arrow path and add storyboard TextureRect in _setup_nodes():
Line 211: change both occurrences of "res://assets/icons/ftue_arrow.png" to "res://assets/textures/icons/ftue_arrow.png" (the ResourceLoader.exists() check and the load() call use the same string — update both).
After the narration_row HBoxContainer is added to _narration_container (after line 201 "add_child(narration_row)"), insert a new storyboard panel node:
  - Create a TextureRect named "StoryboardPanel"
  - custom_minimum_size = Vector2(256, 144)
  - expand_mode = TextureRect.EXPAND_FIT_WIDTH_PROPORTIONAL
  - stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
  - mouse_filter = Control.MOUSE_FILTER_IGNORE
  - Add as a child of _narration_container (add_child before or after narration_row — before narration_row so it appears above the text)
  - Assign to _storyboard_panel

EDIT 3 — Load panel texture in _activate_step():
Add a const _STEP_PANELS at the top of the file (after the other constants, around line 65-66 area):
const _STEP_PANELS: Array[String] = [
    "res://assets/textures/ftue/panel_01_builder_lands_on_island.png",
    "res://assets/textures/ftue/panel_02_breaks_first_block.png",
    "res://assets/textures/ftue/panel_04_places_block.png",
    "res://assets/textures/ftue/panel_05_crafts_a_tool.png",
]

At the START of _activate_step(step) function, before the match statement, add panel loading:
  var panel_idx: int = clamp(step - 1, 0, _STEP_PANELS.size() - 1)
  var panel_path: String = _STEP_PANELS[panel_idx]
  if _storyboard_panel != null:
      if ResourceLoader.exists(panel_path):
          _storyboard_panel.texture = load(panel_path) as Texture2D
      else:
          _storyboard_panel.texture = null

The FTUE overlay is NON-DISMISSABLE (DOCS §1.4). Do not add any close/skip/dismiss behavior. The word "skip" must not appear in any new code or comments per the ftue-arrow-no-skip decision.
  </action>
  <verify>
    <automated>grep -c "assets/textures/icons/ftue_arrow" /Users/jnuyens/src/Cubicraftia/src/ui/ftue_overlay.gd && grep -c "_storyboard_panel" /Users/jnuyens/src/Cubicraftia/src/ui/ftue_overlay.gd && grep -c "_STEP_PANELS" /Users/jnuyens/src/Cubicraftia/src/ui/ftue_overlay.gd && ! grep -qi "skip" /Users/jnuyens/src/Cubicraftia/src/ui/ftue_overlay.gd</automated>
  </verify>
  <done>ftue_overlay.gd has: (a) "assets/textures/icons/ftue_arrow" present (count >= 2, both occurrences fixed), (b) _storyboard_panel var declared and assigned in _setup_nodes, (c) _STEP_PANELS const with 4 panel paths, (d) panel load logic in _activate_step, (e) zero occurrences of "skip" (case-insensitive).</done>
</task>

<task type="auto">
  <name>Task 3: Fix world_select_screen.gd dead background path (G-08)</name>
  <files>src/ui/world_select_screen.gd</files>
  <read_first>
    - src/ui/world_select_screen.gd lines 140-163 (_build_ui background TextureRect setup)
  </read_first>
  <action>
Edit src/ui/world_select_screen.gd line 153: change the background texture path from the dead "res://assets/textures/title_bg_bricks.png" (never existed) to "res://assets/textures/icons/title_bg.png" (the real 1.6 MB hero art, already used by title_scene.gd).

The existing ResourceLoader.exists() guard stays in place — no structural change. Only the path string changes.

Update the adjacent comment if one exists to reflect that the new path points to the hero vista from title_scene, giving visual continuity between title and world-select screens.
  </action>
  <verify>
    <automated>grep -c "assets/textures/icons/title_bg.png" /Users/jnuyens/src/Cubicraftia/src/ui/world_select_screen.gd</automated>
  </verify>
  <done>world_select_screen.gd line ~153 references "res://assets/textures/icons/title_bg.png". No reference to "title_bg_bricks" remains in the file. grep returns >= 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| res:// texture load | ResourceLoader.exists() guard preserved on all paths; graceful no-texture fallback unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-T | Tampering | ftue_overlay.gd storyboard load | accept | Paths are compile-time const strings in res://; no user-controlled input |
| T-07-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages — GDScript edits only |
</threat_model>

<verification>
After all three tasks:

1. Run audit test:
   godot --headless -s addons/gut/gut_cmdln.gd -- -gdir=tests/unit -gtest=test_asset_stub_audit.gd -gexit

   Expected: hp_bar.gd heart paths no longer appear in failures (health_heart_full/half/empty.png are all >200 B). ftue_overlay.gd arrow path no longer appears in failures.

2. Run full GUT suite:
   godot --headless -s addons/gut/gut_cmdln.gd -- -gdir=tests/unit -gexit

   Expected: No new test failures introduced. Existing tests for ftue, hp_bar, avatar all still pass.

3. grep gates:
   grep -c "health_heart_full" src/ui/hp_bar.gd  (>= 1)
   grep -c "assets/textures/icons/ftue_arrow" src/ui/ftue_overlay.gd  (>= 2)
   grep -c "assets/textures/icons/title_bg.png" src/ui/world_select_screen.gd  (>= 1)
</verification>

<success_criteria>
- hp_bar.gd ICON_FULL/HALF/EMPTY point to health_heart_*.png (real art, ~60 KB each)
- ftue_overlay.gd arrow path corrected to res://assets/textures/icons/ftue_arrow.png
- ftue_overlay.gd has a StoryboardPanel TextureRect added to _narration_container
- ftue_overlay.gd _activate_step loads the correct panel for each of the 4 steps
- world_select_screen.gd background path points to res://assets/textures/icons/title_bg.png
- Audit test passes for ASSET-04 and ASSET-05 items (no path-gap failures remain for these files)
- Zero occurrences of "skip" (case-insensitive) in ftue_overlay.gd
</success_criteria>

<output>
Create .planning/phases/07-asset-integration/07-02-SUMMARY.md when done
</output>
