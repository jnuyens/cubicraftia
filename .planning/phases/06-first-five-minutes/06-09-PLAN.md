---
phase: 06-first-five-minutes
plan: 09
type: execute
wave: 4
depends_on: [06-02]
files_modified:
  - src/autoload/onboarding_telemetry.gd
  - tests/unit/test_telemetry_rotation.gd
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "OnboardingTelemetry._flush_to_disk() correctly writes queued events to user://telemetry.cfg"
    - "10,000-event cap: when event_count exceeds 10,000, _rotate_oldest() drops oldest events"
    - "Flushed events have only ts (int) and event (String) keys — no other fields"
    - "60-second Timer triggers _flush_to_disk(); NOTIFICATION_WM_CLOSE_REQUEST also triggers flush"
    - "GUT test_telemetry_rotation.gd: all stubs pass real assertions (not PENDING)"
    - "GUT test_deeplink_parser.gd: all stubs pass real assertions (not PENDING)"
  artifacts:
    - path: "src/autoload/onboarding_telemetry.gd"
      provides: "Full implementation: flush, cap rotation, CLOSE_REQUEST notification"
    - path: "tests/unit/test_telemetry_rotation.gd"
      provides: "Passing GUT tests: append, cap rotation, no-PII verification"
  key_links:
    - from: "onboarding_telemetry.gd"
      to: "user://telemetry.cfg"
      via: "_flush_to_disk() → ConfigFile.save()"
      pattern: "telemetry\\.cfg"
---

<objective>
Onboarding telemetry full implementation: flush logic, 10k-event cap rotation, CLOSE_REQUEST hook, and activating the test stubs created in Plan 06-01.

Purpose: The skeleton was created in Plan 06-02; this plan completes the implementation and activates the unit tests. Separated from Plan 06-02 to keep each plan within context budget and to allow Plan 06-02 to focus on the autoload skeleton that other plans (06-03 through 06-08) depend on.

Output: onboarding_telemetry.gd flushing/rotation logic completed and tested; test_telemetry_rotation.gd promoted from PENDING stubs to real assertions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-first-five-minutes/06-CONTEXT.md
@.planning/phases/06-first-five-minutes/06-02-SUMMARY.md
</context>

<interfaces>
<!-- OnboardingTelemetry skeleton from Plan 06-02 -->

From src/autoload/onboarding_telemetry.gd (created in Plan 06-02):
```gdscript
extends Node
const MAX_EVENTS: int = 10_000
const FLUSH_INTERVAL_SEC: float = 60.0
var _queue: Array[Dictionary] = []

func log(event_name: String) -> void  # appends to _queue
func _flush_to_disk() -> void         # writes _queue to user://telemetry.cfg
func _rotate_oldest(cfg: ConfigFile, drop_count: int) -> void  # drops oldest N events
```

ConfigFile telemetry schema (locked from 06-CONTEXT.md Area 6):
```ini
[meta]
event_count = N

[event_0]
ts = 1234567890
event = "title_shown"

[event_1]
ts = 1234567891
event = "signup_started"
```

GDSCRIPT ConfigFile section API:
```gdscript
cfg.get_sections() -> PackedStringArray  # returns all section names
cfg.erase_section(section: String) -> void  # removes a section
cfg.get_value(section, key, default)
cfg.set_value(section, key, value)
cfg.save(path) -> Error
cfg.load(path) -> Error
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete OnboardingTelemetry flush + cap + activate telemetry + deeplink parser tests</name>
  <files>src/autoload/onboarding_telemetry.gd, tests/unit/test_telemetry_rotation.gd, tests/unit/test_deeplink_parser.gd</files>
  <behavior>
    OnboardingTelemetry:
    - _flush_to_disk() with 5 queued events: writes [event_0] through [event_4] sections; clears _queue
    - _flush_to_disk() called twice: second call appends after first batch (event_count accumulates correctly)
    - With 10,001 total events: after flush, [meta] event_count == 10,000; oldest 1 event removed
    - With 10,500 total events: 500 oldest removed; total == 10,000
    - _rotate_oldest() removes sections named "event_0" through "event_{drop_count-1}" (oldest first by section name sort)
    - log("title_shown") followed by flush: cfg contains [event_N] with event = "title_shown"
    - No field other than "ts" and "event" appears in any [event_N] section
    - NOTIFICATION_WM_CLOSE_REQUEST triggers _flush_to_disk() (tested via direct _notification() call)
    DeepLinkHandler (activating stubs from Plan 06-01):
    - DeepLinkHandler parses "--invite=VALIDTOKEN00000000000" → get_pending_token() == "VALIDTOKEN00000000000"
    - "--invite=SHORT" (< 20 chars) → get_pending_token() == "" (rejected)
    - "--uri=cubicraftia://invite/VALIDTOKEN00000000000" → get_pending_token() == "VALIDTOKEN00000000000"
    - "--other=arg" with no invite → get_pending_token() == ""
    - Multiple args including one valid --invite= → only the invite token is captured
    - invite_token_received fires at most once even if _ready() is called with two valid --invite= args (defensive)
  </behavior>
  <action>
    Read `src/autoload/onboarding_telemetry.gd` (created in Plan 06-02 skeleton). Complete the implementation if any methods are stubs:

    Complete `func _flush_to_disk() -> void:`:
    - If _queue is empty: return (no-op)
    - `var cfg := ConfigFile.new()`
    - `cfg.load("user://telemetry.cfg")` — OK if file missing (starts fresh)
    - `var count: int = cfg.get_value("meta", "event_count", 0)`
    - For each entry in _queue: `cfg.set_value("event_%d" % count, "ts", entry["ts"])` + `cfg.set_value("event_%d" % count, "event", entry["event"])`; count += 1
    - If count > MAX_EVENTS: `_rotate_oldest(cfg, count - MAX_EVENTS); count = MAX_EVENTS`
    - `cfg.set_value("meta", "event_count", count)`
    - `cfg.save("user://telemetry.cfg")`
    - `_queue.clear()`

    Complete `func _rotate_oldest(cfg: ConfigFile, drop_count: int) -> void:`:
    - Get all section names: `var sections := cfg.get_sections()`
    - Filter to only event sections (starts with "event_"): `var event_sections := sections.filter(func(s): return s.begins_with("event_"))`
    - Sort numerically: `event_sections.sort_custom(func(a, b): return int(a.substr(6)) < int(b.substr(6)))`
    - Remove first `drop_count` sections: `for i in range(min(drop_count, event_sections.size())): cfg.erase_section(event_sections[i])`

    Activate `tests/unit/test_telemetry_rotation.gd` (created as a PENDING stub in Plan 06-01):
    - Remove pending() calls; replace with real test methods
    - Use conftest_phase6.gd's make_telemetry_cfg_path() for isolated test file paths
    - Test: flush 5 events, load cfg, verify event_count == 5, verify [event_0].event value
    - Test: flush 10,001 events (simulate by calling log() 10,001 times then flush), verify count == 10,000
    - Test: verify no "user_id", "email", "position" keys in any event section (PII check)
    - Test: _notification(NOTIFICATION_WM_CLOSE_REQUEST) triggers _flush_to_disk() (mock by calling _notification() directly)

    Activate `tests/unit/test_deeplink_parser.gd` (created as PENDING stub in Plan 06-01):
    - Test: "--invite=AAAABBBBCCCCDDDD0000" → token parsed, get_pending_token() returns it
    - Test: "--invite=SHORT" → rejected, get_pending_token() == ""
    - Test: "--uri=cubicraftia://invite/AAAABBBBCCCCDDDD0000" → token parsed
    - Test: no relevant args → get_pending_token() == ""
    - Note: DeepLinkHandler._ready() reads OS.get_command_line_args() — for unit tests, mock by calling the internal parse method directly rather than relying on actual CLI args. If the parse logic is in _ready(), extract it to a testable `func _parse_args(args: Array[String]) -> String:` helper method (returns the token or ""). Tests call _parse_args() directly.

    If `_parse_args()` does not exist yet in DeepLinkHandler (Plan 06-02 may have inlined it in _ready()), refactor: extract the parsing logic to `func _parse_args(args: Array[String]) -> String:` and call it from _ready(). This makes the logic testable without manipulating CLI args.
  </action>
  <verify>
    <automated>godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_telemetry -gsuffix=.gd -gexit 2>&1 | grep -E "(PASS|FAIL|ERROR)" && godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_deeplink -gsuffix=.gd -gexit 2>&1 | grep -E "(PASS|FAIL|ERROR)"</automated>
  </verify>
  <done>test_telemetry_rotation.gd passes all assertions (no PENDING or FAIL); test_deeplink_parser.gd passes all assertions; _flush_to_disk() + _rotate_oldest() fully implemented; DeepLinkHandler._parse_args() testable independently of CLI args.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OnboardingTelemetry → user://telemetry.cfg | App-sandbox file; no external data; append-only by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-D1 | Denial-of-Service | telemetry.cfg fills disk | mitigate | 10,000-event cap with _rotate_oldest() enforced in _flush_to_disk(); each event ~40 bytes; 10k events ≈ 400 KB max |
| T-06-I1 | Information Disclosure | telemetry event contains PII | mitigate | Unit test verifies no PII keys (no user_id, email, position fields) in any event section |
| T-06-SC | Tampering | No new package installs | accept | No new packages |
</threat_model>

<verification>
1. GUT test_telemetry_rotation runs with PASS (no PENDING or FAIL)
2. GUT test_deeplink_parser runs with PASS
3. `grep "_parse_args" src/autoload/deep_link_handler.gd` — testable helper method
4. `grep "_rotate_oldest" src/autoload/onboarding_telemetry.gd` — rotation method implemented
5. `grep "NOTIFICATION_WM_CLOSE_REQUEST" src/autoload/onboarding_telemetry.gd` — flush hook present
</verification>

<success_criteria>
- OnboardingTelemetry flushes events to user://telemetry.cfg with correct [event_N] schema
- 10,000-event cap drops oldest events when exceeded
- 60-second Timer and CLOSE_REQUEST both trigger flush
- No PII keys in any telemetry event (verified by unit test)
- DeepLinkHandler._parse_args() is testable; both token formats parse correctly
- All telemetry and deeplink tests PASS (not PENDING, not FAIL)
</success_criteria>

<output>
Create `.planning/phases/06-first-five-minutes/06-09-SUMMARY.md` when done
</output>
