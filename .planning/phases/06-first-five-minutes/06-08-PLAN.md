---
phase: 06-first-five-minutes
plan: 08
type: execute
wave: 4
depends_on: [06-03, 06-06]
files_modified:
  - src/ui/title_scene.gd
  - src/ui/sign_in_panel.gd
  - src/autoload/onboarding_telemetry.gd
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Deep-link invite flow: signed-in user → redeem_invite → join_session → pre-set ftue_complete → toast tip"
    - "Deep-link unsigned user → inline sign-in prompt (Surface 6 compact form) → then redeem_invite flow"
    - "user://ftue.cfg written with joined_via_invite=true after successful invite join"
    - "Invite-link joiner's ftue_complete is pre-set to true (ftue overlay never appears for them)"
    - "Toast tip 'This is [Friend]'s world. They have what you need to get started.' shown after world load"
    - "sign_in_panel.set_abbreviated_mode(true) shows only email+password+DOB fields (no tab bar)"
    - "All 15 OnboardingTelemetry event names log at the correct funnel points"
    - "user://ftue.cfg joined_via_invite=true causes title_scene to skip to world_select on return visits"
  artifacts:
    - path: "src/ui/title_scene.gd"
      provides: "_handle_invite_deep_link flow: sign-in check, redeem, join, pre-set ftue_complete, toast"
    - path: "src/ui/sign_in_panel.gd"
      provides: "set_abbreviated_mode(bool) method; abbreviated mode shows only email+password+DOB"
    - path: "src/autoload/onboarding_telemetry.gd"
      provides: "All 15 event log points wired: title_shown, deep_link_received, signup_started, etc."
  key_links:
    - from: "title_scene.gd"
      to: "friends_client.gd"
      via: "FriendsClient.redeem_invite(token) → friendship_created signal → NetworkManager.join_session()"
      pattern: "redeem_invite"
    - from: "title_scene.gd"
      to: "world_save.gd"
      via: "pre-set ftue_complete via WorldSave.set_world_meta() before main_scene loads"
      pattern: "ftue_complete"
    - from: "title_scene.gd"
      to: "toasts.gd"
      via: "Toasts.show('ui.deeplink.joiner_tip', 8) after world_ready"
      pattern: "joiner_tip"
---

<objective>
Deep-link joiner skip flow (Surface 6) + full onboarding telemetry wiring: the complete invite-link join path, abbreviated sign-in, and all 15 telemetry event log points across all Phase 6 scenes.

Purpose: The invite-link flow is one of Phase 6's three primary user journeys (alongside new-player FTUE and returning-player world-select). Getting it right means an invited friend appears next to the host without friction. The telemetry wiring closes the loop on all funnel measurement.

Output: title_scene.gd _handle_invite_deep_link() complete implementation; sign_in_panel.gd set_abbreviated_mode(); user://ftue.cfg marker write; 15 telemetry event names logged at correct call sites across all Phase 6 scenes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-first-five-minutes/06-CONTEXT.md
@.planning/phases/06-first-five-minutes/06-UI-SPEC.md
@.planning/phases/06-first-five-minutes/06-03-SUMMARY.md
@.planning/phases/06-first-five-minutes/06-06-SUMMARY.md
</context>

<interfaces>
<!-- Deep-link flow interfaces from existing code -->

From src/autoload/friends_client.gd (Phase 4 — existing):
```gdscript
func redeem_invite(token: String) -> void
# Emits: signal friendship_created(host_uid: String) on success
# Emits: signal invite_error(reason: String) on failure
func get_user_id() -> String
func is_signed_in() -> bool
```

From src/autoload/network_manager.gd (Phase 4 — existing):
```gdscript
func join_session(host_uid: String) -> void
signal peer_joined(peer_id: int)  # fires when we (self) successfully join
```

From src/autoload/toasts.gd (Phase 1 — existing):
```gdscript
func show(key: String, duration: float = 3.0) -> void
# Duration 8.0s for the invite joiner tip per CONTEXT.md Area 5
```

From src/world/main_scene.gd (Phase 4):
```gdscript
signal world_ready  # fires after first chunks stream; title_scene connects to this after join
```

Deep-link flow steps (from 06-CONTEXT.md Area 5):
```
1. DeepLinkHandler.invite_token_received(token) → title_scene._handle_invite_deep_link(token)
2. If signed in → FriendsClient.redeem_invite(token)
3. If not signed in → show inline sign-in prompt (Surface 6) → on sign_in_complete → redeem_invite(token)
4. friendship_created(host_uid) → NetworkManager.join_session(host_uid)
5. Pre-set ftue_complete = true (write to pending world meta BEFORE main_scene loads)
6. peer_joined fires → write user://ftue.cfg joined_via_invite=true
7. After world_ready: Toasts.show("ui.deeplink.joiner_tip", 8.0) with host username
```

15 telemetry events (locked list from 06-CONTEXT.md Area 6):
```
title_shown — title_scene._ready()                [already in 06-03]
deep_link_received — DeepLinkHandler fires         [add to _handle_invite_deep_link]
signup_started — user taps "Create account"        [add to title_scene button handler]
signup_complete — FriendsClient.sign_up_ok fires   [add to sign_up_ok handler]
signin_complete — FriendsClient.signed_in fires    [add to signed_in handler]
avatar_picker_shown — avatar_creator._ready()      [add to 06-04 scene]
avatar_complete — avatar_creator Done pressed      [already in 06-04]
world_select_shown — world_select._ready()         [already in 06-05]
world_created — WorldSave.create_world() OK        [already in 06-05]
world_loaded — main_scene.world_ready signal       [add to main_scene handler]
ftue_step_1_complete — chest opened                [already in 06-06]
ftue_step_2_complete — wood_log picked up          [already in 06-06]
ftue_step_3_complete — wood_plank placed           [already in 06-06]
ftue_complete — "That's it" shown                  [already in 06-06]
invite_joined — NetworkManager.peer_joined fires   [add to deep-link join handler]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete _handle_invite_deep_link() in title_scene.gd + sign_in_panel abbreviated mode + ftue.cfg marker</name>
  <files>src/ui/title_scene.gd, src/ui/sign_in_panel.gd</files>
  <behavior>
    - _handle_invite_deep_link(token) hides title UI, shows "Joining your friend's world..." toast immediately
    - If signed in: proceeds directly to redeem_invite(token)
    - If not signed in: shows compact inline sign-in prompt (email+password+DOB only via abbreviated mode)
    - After successful sign-in in abbreviated mode: resumes redeem_invite(token)
    - friendship_created(host_uid) → NetworkManager.join_session(host_uid)
    - After join_session initiated: WorldSave pending ftue_complete set (the mechanism: write to user://ftue.cfg a "pending_ftue_complete = true" marker that main_scene reads and applies before FTUE would trigger)
    - After world_ready: Toasts.show("ui.deeplink.joiner_tip", 8.0) with host username
    - user://ftue.cfg written with joined_via_invite=true after peer_joined fires
    - On subsequent launch: if user://ftue.cfg joined_via_invite=true AND signed in → skip directly to world_select
    - sign_in_panel.set_abbreviated_mode(true): hides tab bar, shows only email+password+DOB fields
    - "Create account" in abbreviated mode → abbreviated sign-up → write user://pending_avatar_creation.cfg → resume invite flow
  </behavior>
  <action>
    Read `src/ui/title_scene.gd` (created in Plan 06-03) to understand its current structure.

    Complete `func _handle_invite_deep_link(token: String) -> void:`:
    1. OnboardingTelemetry.log("deep_link_received")
    2. `_content_column.visible = false` — hide title UI
    3. Toasts.show("ui.deeplink.connecting", 2.0) — brief "Connecting..." feedback (add this key to locale/en.po)
    4. `_pending_invite_token = token` — store for use after sign-in if needed
    5. If FriendsClient.is_signed_in(): call `_redeem_invite_and_join(token)`
    6. Else: show inline sign-in prompt — instantiate sign_in_panel.tscn, call `set_abbreviated_mode(true)`, connect `sign_in_complete` to `_on_inline_sign_in_complete.bind(token)`, connect `back_to_title_requested` to `_on_escape_from_deep_link`, add as child

    `func _redeem_invite_and_join(token: String) -> void:`:
    1. FriendsClient.friendship_created.connect(_on_friendship_created.bind(token), CONNECT_ONE_SHOT)
    2. FriendsClient.invite_error.connect(_on_invite_error, CONNECT_ONE_SHOT)
    3. FriendsClient.redeem_invite(token)

    `func _on_friendship_created(host_uid: String, token: String) -> void:`:
    1. NetworkManager.join_session(host_uid)
    2. `_write_pending_ftue_complete()` — writes user://ftue.cfg `[state] pending_ftue_complete = true` so main_scene pre-sets ftue_complete before FTUE overlay checks
    3. NetworkManager.peer_joined.connect(_on_self_joined.bind(host_uid), CONNECT_ONE_SHOT)

    `func _on_self_joined(peer_id: int, host_uid: String) -> void:`:
    1. OnboardingTelemetry.log("invite_joined")
    2. `_write_joined_via_invite_marker()` — ConfigFile user://ftue.cfg, [state] joined_via_invite = true
    3. Get host username from FriendsClient or NetworkManager session data (read Phase 4 session state API to find how to get the host's username by uid)
    4. After world_ready signal fires: Toasts.show("ui.deeplink.joiner_tip".format({"friend": host_username}), 8.0)

    `func _on_escape_from_deep_link() -> void:`: show title UI again (_content_column.visible = true), remove inline sign-in panel

    `func _check_ftue_marker_on_ready() -> void:` — called in _ready() after connection checks:
    - Load user://ftue.cfg; if [state] joined_via_invite = true AND FriendsClient.is_signed_in(): skip title UI, go directly to world_select_screen

    In main_scene.gd: read user://ftue.cfg `[state] pending_ftue_complete` at world load time; if true: call WorldSave.set_world_meta("ftue_complete", var_to_bytes(true)) immediately (before FTUE overlay check); then erase the pending flag from ftue.cfg.

    Modify `src/ui/sign_in_panel.gd`: Add `func set_abbreviated_mode(enabled: bool) -> void:`:
    - If enabled: hide the tab bar (`_tab_bar.visible = false` or equivalent); show only email field, password field, and DOB picker. Hide any "Forgot password" and extra links. Keep the "Create account" link button visible (it triggers abbreviated sign-up).
    - The existing Phase 5 DOB picker and parental_gate_panel flow remain unchanged.
    - Add this method after the existing `show_back_button` property addition from Plan 06-03.
  </action>
  <verify>
    <automated>grep -n "_handle_invite_deep_link\|_redeem_invite_and_join\|_on_friendship_created" src/ui/title_scene.gd && grep -n "set_abbreviated_mode\|abbreviated_mode" src/ui/sign_in_panel.gd && grep -n "ftue\.cfg\|joined_via_invite" src/ui/title_scene.gd</automated>
  </verify>
  <done>_handle_invite_deep_link() fully implemented with sign-in check, redeem, join, ftue_complete pre-set, and toast; sign_in_panel has set_abbreviated_mode(); user://ftue.cfg marker written after join; second-launch shortcut to world_select works for invite joiners.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 15 telemetry event log points across Phase 6 scenes</name>
  <files>src/ui/title_scene.gd, src/ui/avatar_creator.gd, src/ui/world_select_screen.gd, src/world/main_scene.gd, src/autoload/onboarding_telemetry.gd</files>
  <action>
    Read each of the 5 files listed. For each, add missing OnboardingTelemetry.log() calls. Cross-check against the 15-event list in the interfaces section above. Plans 06-03 through 06-06 added some events already; this task adds the remaining gaps and verifies the full set.

    Events to add (verify which are missing by reading each file):

    In `src/ui/title_scene.gd`:
    - "title_shown" — already in _ready() per Plan 06-03 [verify present, add if missing]
    - "deep_link_received" — in _handle_invite_deep_link() [just added in Task 1, verify]
    - "signup_started" — in _on_create_account_pressed() handler
    - "signup_complete" — connect to FriendsClient.sign_up_ok signal in _ready(); on signal: log("signup_complete")
    - "signin_complete" — connect to FriendsClient.signed_in signal in _ready(); on signal: log("signin_complete")

    In `src/ui/avatar_creator.gd`:
    - "avatar_picker_shown" — in _ready() [add if not present from Plan 06-04]
    - "avatar_complete" — in _on_done_pressed() [verify present from Plan 06-04]

    In `src/ui/world_select_screen.gd`:
    - "world_select_shown" — in _ready() [verify present from Plan 06-05]
    - "world_created" — after WorldSave.create_world() returns OK [verify present from Plan 06-05]

    In `src/world/main_scene.gd`:
    - "world_loaded" — connect to world_ready signal in main_scene._ready(); on world_ready: OnboardingTelemetry.log("world_loaded"). Read main_scene.gd to find where world_ready is used and add the telemetry call in the same handler.

    FTUE events (ftue_step_1_complete, ftue_step_2_complete, ftue_step_3_complete, ftue_complete) — already in ftue_overlay.gd from Plan 06-06. Verify they match the exact locked event name strings.

    "invite_joined" — already in _on_self_joined() from Task 1 above.

    After verifying and adding all missing calls, run the grep verification to confirm all 15 are present.

    Also add one key missing from locale/en.po: `ui.deeplink.connecting` (brief "Connecting..." message used in _handle_invite_deep_link — add as a stub msgstr "").
  </action>
  <verify>
    <automated>grep -rn "OnboardingTelemetry.log" src/ui/ src/world/main_scene.gd | grep -oP '"[^"]+"' | sort | uniq -c | sort -rn | head -20</automated>
  </verify>
  <done>All 15 telemetry event names from the locked list in 06-CONTEXT.md Area 6 are present as OnboardingTelemetry.log() calls in the correct files at the correct funnel points. No duplicate logs for the same event on a single user path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Invite token → FriendsClient.redeem_invite() → Go signaling | Server enforces single-use and expiry; client only does basic sanity (20-char check) |
| user://ftue.cfg → main_scene.gd | Local file; read by main_scene before FTUE overlay check; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-T1 | Tampering | Invite token spoofed to bypass authentication | mitigate | Server-side: Go signaling validates token exists, is unused, not expired; Supabase RLS enforces per-row friendship insertion |
| T-06-I2 | Information Disclosure | user://ftue.cfg contains join metadata | accept | Only boolean flags (joined_via_invite, pending_ftue_complete); no PII; app-sandboxed |
| T-06-T2 | Tampering | Malformed invite join sets ftue_complete on arbitrary world | mitigate | pending_ftue_complete is only applied when main_scene verifies peer_joined has fired (world belongs to the host who accepted the invite) |
| T-06-SC | Tampering | No new package installs | accept | No new packages |
</threat_model>

<verification>
1. `grep "_handle_invite_deep_link" src/ui/title_scene.gd` — method fully implemented
2. `grep "redeem_invite\|join_session" src/ui/title_scene.gd` — both calls present
3. `grep "joined_via_invite\|pending_ftue_complete" src/ui/title_scene.gd` — both markers written
4. `grep "set_abbreviated_mode" src/ui/sign_in_panel.gd` — method exists
5. `grep -c "OnboardingTelemetry.log" src/ui/title_scene.gd` — minimum 5 log calls in title_scene
6. `grep "world_loaded\|OnboardingTelemetry" src/world/main_scene.gd` — world_loaded event wired
7. `grep "signup_started\|signup_complete\|signin_complete" src/ui/title_scene.gd` — all 3 present
</verification>

<success_criteria>
- Signed-in user with invite link → redeem_invite → join_session → world loads → toast shown
- Unsigned user with invite link → compact sign-in → sign-in complete → redeem_invite → join
- user://ftue.cfg written after join; second launch skips title for invite joiners
- Invite joiner never sees FTUE overlay (ftue_complete pre-set)
- All 15 telemetry events fire at correct funnel points across all Phase 6 scenes
</success_criteria>

<output>
Create `.planning/phases/06-first-five-minutes/06-08-SUMMARY.md` when done
</output>
