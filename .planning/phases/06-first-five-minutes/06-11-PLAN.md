---
phase: 06-first-five-minutes
plan: 11
type: execute
wave: 5
depends_on: [06-06, 06-07, 06-08, 06-09, 06-10]
files_modified:
  - tests/unit/test_avatar_config_schema.gd
  - tests/unit/test_avatar_preset_validity.gd
  - tests/unit/test_ftue_state_machine.gd
  - tests/unit/test_world_thumbnail_capture.gd
  - tests/integration/test_first_time_user_e2e.gd
  - .planning/DOCS.md
  - docs/PRIVACY.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "All 7 Phase 6 GUT test files PASS (no PENDING, no FAIL)"
    - "DOCS.md §1.1 through §1.4 accurately describes the shipped implementation"
    - "docs/PRIVACY.md enumeration of local telemetry event log is present"
    - "Full GUT suite (unit + integration) is green"
    - "Phase 6 DOCS.md sync confirms DOC-01 requirement met"
  artifacts:
    - path: "tests/unit/test_avatar_config_schema.gd"
      provides: "Passing tests: avatar cfg round-trip, required keys, valid ranges"
    - path: "tests/unit/test_ftue_state_machine.gd"
      provides: "Passing tests: FTUE step 1/2/3/4 advancement + completion persistence"
    - path: "tests/integration/test_first_time_user_e2e.gd"
      provides: "Integration test: world create → FTUE complete flow"
    - path: ".planning/DOCS.md"
      provides: "§1 First five minutes is a true description of the shipped implementation"
  key_links:
    - from: "DOCS.md §1"
      to: "title_scene.gd + avatar_creator.gd + world_select_screen.gd + ftue_overlay.gd"
      via: "DDD discipline: DOCS.md must be a true description of shipped code"
      pattern: "§1"
---

<objective>
Activate all Phase 6 test stubs to real assertions + DOCS.md §1 sync (DDD discipline gate).

Purpose: This is the phase-completion gate. All 7 GUT test files move from PENDING stubs to real passing assertions. DOCS.md §1 is updated to be a true description of the shipped implementation, satisfying the DOC-01 DDD requirement. The privacy policy gets the telemetry log enumeration.

Output: All Phase 6 tests passing; DOCS.md §1 reflects reality; docs/PRIVACY.md updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-first-five-minutes/06-CONTEXT.md
@.planning/phases/06-first-five-minutes/06-06-SUMMARY.md
@.planning/phases/06-first-five-minutes/06-07-SUMMARY.md
@.planning/phases/06-first-five-minutes/06-08-SUMMARY.md
@.planning/phases/06-first-five-minutes/06-09-SUMMARY.md
@.planning/phases/06-first-five-minutes/06-10-SUMMARY.md
</context>

<interfaces>
<!-- Test framework: GUT (Godot Unit Test) already installed -->

GUT quick run commands:
```bash
# Unit tests only:
godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_ -gsuffix=.gd -gexit

# Full suite (unit + integration):
godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests -gprefix=test_ -gsuffix=.gd -ginclude_subdirs -gexit
```

Tests to activate (each currently has pending() stubs from Plan 06-01):
- test_avatar_config_schema.gd — avatar cfg read/write, key presence, range validation
- test_avatar_preset_validity.gd — 8 presets distinct, all 8 keys present, valid values
- test_ftue_state_machine.gd — step 1/2/3 signal gating, step 4 completion, persistence
- test_world_thumbnail_capture.gd — capture creates file at expected path (may remain partially pending if requires viewport)
- test_first_time_user_e2e.gd (integration) — world create → FTUE steps → ftue_complete written

Test files already activated in Plan 06-09:
- test_telemetry_rotation.gd — PASS after Plan 06-09
- test_deeplink_parser.gd — PASS after Plan 06-09

DOCS.md §1 ("First five minutes") anchors to DOC-01. The DDD contract:
"A phase is complete when DOCS.md's anchored section(s) are a true description of the shipped implementation."
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate 4 remaining test stubs to real assertions</name>
  <files>
    tests/unit/test_avatar_config_schema.gd,
    tests/unit/test_avatar_preset_validity.gd,
    tests/unit/test_ftue_state_machine.gd,
    tests/unit/test_world_thumbnail_capture.gd,
    tests/integration/test_first_time_user_e2e.gd
  </files>
  <behavior>
    test_avatar_config_schema.gd:
    - Avatar cfg round-trip: write [avatar] section, load back, all 8 keys present
    - Skin colour index: 0 ≤ idx ≤ 4; 5 raises an assertion in test (or is clamped by builder)
    - Head shape: "square", "round", "tall" — valid; "invalid" is not in HEAD_SHAPES array
    - Body colour index: 0 ≤ idx ≤ 9
    - Required keys: ["skin_colour_index","head_shape","face_expression","body_colour_index","body_accessory","leg_colour_index","leg_shoes","hand_accessory"]
    test_avatar_preset_validity.gd:
    - All 8 presets in avatar_creator.PRESETS have all 8 required keys
    - No two presets are identical (8 distinct configs)
    - Skin colour coverage: at least 3 distinct skin_colour_index values across 8 presets
    test_ftue_state_machine.gd (unit-level test using GUT doubles/mocks):
    - Create a mock ChestEntity that emits opened; verify FTUE advances from step 1 to 2
    - Create a mock Inventory that emits item_added("builder_1", "wood_log", 1); verify FTUE advances from 2 to 3
    - Create a mock StudGrid that emits placed with definition.brick_id == "wood_plank"; verify advance from 3 to 4
    - Non-matching items (def_id == "stone") do NOT advance step 2
    - Non-matching bricks (brick_id == "stone_brick") do NOT advance step 3
    - WorldSave.get_world_meta("ftue_complete") returns a truthy value after step 4 completes
    test_world_thumbnail_capture.gd:
    - Mark as pending("Requires rendering viewport — manual verification; WorldSave.capture_thumbnail() method exists") if a real viewport is unavailable in headless mode
    - If headless rendering is available: assert file exists at expected path after capture_thumbnail() called with a valid world_id
    test_first_time_user_e2e.gd (integration):
    - Create a survival world (WorldSave.create_world("Test E2E", 42, "survival"))
    - Verify ftue_complete is null before FTUE runs
    - Simulate FTUE completion by calling WorldSave.set_world_meta("ftue_complete", var_to_bytes(true))
    - Verify WorldSave.get_world_meta("ftue_complete") is now truthy
    - Verify OnboardingTelemetry queue contains "ftue_complete" event after step 4 sequence
    - Clean up: delete test world after test
  </behavior>
  <action>
    For each test file, read the current PENDING stub content. Replace pending() calls with real test assertions.

    For `test_ftue_state_machine.gd`: The FTUE step state machine in ftue_overlay.gd connects to game signals. To unit-test this without a full game scene, use GUT's doubling system or test the step-advancement logic in isolation:
    - Extract the step-advancement logic to a testable helper class (FTUEStateMachine inner class in ftue_overlay.gd), OR
    - Test the step conditions directly using the signal filter logic: instantiate a minimal FTUE state tracker that advances based on the same conditions, without requiring a full viewport

    If GUT's scene-loading approach is available (load ftue_overlay.tscn in tests), use it — but ensure the test doesn't hang waiting for game signals that never fire in headless mode. Use `double()` or `simulate()` for ChestEntity, Inventory, and StudGrid.

    For `test_first_time_user_e2e.gd`: This integration test works at the WorldSave / OnboardingTelemetry level — no full scene load needed. Test the data layer:
    - WorldSave interactions are filesystem operations (user:// directory)
    - OnboardingTelemetry queue can be tested by calling OnboardingTelemetry.log() and checking _queue
    - Use conftest_phase6.gd helpers for isolated test paths

    Run the full unit test suite after implementing each test file. Address any failures before proceeding to the DOCS sync.
  </action>
  <verify>
    <automated>godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_ -gsuffix=.gd -gexit 2>&1 | grep -E "(PASS|FAIL|ERROR|PENDING)" | sort | uniq -c</automated>
  </verify>
  <done>All test_avatar_config_schema, test_avatar_preset_validity, test_ftue_state_machine tests PASS. test_world_thumbnail_capture marked pending (viewport constraint). test_first_time_user_e2e integration data-layer assertions PASS. No test files report ERROR or unexpected FAIL.</done>
</task>

<task type="auto">
  <name>Task 2: DOCS.md §1 sync + docs/PRIVACY.md telemetry enumeration</name>
  <files>.planning/DOCS.md, docs/PRIVACY.md</files>
  <action>
    Read `.planning/DOCS.md` sections §1.1, §1.2, §1.3, and §1.4 (the "First five minutes" chapter). Compare each statement against what was actually shipped in Phase 6 plans 06-01 through 06-10. Update DOCS.md §1 so that every statement in those sub-sections is a true description of the implementation.

    Key updates to make in DOCS.md §1 (verify each against implementation):

    §1.1 — Title screen and account creation:
    - Confirm: title_scene.tscn replaces boot.gd as run/main_scene — update if DOCS §1.1 still says "boot.gd"
    - Confirm: avatar creator shows 8 presets + 5-part customisation — update if count differs
    - Confirm: email verification sent in background while play continues — verify this matches Phase 5 sign-up flow behaviour
    - Confirm: "Choose your world: New world | Join with code" — verify world_select_screen has these options (Join with code = the invite link flow)

    §1.2 — In-world FTUE:
    - Confirm: 4-step FTUE (not 3, not 5) — the implementation has 4 steps + optional night hint
    - Confirm: starter chest delivers 1 pickaxe + 1 shovel + 1 lantern + 8 wooden planks + 4 cooked food — this is Phase 3 behaviour; verify it is still accurate
    - Confirm: FTUE completion message is "That's it. The world is yours." — matches ftue_overlay.gd
    - Confirm: FTUE unskippable — no skip button exists

    §1.3 — Invite-link joiner:
    - Confirm: joiner appears next to host — Phase 4 spawning behaviour; verify still true
    - Confirm: toast tip text matches ui.deeplink.joiner_tip EN copy
    - Confirm: joiner's ftue_complete pre-set so no FTUE runs

    §1.4 — Constraints:
    - Confirm: EN + NL localisation — locale/nl.po now complete
    - Confirm: email + password + 13+ self-declaration only — no OAuth in Phase 6
    - Confirm: tutorial unskippable — verified by grep gate in Plan 06-06

    If any DOCS.md statement does not match reality (e.g., a feature was implemented differently), update DOCS.md to reflect what was actually built. This is the DDD discipline: DOCS.md must be a true description, not an aspirational spec.

    Also add an admonition block in DOCS.md §1 (or in a Phase 6 implementation notes appendix) noting:
    - Avatar preset artwork: 8 placeholder PNG stubs ship in Phase 6; final artwork is post-v1
    - Builder mesh: programmatic BoxMesh sub-parts ship in Phase 6; sculpted mesh is Phase 999.1
    - Title background: placeholder title_bg.png ships; artist replacement is post-v1
    - Title music: silence ships; title_theme.ogg added when available

    Read `docs/PRIVACY.md` (Phase 5 file). Add a section or paragraph enumerating the onboarding telemetry:
    ```
    ## Local Onboarding Telemetry

    Cubicraftia records an anonymous local event log at `user://telemetry.cfg` on your device.
    This log is used for game improvement analysis during playtesting. It is never transmitted to
    our servers or any third party. The log contains only:
    - A Unix timestamp (integer)
    - An event name from a fixed list (e.g., "title_shown", "ftue_complete")

    No personal information, usernames, world names, positions, or identifiers are recorded.
    The log is capped at 10,000 entries. You can delete this file at any time by clearing the
    app's local storage.
    ```

    Update ROADMAP.md Phase 6 plans list:
    - Change "Plans: TBD" to "Plans: 11 plans across 5 waves"
    - Add the plan list (06-01-PLAN.md through 06-11-PLAN.md with brief objectives)
  </action>
  <verify>
    <automated>grep -c "§1\." .planning/DOCS.md && grep -c "telemetry\|onboarding" docs/PRIVACY.md && grep "11 plans" .planning/ROADMAP.md</automated>
  </verify>
  <done>DOCS.md §1.1-§1.4 accurately describes shipped implementation with any deviations noted (placeholder art, boxmesh builders); docs/PRIVACY.md has telemetry enumeration; ROADMAP.md shows 11 plans with plan list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOCS.md → external readers | Public documentation; must be accurate to maintain trust |
| docs/PRIVACY.md → store compliance | Privacy policy accuracy is a legal obligation (Phase 5 work) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-D2 | Denial-of-Service | Test suite takes too long in CI | mitigate | Unit tests are headless; integration tests are data-layer only; no full scene loads except where unavoidable |
| T-06-SC | Tampering | No new package installs | accept | No new packages |
</threat_model>

<verification>
1. `godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_ -gsuffix=.gd -gexit` — all unit tests PASS or PENDING (no FAIL, no ERROR)
2. `godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests -gprefix=test_ -gsuffix=.gd -ginclude_subdirs -gexit` — full suite green
3. `grep "That's it. The world is yours" .planning/DOCS.md` — exact completion text in DOCS.md
4. `grep "telemetry\|event log" docs/PRIVACY.md` — telemetry enumeration present
5. `grep "06-11-PLAN.md\|11 plans" .planning/ROADMAP.md` — ROADMAP updated
6. `grep -ic "skip" src/ui/ftue_overlay.gd` — must return 0 (no skip affordance — final verification)
</verification>

<success_criteria>
- All Phase 6 GUT unit tests: PASS (test_world_thumbnail_capture may be PENDING if headless rendering unavailable — this is acceptable)
- Integration test data layer: PASS
- DOCS.md §1.1-§1.4 is a true description of what shipped
- docs/PRIVACY.md has local telemetry event log enumeration
- ROADMAP.md Phase 6 entry shows "11 plans across 5 waves" with plan list
- Full GUT suite is green (no regression from Phase 1-5 tests)
</success_criteria>

<output>
Create `.planning/phases/06-first-five-minutes/06-11-SUMMARY.md` when done
</output>
