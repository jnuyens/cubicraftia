---
phase: 06-first-five-minutes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_avatar_config_schema.gd
  - tests/unit/test_avatar_preset_validity.gd
  - tests/unit/test_ftue_state_machine.gd
  - tests/unit/test_deeplink_parser.gd
  - tests/unit/test_telemetry_rotation.gd
  - tests/unit/test_world_thumbnail_capture.gd
  - tests/integration/test_first_time_user_e2e.gd
  - tests/conftest_phase6.gd
  - src/world/chest_entity.gd
  - src/autoload/inventory.gd
  - locale/en.po
  - assets/textures/title/title_bg.png
  - assets/textures/ui/world_thumb_placeholder.png
  - assets/textures/icons/ftue_arrow.png
  - assets/textures/icons/icon_plus.png
  - assets/textures/icons/icon_arrow_down.png
  - assets/textures/icons/icon_dice.png
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "GUT test stubs for all 7 Phase 6 test files exist and run (failing) without syntax errors"
    - "ChestEntity emits an `opened` signal when interacted with"
    - "Inventory emits `item_added(builder_id, def_id, count)` when any item is added to a slot"
    - "All 44 new Phase 6 EN i18n keys exist in locale/en.po as empty stubs"
    - "Asset stub files exist at all required paths so scene loading does not crash"
  artifacts:
    - path: "tests/conftest_phase6.gd"
      provides: "Shared GUT fixtures for Phase 6 test files"
    - path: "tests/unit/test_ftue_state_machine.gd"
      provides: "Failing stub for FTUE step 1/2/3/4 logic"
    - path: "tests/unit/test_deeplink_parser.gd"
      provides: "Failing stub for DeepLinkHandler token parsing"
    - path: "tests/unit/test_telemetry_rotation.gd"
      provides: "Failing stub for OnboardingTelemetry 10k-cap rotation"
    - path: "src/world/chest_entity.gd"
      provides: "opened signal + emit in _open_panel()"
      contains: "signal opened"
    - path: "src/autoload/inventory.gd"
      provides: "item_added signal + emit in _apply_add()"
      contains: "signal item_added"
  key_links:
    - from: "ftue_overlay.gd"
      to: "chest_entity.gd"
      via: "chest.opened signal"
      pattern: "opened\\.connect"
    - from: "ftue_overlay.gd"
      to: "inventory.gd"
      via: "Inventory.item_added signal"
      pattern: "item_added\\.connect"
---

<objective>
Wave 0 scaffolding: signal gap fixes, test stubs, i18n key stubs, asset stubs.

Purpose: All subsequent Phase 6 plans depend on (a) the `ChestEntity.opened` and `Inventory.item_added` signals for FTUE wiring, (b) GUT test stubs that allow test-driven development from Wave 2 onward, and (c) i18n key stubs that prevent `tr()` warnings from polluting logs during development.

Output: 7 GUT test files (all failing), `conftest_phase6.gd`, 2 signal additions to existing files, 44 new EN locale key stubs, 4 asset stub PNG files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-first-five-minutes/06-CONTEXT.md
@.planning/phases/06-first-five-minutes/06-RESEARCH.md
@.planning/phases/06-first-five-minutes/06-UI-SPEC.md
</context>

<interfaces>
<!-- Verified signal signatures from codebase (06-RESEARCH.md Sources section) -->

From src/world/chest_entity.gd (verified lines 121-128 — signals present):
```gdscript
# Currently only emits: signal unlocked, signal broken
# Phase 6 addition required:
signal opened()
# Emit location: _on_interact() or _open_panel() BEFORE instantiating the chest panel
```

From src/autoload/inventory.gd (verified — ADD case in apply_event calls _apply_add()):
```gdscript
# Currently only emits: signal inventory_changed(builder_id: String)
# Phase 6 addition required:
signal item_added(builder_id: String, def_id: String, count: int)
# Emit location: inside _apply_add(builder_id, def_id, count) after slots are updated
```

From src/world/stud_grid.gd line 87 (VERIFIED existing signal — no addition needed):
```gdscript
signal placed(anchor_cell: Vector3i, definition: BrickDefinition, colour_index: int, rotation: int)
# IMPORTANT: FTUE filters by definition.brick_id == "wood_plank", NOT a def_id String param
```

From tests/gut_config.cfg (test framework already installed):
```
# Run unit tests: godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_ -gsuffix=.gd -gexit
# Run full suite: godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests -gprefix=test_ -gsuffix=.gd -ginclude_subdirs -gexit
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ChestEntity.opened + Inventory.item_added signals</name>
  <files>src/world/chest_entity.gd, src/autoload/inventory.gd</files>
  <behavior>
    - chest_entity: chest.has_signal("opened") returns true
    - chest_entity: opening the chest (calling _open_panel or _on_interact) causes opened.emit() to fire before the panel is instantiated
    - inventory: Inventory.item_added fires with (builder_id, def_id, count) when _apply_add() succeeds
    - inventory: item_added does NOT fire for REMOVE or MOVE events
    - inventory: item_added fires before inventory_changed so listeners can act on the new state
  </behavior>
  <action>
    In `src/world/chest_entity.gd`: Add `signal opened()` in the signal block (after the existing `signal unlocked` and `signal broken` declarations — per RESEARCH verified lines 121-128). In the method that opens the chest panel (the `_open_panel()` or `_on_interact()` path that currently calls `slide_in.open_chest_mode(...)` or equivalent — read the file to confirm the exact method name), add `opened.emit()` immediately before the panel instantiation call. This is a 2-line addition.

    In `src/autoload/inventory.gd`: Add `signal item_added(builder_id: String, def_id: String, count: int)` in the signal block (after `inventory_changed`). In `_apply_add(builder_id, def_id, count)` (or equivalent ADD handler — confirm exact method name by reading), after the slot update logic but before emitting `inventory_changed`, emit: `item_added.emit(builder_id, def_id, count)`. This is a 2-line addition.

    Do NOT modify any other logic in either file. These are surgical 2-line additions per CONTEXT.md specifics and RESEARCH.md Pitfall 3 and Pitfall 5.
  </action>
  <verify>
    <automated>grep -n "signal opened" src/world/chest_entity.gd && grep -n "signal item_added" src/autoload/inventory.gd && grep -n "opened\.emit\(\)" src/world/chest_entity.gd && grep -n "item_added\.emit" src/autoload/inventory.gd</automated>
  </verify>
  <done>Both signals declared and emitted; grep confirms all 4 patterns present; no other logic changed in either file.</done>
</task>

<task type="auto">
  <name>Task 2: GUT test stubs (7 files) + conftest_phase6.gd + 44 EN i18n key stubs</name>
  <files>
    tests/conftest_phase6.gd,
    tests/unit/test_avatar_config_schema.gd,
    tests/unit/test_avatar_preset_validity.gd,
    tests/unit/test_ftue_state_machine.gd,
    tests/unit/test_deeplink_parser.gd,
    tests/unit/test_telemetry_rotation.gd,
    tests/unit/test_world_thumbnail_capture.gd,
    tests/integration/test_first_time_user_e2e.gd,
    locale/en.po
  </files>
  <action>
    Create `tests/conftest_phase6.gd` extending GutTest with these fixtures (static helpers, no @onready needed since GUT loads these as helpers):
    - `static func make_avatar_cfg() -> Dictionary` — returns a valid default avatar config dict with keys: skin_colour_index(0), head_shape("square"), face_expression("neutral"), body_colour_index(0), body_accessory("none"), leg_colour_index(0), leg_shoes("none"), hand_accessory("none").
    - `static func make_telemetry_cfg_path() -> String` — returns "user://test_telemetry_%d.cfg" % randi() so tests don't clobber each other.
    - `static func make_world_index_path() -> String` — same pattern for world index.

    Create each test stub extending GutTest. Each test file must have at minimum one test method that calls `pending("STUB — implement in wave N")` so GUT reports them as pending (not failing with errors). Structure:

    `tests/unit/test_avatar_config_schema.gd`: Tests for avatar cfg round-trip through user://avatar.cfg (load/save via ConfigFile), required keys present, valid index ranges (skin 0-4, head_shape in ["square","round","tall"], etc.).

    `tests/unit/test_avatar_preset_validity.gd`: Tests that all 8 presets are distinct Dictionaries with all 5 required keys and valid values.

    `tests/unit/test_ftue_state_machine.gd`: Tests for FTUE step advancement — step 1 fires on chest opened signal, step 2 fires on item_added with def_id=="wood_log", step 3 fires on StudGrid.placed with definition.brick_id=="wood_plank", step 4 completion persists ftue_complete.

    `tests/unit/test_deeplink_parser.gd`: Tests for DeepLinkHandler token parsing from "--invite=TOKEN" and "--uri=cubicraftia://invite/TOKEN" args; token length < 20 chars is rejected.

    `tests/unit/test_telemetry_rotation.gd`: Tests that telemetry log appends events, that the 10,000-event cap drops oldest events on overflow, that no PII keys appear in logged events.

    `tests/unit/test_world_thumbnail_capture.gd`: Tests that WorldSave.capture_thumbnail creates a file at the expected path; stub — mark as pending since it requires a viewport.

    `tests/integration/test_first_time_user_e2e.gd`: Integration stub — pending("Full e2e flow: title → avatar → world create → FTUE steps 1-4 complete").

    For `locale/en.po`: Append all 44 new Phase 6 msgid entries as stubs (msgstr "") after the existing Phase 1-5 entries. The exact keys to add are the 44 keys listed in 06-UI-SPEC.md Localisation Contract section (Surface 1-8 consolidated key list). Each entry format:
    ```
    msgid "ui.title.tagline"
    msgstr ""
    ```
    Add all 44 new keys. Do not modify any existing msgid entries. Do not add NL translations yet (that is Plan 06-10).

    Create the following asset stub files (1×1 transparent PNG is sufficient — they prevent scene load crashes):
    - `assets/textures/title/title_bg.png` — 1×1 px placeholder (create the directory if it doesn't exist)
    - `assets/textures/ui/world_thumb_placeholder.png` — 1×1 px placeholder
    - `assets/textures/icons/ftue_arrow.png` — 1×1 px placeholder
    - `assets/textures/icons/icon_plus.png` — 1×1 px
    - `assets/textures/icons/icon_arrow_down.png` — 1×1 px
    - `assets/textures/icons/icon_dice.png` — 1×1 px

    For PNG stubs, use Godot's `Image.create(1, 1, false, Image.FORMAT_RGBA8)` + `image.save_png(path)` pattern in a one-shot GDScript — OR create them as minimal binary PNGs via Bash (the minimal valid PNG header for a 1×1 transparent pixel is well-known). Use whichever approach works in the headless environment. If neither works, create empty placeholder .import files with a comment noting they are stubs — but prefer actual minimal PNGs so Godot's importer doesn't error.
  </action>
  <verify>
    <automated>godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_avatar -gsuffix=.gd -gexit 2>&1 | grep -E "(PASS|PENDING|ERROR)" && grep -c "msgid \"ui\.title\." locale/en.po && ls assets/textures/title/title_bg.png assets/textures/ui/world_thumb_placeholder.png assets/textures/icons/ftue_arrow.png</automated>
  </verify>
  <done>All 7 test stub files exist and load without GDScript syntax errors; GUT reports them as PENDING (not ERROR); 44 new EN keys are in locale/en.po; all 6 asset stub files exist at their paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → inventory.gd | Test mocks emit item_added; production code must not double-emit |
| en.po msgid stubs → runtime tr() | Empty msgstr falls back to msgid string in engine; acceptable for stubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | chest_entity.gd signal addition | accept | 2-line addition; no auth or data path; cosmetic signal only |
| T-06-02 | Information Disclosure | item_added signal emits def_id | accept | def_id is a brick type identifier (public game data); no PII |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; GUT already present |
</threat_model>

<verification>
1. `grep -n "signal opened" src/world/chest_entity.gd` — must return a result
2. `grep -n "signal item_added" src/autoload/inventory.gd` — must return a result
3. `grep -n "opened\.emit" src/world/chest_entity.gd` — must return a result
4. `grep -n "item_added\.emit" src/autoload/inventory.gd` — must return a result
5. `ls tests/unit/test_avatar_config_schema.gd tests/unit/test_ftue_state_machine.gd tests/unit/test_deeplink_parser.gd tests/unit/test_telemetry_rotation.gd` — all must exist
6. `ls tests/integration/test_first_time_user_e2e.gd tests/conftest_phase6.gd` — must exist
7. `grep -c "msgid \"ui\." locale/en.po` — count must be >= 44 higher than before this plan
</verification>

<success_criteria>
- ChestEntity.opened signal declared and emitted before chest panel opens
- Inventory.item_added(builder_id, def_id, count) declared and emitted in _apply_add()
- 7 GUT test stubs exist and report PENDING (not syntax errors)
- conftest_phase6.gd provides make_avatar_cfg(), make_telemetry_cfg_path(), make_world_index_path()
- 44 new Phase 6 EN i18n keys exist in locale/en.po (empty msgstr stubs)
- 6 asset stub files exist at their required paths
</success_criteria>

<output>
Create `.planning/phases/06-first-five-minutes/06-01-SUMMARY.md` when done
</output>
