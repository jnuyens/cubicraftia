---
phase: 06-first-five-minutes
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - src/ui/avatar_creator.gd
  - src/ui/avatar_creator.tscn
  - src/autoload/friends_client.gd
  - assets/textures/ui/avatar_presets/preset_1.png
  - assets/textures/ui/avatar_presets/preset_2.png
  - assets/textures/ui/avatar_presets/preset_3.png
  - assets/textures/ui/avatar_presets/preset_4.png
  - assets/textures/ui/avatar_presets/preset_5.png
  - assets/textures/ui/avatar_presets/preset_6.png
  - assets/textures/ui/avatar_presets/preset_7.png
  - assets/textures/ui/avatar_presets/preset_8.png
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Avatar creator shows 5-part customiser (skin, head/face, body/accessory, legs/shoes, hand) per 06-UI-SPEC Surface 2"
    - "8 preset tiles apply all 5 parts instantly; Randomise button picks a random valid combination"
    - "SubViewportContainer (256x256) shows rotating builder preview at 0.4 rad/s"
    - "Done button writes user://avatar.cfg and calls FriendsClient.save_avatar() fire-and-forget"
    - "avatar_creator emits avatar_complete on Done, avatar_cancelled on Back"
    - "FriendsClient.save_avatar(cfg) PATCHes Supabase profiles.avatar_json (fire-and-forget)"
  artifacts:
    - path: "src/ui/avatar_creator.gd"
      provides: "5-part avatar customiser with 8 presets, preset tiles, randomise, SubViewport preview"
      exports: ["avatar_complete", "avatar_cancelled"]
    - path: "src/ui/avatar_creator.tscn"
      provides: "CanvasLayer layer=10 avatar creator scene"
    - path: "src/autoload/friends_client.gd"
      provides: "save_avatar(cfg: Dictionary) method + _avatar_req HTTPRequest node"
  key_links:
    - from: "avatar_creator.gd"
      to: "friends_client.gd"
      via: "FriendsClient.save_avatar(cfg) on Done"
      pattern: "FriendsClient\\.save_avatar"
    - from: "avatar_creator.gd"
      to: "user://avatar.cfg"
      via: "ConfigFile write on Done and preset tap"
      pattern: "avatar\\.cfg"
---

<objective>
Avatar / Builder Creator (Surface 2): 5-part customiser, 8 presets, Randomise, SubViewport 3D preview, save to user://avatar.cfg + Supabase.

Purpose: First-time users see the avatar creator immediately after account creation. It must be functional and aesthetically coherent. The SubViewport preview (builder.tscn rendering) closes the loop between the UI controls and the in-world builder appearance.

Output: avatar_creator.gd/.tscn (CanvasLayer layer=10), FriendsClient.save_avatar() method, 8 preset placeholder PNG stubs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-first-five-minutes/06-CONTEXT.md
@.planning/phases/06-first-five-minutes/06-UI-SPEC.md
@.planning/phases/06-first-five-minutes/06-01-SUMMARY.md
@.planning/phases/06-first-five-minutes/06-02-SUMMARY.md
</context>

<interfaces>
<!-- Verified interfaces the avatar creator needs -->

From src/autoload/friends_client.gd (Phase 5 — existing pattern for HTTP nodes):
```gdscript
# Per-concern HTTPRequest node pattern (existing in FriendsClient):
@onready var _friendships_req: HTTPRequest = HTTPRequest.new()
# New addition follows same pattern:
@onready var _avatar_req: HTTPRequest = HTTPRequest.new()
# In _ready(): add_child(_avatar_req)

# save_avatar method (new — to be added):
func save_avatar(cfg: Dictionary) -> void:
    # Fire-and-forget PATCH to /rest/v1/profiles?id=eq.<uid>
    # Uses _avatar_req.request() — failure is silent (local cfg is canonical)
    var body := JSON.stringify({"avatar_json": JSON.stringify(cfg)})
    var headers := [...] # same auth headers pattern as existing methods

# Avatar config dict schema (locked from 06-CONTEXT.md Area 2):
# { skin_colour_index: int(0-4), head_shape: String, face_expression: String,
#   body_colour_index: int(0-9), body_accessory: String, leg_colour_index: int(0-9),
#   leg_shoes: String, hand_accessory: String }
```

From src/builder/builder.gd (Plan 06-07 will add apply_avatar_config — but for the SubViewport preview in this plan, implement a preview-only version in avatar_creator.gd):
```gdscript
# The SubViewport contains a builder.tscn instance.
# Until Plan 06-07 adds Builder.apply_avatar_config(), the preview applies
# avatar changes by directly manipulating child MeshInstance3D material colours.
# This is the pre-07 compatibility path. Plan 06-07 will upgrade it.
```

From 06-UI-SPEC.md Surface 2 — preset grid spec:
```
# 8 presets stored as constants in avatar_creator.gd
# Preset tile: Button, 72x72px, image from assets/textures/ui/avatar_presets/preset_N.png
# Active preset: 2px accent-yellow border
# Randomise button: StyleBox_button_secondary, icon_dice.png prefix
```

From 06-CONTEXT.md Area 2 — 8 presets (diverse skin, gender presentation, accessories):
```gdscript
# The 8 preset dictionaries must be diverse. Example config (not exhaustive):
const PRESETS: Array[Dictionary] = [
    {"skin_colour_index": 0, "head_shape": "square", "face_expression": "neutral", "body_colour_index": 2, "body_accessory": "none", "leg_colour_index": 2, "leg_shoes": "none", "hand_accessory": "none"},
    # ... 7 more with diverse combinations
]
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FriendsClient.save_avatar() method + _avatar_req node</name>
  <files>src/autoload/friends_client.gd</files>
  <behavior>
    - FriendsClient.save_avatar({}) can be called without crashing even when not signed in (fire-and-forget guard)
    - save_avatar() sends a PATCH request to "/rest/v1/profiles?id=eq.<uid>" with avatar_json in body
    - If _avatar_req is busy (previous request in flight), the new call is silently dropped (fire-and-forget semantics)
    - save_avatar() does not await completion; it returns immediately
  </behavior>
  <action>
    Read `src/autoload/friends_client.gd` to identify the exact pattern for existing HTTPRequest nodes (e.g., _friendships_req). Replicate the pattern:

    Add `@onready var _avatar_req: HTTPRequest = HTTPRequest.new()` in the node declarations.
    In `_ready()`: add `add_child(_avatar_req)`.

    Add method `func save_avatar(cfg: Dictionary) -> void:`:
    - Guard: `if not is_signed_in(): return`
    - Guard: `if _avatar_req.get_http_client_status() != HTTPClient.STATUS_DISCONNECTED: return` (drop if in-flight)
    - Build body: `var body := JSON.stringify({"avatar_json": JSON.stringify(cfg)})`
    - Build headers: same auth header array pattern as existing methods (Authorization: Bearer token, Content-Type: application/json, apikey header)
    - Call `_avatar_req.request(_supabase_rest_url + "/profiles?id=eq." + get_user_id(), headers, HTTPClient.METHOD_PATCH, body)` — read existing methods to get the exact supabase REST URL constant name and header construction pattern
    - No signal connection needed for the response (fire-and-forget — failure is silent)

    This is a small addition (~20 lines). Read the file first, find the last existing method, and insert after it. Do NOT change any existing FriendsClient logic.
  </action>
  <verify>
    <automated>grep -n "save_avatar" src/autoload/friends_client.gd && grep -n "_avatar_req" src/autoload/friends_client.gd</automated>
  </verify>
  <done>save_avatar method exists; _avatar_req node declared and added in _ready(); GUT test for avatar preset validity no longer reports PENDING for the FriendsClient hook test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: avatar_creator.gd / .tscn + preset stubs</name>
  <files>
    src/ui/avatar_creator.gd,
    src/ui/avatar_creator.tscn,
    assets/textures/ui/avatar_presets/preset_1.png,
    assets/textures/ui/avatar_presets/preset_2.png,
    assets/textures/ui/avatar_presets/preset_3.png,
    assets/textures/ui/avatar_presets/preset_4.png,
    assets/textures/ui/avatar_presets/preset_5.png,
    assets/textures/ui/avatar_presets/preset_6.png,
    assets/textures/ui/avatar_presets/preset_7.png,
    assets/textures/ui/avatar_presets/preset_8.png
  </files>
  <behavior>
    - avatar_creator._current_config() returns a Dictionary with all 8 required keys
    - Tapping preset 1 sets all 8 keys to PRESETS[0] values instantly
    - Randomise button sets a random valid combination (all 8 keys present, all values within valid ranges)
    - Done button emits avatar_complete after writing user://avatar.cfg
    - Back button emits avatar_cancelled without saving
    - SubViewport preview builder rotates at 0.4 rad/s in _process
    - Skin swatch selection changes skin_colour_index and updates preview
    - Parts written to user://avatar.cfg on preset tap (fire-and-forget save for force-quit resilience)
    - FriendsClient.save_avatar() called on Done tap (not on every part change)
  </behavior>
  <action>
    Create `src/ui/avatar_creator.gd` (extends CanvasLayer):

    Constants:
    - `const SKIN_COLOURS: Array[Color] = [Color("#FDDCB5"), Color("#D4956A"), Color("#A0614A"), Color("#6B3A2A"), Color("#3B1F16")]` (5 swatches per UI-SPEC Surface 2)
    - `const BODY_COLOURS: Array[Color]` — 10 colours from the 18-colour palette subset (red, orange, yellow, lime, green, teal, blue, purple, pink, white — use the palette values from BrickRegistry or hardcode from the Phase 2 palette)
    - `const HEAD_SHAPES: Array[String] = ["square", "round", "tall"]`
    - `const FACE_EXPRESSIONS: Array[String] = ["neutral", "happy", "cool", "surprised", "sleepy"]`
    - `const BODY_ACCESSORIES: Array[String] = ["none", "backpack", "cape"]`
    - `const LEG_SHOES: Array[String] = ["none", "boots", "sneakers"]`
    - `const HAND_ACCESSORIES: Array[String] = ["none", "pickaxe", "lantern", "flower", "blank"]`
    - `const PRESETS: Array[Dictionary] = [...]` — 8 preset configs. Make them diverse: vary skin_colour_index (0,1,2,3,4 covering all 5), head shapes, expressions, accessories. All 8 must be distinct. Include at least 2 of each skin tone coverage across the 8 presets.

    Signals: `signal avatar_complete()`, `signal avatar_cancelled()`

    State: `var _cfg: Dictionary` — starts from default or loaded from user://avatar.cfg if it exists.

    `_ready()`:
    - Load existing avatar.cfg if present (user restoring a previous customisation)
    - Connect all part buttons/swatches to their handlers
    - Connect preset tile buttons
    - Connect Done/Back/Randomise buttons
    - Apply initial _cfg to all UI controls (set selected states)
    - Apply initial _cfg to SubViewport preview

    `func _current_config() -> Dictionary:` — returns _cfg snapshot.

    `func _apply_config_to_preview(cfg: Dictionary) -> void:` — applies avatar colours to the SubViewport builder. Since Builder.apply_avatar_config() is not yet available (Plan 06-07 adds it), this plan applies colours directly to the builder's child MeshInstance3D StandardMaterial3D nodes by name (Body, Legs, Head — nodes that will exist in builder.tscn after Plan 06-07 refactors them). For the preview in this plan, set the albedo_color of the builder's primary MeshInstance3D to the body colour index. This is a preview approximation — Plan 06-07 upgrades it to full multi-part rendering.

    `_process(delta)`: rotate SubViewport builder: `_preview_builder.rotation.y += 0.4 * delta` (if _preview_builder is not null).

    `func _on_done_pressed() -> void:`:
    - Write user://avatar.cfg (ConfigFile, [avatar] section, all 8 keys)
    - Call FriendsClient.save_avatar(_cfg) — fire-and-forget
    - OnboardingTelemetry.log("avatar_complete")
    - Emit avatar_complete

    `func _on_back_pressed() -> void:`: emit avatar_cancelled.

    `func _on_preset_pressed(idx: int) -> void:`: _cfg = PRESETS[idx].duplicate(); update all UI controls; update preview; _write_avatar_cfg_silent() (save to disk immediately, fire-and-forget).

    `func _on_randomise_pressed() -> void:`: generate a random valid _cfg dict; apply same as preset tap.

    `func _write_avatar_cfg_silent() -> void:`: writes _cfg to user://avatar.cfg without emitting avatar_complete (used for preset tap saves).

    Create `src/ui/avatar_creator.tscn` as a CanvasLayer (layer=10) with:
    - Root CanvasLayer assigns avatar_creator.gd as script
    - PanelContainer centred (560px wide on desktop, full-width - 32px on mobile via theme override)
    - HBoxContainer inside: left VBoxContainer (2/3 width, all part sections) + right SubViewportContainer 256x256px
    - Preset GridContainer (4 columns, 8 tiles) above part sections
    - Part sections per UI-SPEC Surface 2 (Skin 5 swatches, Head shape+expression rows, Body colour+accessory, Legs colour+shoes, Hand 5 tiles)
    - Footer HBoxContainer: Done button (primary), Back button (secondary)
    - SubViewport inside SubViewportContainer: update_mode = SubViewport.UPDATE_WHEN_VISIBLE; contains builder.tscn instance (load from res://src/builder/builder.tscn if path exists, else use a placeholder Capsule MeshInstance3D)
    - Camera3D inside SubViewport: positioned to see the builder from slightly above and to the side

    Create 8 preset PNG stubs (1×1 px transparent PNGs) at `assets/textures/ui/avatar_presets/preset_N.png` for N in 1..8. Same minimal PNG approach as Plan 06-01 Task 2 asset stubs. These will be replaced by real artwork later.
  </action>
  <verify>
    <automated>ls src/ui/avatar_creator.gd src/ui/avatar_creator.tscn && grep -c "signal avatar_complete" src/ui/avatar_creator.gd && grep -c "PRESETS" src/ui/avatar_creator.gd && ls assets/textures/ui/avatar_presets/preset_1.png assets/textures/ui/avatar_presets/preset_8.png && godot --headless -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gprefix=test_avatar -gsuffix=.gd -gexit 2>&1 | tail -5</automated>
  </verify>
  <done>avatar_creator.gd/.tscn created; 8 presets defined with diverse configs; Done emits avatar_complete and writes avatar.cfg; Back emits avatar_cancelled; FriendsClient.save_avatar() called on Done; 8 preset stub PNGs exist; GUT avatar tests advance from PENDING (stubs will pass or update to assert real values).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| avatar_creator → FriendsClient.save_avatar() → Supabase | Avatar JSON contains only enum indices (no PII); Supabase RLS enforces per-user update |
| user://avatar.cfg → filesystem | ConfigFile in app sandbox; not accessible to other apps on iOS/Android |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-S1 | Spoofing | Avatar config injection via malformed cfg dict | accept | avatar_json is cosmetic only (no gameplay impact); server trusts client for cosmetic values per RESEARCH.md T-06-S1 |
| T-06-I1 | Information Disclosure | avatar_json contains PII | mitigate | Keys are only integer indices and fixed string enum values; no names, no email, no location — verified in RESEARCH.md T-06-I1 |
| T-06-SC | Tampering | No new package installs | accept | No new packages |
</threat_model>

<verification>
1. `grep "signal avatar_complete" src/ui/avatar_creator.gd` — must exist
2. `grep "signal avatar_cancelled" src/ui/avatar_creator.gd` — must exist
3. `grep -c "PRESETS" src/ui/avatar_creator.gd` — constant present
4. `grep "FriendsClient.save_avatar" src/ui/avatar_creator.gd` — called in _on_done_pressed
5. `grep "avatar_complete\|avatar_cancelled" src/ui/avatar_creator.gd` — both emitted
6. `grep "save_avatar" src/autoload/friends_client.gd` — method exists
7. `ls assets/textures/ui/avatar_presets/preset_{1..8}.png` — all 8 stub files exist
8. `grep "OnboardingTelemetry.log.*avatar" src/ui/avatar_creator.gd` — telemetry calls present
</verification>

<success_criteria>
- Avatar creator opens as CanvasLayer layer=10 with 5 part sections
- 8 presets each cover all 5 parts; presets are diverse in skin/gender/accessory
- Randomise generates a random valid combination
- Done writes user://avatar.cfg with all 8 keys and calls FriendsClient.save_avatar()
- Back emits avatar_cancelled without saving
- SubViewport preview shows rotating builder that responds to part changes
- FriendsClient.save_avatar() is fire-and-forget; failure is silent
</success_criteria>

<output>
Create `.planning/phases/06-first-five-minutes/06-04-SUMMARY.md` when done
</output>
